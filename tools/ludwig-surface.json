{
 "combiners": {
  "options": {
   "comparator": {
    "activation": {
     "default": "relu",
     "description": "Default activation function applied to the output of the fully connected layers.",
     "enum": [
      "elu",
      "leakyRelu",
      "logSigmoid",
      "relu",
      "sigmoid",
      "tanh",
      "softmax",
      "gelu",
      "silu",
      "swish",
      "mish",
      "selu",
      "prelu",
      "relu6",
      "hardswish",
      "hardsigmoid",
      "softplus",
      "celu",
      "swiglu",
      "geglu",
      "reglu",
      "sparsemax",
      "entmax15",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "bias_initializer": {
     "default": "zeros",
     "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    },
    "dropout": {
     "default": 0.0,
     "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "entity_1": {
     "default": null,
     "description": "The list of input feature names `[feature_1, feature_2, ...]` constituting the first entity to compare. *Required*.",
     "type": null
    },
    "entity_2": {
     "default": null,
     "description": "The list of input feature names `[feature_1, feature_2, ...]` constituting the second entity to compare. *Required*.",
     "type": null
    },
    "fc_layers": {
     "default": null,
     "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
     "type": null
    },
    "norm": {
     "default": null,
     "description": "Default normalization applied at the beginnging of fully connected layers.",
     "enum": [
      "batch",
      "layer",
      "ghost",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "norm_params": {
     "default": null,
     "description": "Default parameters passed to the `norm` module.",
     "nullable": true,
     "type": "object"
    },
    "num_fc_layers": {
     "default": 1,
     "description": "Number of stacked fully connected layers to apply. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
     "minimum": 0,
     "type": "integer"
    },
    "output_size": {
     "default": 256,
     "description": "Output size of a fully connected layer.",
     "minimum": 1,
     "type": "integer"
    },
    "use_bias": {
     "default": true,
     "description": "Whether the layer uses a bias vector.",
     "type": "boolean"
    },
    "weights_initializer": {
     "default": "xavier_uniform",
     "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    }
   },
   "concat": {
    "activation": {
     "default": "relu",
     "description": "Default activation function applied to the output of the fully connected layers.",
     "enum": [
      "elu",
      "leakyRelu",
      "logSigmoid",
      "relu",
      "sigmoid",
      "tanh",
      "softmax",
      "gelu",
      "silu",
      "swish",
      "mish",
      "selu",
      "prelu",
      "relu6",
      "hardswish",
      "hardsigmoid",
      "softplus",
      "celu",
      "swiglu",
      "geglu",
      "reglu",
      "sparsemax",
      "entmax15",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "batch_ensemble": {
     "default": false,
     "description": "Whether to use BatchEnsemble (TabM-style) for parameter-efficient ensembling. Adds per-member rank-1 scaling vectors to the output layer, providing ensemble-level performance at single-model cost.",
     "type": "boolean"
    },
    "bias_initializer": {
     "default": "zeros",
     "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    },
    "dropout": {
     "default": 0.0,
     "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "fc_layers": {
     "default": null,
     "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
     "type": null
    },
    "flatten_inputs": {
     "default": false,
     "description": "Whether to flatten input tensors to a vector.",
     "type": "boolean"
    },
    "norm": {
     "default": null,
     "description": "Default normalization applied at the beginnging of fully connected layers.",
     "enum": [
      "batch",
      "layer",
      "ghost",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "norm_params": {
     "default": null,
     "description": "Default parameters passed to the `norm` module.",
     "nullable": true,
     "type": "object"
    },
    "num_ensemble_members": {
     "default": 4,
     "description": "Number of ensemble members when batch_ensemble is enabled.",
     "minimum": 1,
     "type": "integer"
    },
    "num_fc_layers": {
     "default": 0,
     "description": "Number of stacked fully connected layers to apply. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
     "minimum": 0,
     "type": "integer"
    },
    "output_size": {
     "default": 256,
     "description": "Output size of a fully connected layer.",
     "minimum": 1,
     "type": "integer"
    },
    "residual": {
     "default": false,
     "description": "Whether to add a residual connection to each fully connected layer block. Requires all fully connected layers to have the same `output_size`.",
     "type": "boolean"
    },
    "use_bias": {
     "default": true,
     "description": "Whether the layer uses a bias vector.",
     "type": "boolean"
    },
    "weights_initializer": {
     "default": "xavier_uniform",
     "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    }
   },
   "cross_attention": {
    "activation": {
     "default": "relu",
     "description": "Default activation function applied to the output of the fully connected layers.",
     "enum": [
      "elu",
      "leakyRelu",
      "logSigmoid",
      "relu",
      "sigmoid",
      "tanh",
      "softmax",
      "gelu",
      "silu",
      "swish",
      "mish",
      "selu",
      "prelu",
      "relu6",
      "hardswish",
      "hardsigmoid",
      "softplus",
      "celu",
      "swiglu",
      "geglu",
      "reglu",
      "sparsemax",
      "entmax15",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "bias_initializer": {
     "default": "zeros",
     "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    },
    "dropout": {
     "default": 0.1,
     "description": "Dropout rate for the cross-attention layers.",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "fc_layers": {
     "default": null,
     "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
     "type": null
    },
    "hidden_size": {
     "default": 256,
     "description": "Hidden size of the cross-attention layers. Each input feature is projected to this size.",
     "minimum": 1,
     "type": "integer"
    },
    "norm": {
     "default": null,
     "description": "Default normalization applied at the beginnging of fully connected layers.",
     "enum": [
      "batch",
      "layer",
      "ghost",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "norm_params": {
     "default": null,
     "description": "Default parameters passed to the `norm` module.",
     "nullable": true,
     "type": "object"
    },
    "num_fc_layers": {
     "default": 0,
     "description": "Number of stacked fully connected layers to apply. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
     "minimum": 0,
     "type": "integer"
    },
    "num_heads": {
     "default": 8,
     "description": "Number of attention heads in the cross-attention layers.",
     "minimum": 1,
     "type": "integer"
    },
    "num_layers": {
     "default": 1,
     "description": "Number of stacked cross-attention layers.",
     "minimum": 1,
     "type": "integer"
    },
    "output_size": {
     "default": 256,
     "description": "Output size of the fully connected layer after cross-attention.",
     "minimum": 1,
     "type": "integer"
    },
    "use_bias": {
     "default": true,
     "description": "Whether the layer uses a bias vector.",
     "type": "boolean"
    },
    "weights_initializer": {
     "default": "xavier_uniform",
     "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    }
   },
   "ft_transformer": {
    "bias_initializer": {
     "default": "zeros",
     "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    },
    "dropout": {
     "default": 0.1,
     "description": "Dropout rate for the transformer block.",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "fc_activation": {
     "default": "relu",
     "description": "Default activation function applied to the output of the fully connected layers.",
     "enum": [
      "elu",
      "leakyRelu",
      "logSigmoid",
      "relu",
      "sigmoid",
      "tanh",
      "softmax",
      "gelu",
      "silu",
      "swish",
      "mish",
      "selu",
      "prelu",
      "relu6",
      "hardswish",
      "hardsigmoid",
      "softplus",
      "celu",
      "swiglu",
      "geglu",
      "reglu",
      "sparsemax",
      "entmax15",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "fc_dropout": {
     "default": 0.0,
     "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "fc_layers": {
     "default": null,
     "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
     "type": null
    },
    "fc_residual": {
     "default": false,
     "description": "Whether to add a residual connection to each fully connected layer block. Requires all fully connected layers to have the same `output_size`.",
     "type": "boolean"
    },
    "hidden_size": {
     "default": 256,
     "description": "The number of hidden units of the TransformerStack as well as the dimension that each incoming input feature is projected to before feeding to the TransformerStack.",
     "minimum": 0,
     "type": "integer"
    },
    "norm": {
     "default": null,
     "description": "Default normalization applied at the beginnging of fully connected layers.",
     "enum": [
      "batch",
      "layer",
      "ghost",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "norm_params": {
     "default": null,
     "description": "Default parameters passed to the `norm` module.",
     "nullable": true,
     "type": "object"
    },
    "num_fc_layers": {
     "default": 0,
     "description": "The number of stacked fully connected layers (only applies if `reduce_output` is not null).",
     "minimum": 0,
     "type": "integer"
    },
    "num_heads": {
     "default": 8,
     "description": "Number of heads of the self attention in the transformer block.",
     "minimum": 0,
     "type": "integer"
    },
    "num_layers": {
     "default": 1,
     "description": "The number of transformer layers.",
     "minimum": 1,
     "type": "integer"
    },
    "output_size": {
     "default": 256,
     "description": "Output size of a fully connected layer.",
     "minimum": 1,
     "type": "integer"
    },
    "transformer_output_size": {
     "default": 256,
     "description": "Size of the fully connected layer after self attention in the transformer block. This is usually the same as `hidden_size` and `embedding_size`.",
     "minimum": 0,
     "type": "integer"
    },
    "use_bias": {
     "default": true,
     "description": "Whether the layer uses a bias vector.",
     "type": "boolean"
    },
    "weights_initializer": {
     "default": "xavier_uniform",
     "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    }
   },
   "gated_fusion": {
    "activation": {
     "default": "relu",
     "description": "Default activation function applied to the output of the fully connected layers.",
     "enum": [
      "elu",
      "leakyRelu",
      "logSigmoid",
      "relu",
      "sigmoid",
      "tanh",
      "softmax",
      "gelu",
      "silu",
      "swish",
      "mish",
      "selu",
      "prelu",
      "relu6",
      "hardswish",
      "hardsigmoid",
      "softplus",
      "celu",
      "swiglu",
      "geglu",
      "reglu",
      "sparsemax",
      "entmax15",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "bias_initializer": {
     "default": "zeros",
     "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    },
    "dropout": {
     "default": 0.1,
     "description": "Dropout rate for the gating layers.",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "fc_layers": {
     "default": null,
     "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
     "type": null
    },
    "hidden_size": {
     "default": 256,
     "description": "Hidden size of the gating layers. Each input feature is projected to this size.",
     "minimum": 1,
     "type": "integer"
    },
    "norm": {
     "default": null,
     "description": "Default normalization applied at the beginnging of fully connected layers.",
     "enum": [
      "batch",
      "layer",
      "ghost",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "norm_params": {
     "default": null,
     "description": "Default parameters passed to the `norm` module.",
     "nullable": true,
     "type": "object"
    },
    "num_fc_layers": {
     "default": 0,
     "description": "Number of stacked fully connected layers to apply. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
     "minimum": 0,
     "type": "integer"
    },
    "output_size": {
     "default": 256,
     "description": "Output size of the fully connected layer after gated fusion.",
     "minimum": 1,
     "type": "integer"
    },
    "use_bias": {
     "default": true,
     "description": "Whether the layer uses a bias vector.",
     "type": "boolean"
    },
    "weights_initializer": {
     "default": "xavier_uniform",
     "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    }
   },
   "hypernetwork": {
    "activation": {
     "default": "relu",
     "description": "Default activation function applied to the output of the fully connected layers.",
     "enum": [
      "elu",
      "leakyRelu",
      "logSigmoid",
      "relu",
      "sigmoid",
      "tanh",
      "softmax",
      "gelu",
      "silu",
      "swish",
      "mish",
      "selu",
      "prelu",
      "relu6",
      "hardswish",
      "hardsigmoid",
      "softplus",
      "celu",
      "swiglu",
      "geglu",
      "reglu",
      "sparsemax",
      "entmax15",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "dropout": {
     "default": 0.1,
     "description": "Dropout rate.",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "hidden_size": {
     "default": 128,
     "description": "Hidden size for feature projections.",
     "minimum": 1,
     "type": "integer"
    },
    "hyper_hidden_size": {
     "default": 64,
     "description": "Hidden size inside the hypernetwork weight generator.",
     "minimum": 1,
     "type": "integer"
    },
    "num_fc_layers": {
     "default": 0,
     "description": "Number of stacked fully connected layers to apply. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
     "minimum": 0,
     "type": "integer"
    },
    "output_size": {
     "default": 128,
     "description": "Output size of the FC stack.",
     "minimum": 1,
     "type": "integer"
    }
   },
   "perceiver": {
    "activation": {
     "default": "relu",
     "description": "Default activation function applied to the output of the fully connected layers.",
     "enum": [
      "elu",
      "leakyRelu",
      "logSigmoid",
      "relu",
      "sigmoid",
      "tanh",
      "softmax",
      "gelu",
      "silu",
      "swish",
      "mish",
      "selu",
      "prelu",
      "relu6",
      "hardswish",
      "hardsigmoid",
      "softplus",
      "celu",
      "swiglu",
      "geglu",
      "reglu",
      "sparsemax",
      "entmax15",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "bias_initializer": {
     "default": "zeros",
     "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    },
    "dropout": {
     "default": 0.1,
     "description": "Dropout rate for the attention layers.",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "fc_layers": {
     "default": null,
     "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
     "type": null
    },
    "latent_dim": {
     "default": 256,
     "description": "Dimensionality of each latent query vector.",
     "minimum": 1,
     "type": "integer"
    },
    "norm": {
     "default": null,
     "description": "Default normalization applied at the beginnging of fully connected layers.",
     "enum": [
      "batch",
      "layer",
      "ghost",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "norm_params": {
     "default": null,
     "description": "Default parameters passed to the `norm` module.",
     "nullable": true,
     "type": "object"
    },
    "num_fc_layers": {
     "default": 0,
     "description": "Number of stacked fully connected layers to apply. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
     "minimum": 0,
     "type": "integer"
    },
    "num_heads": {
     "default": 8,
     "description": "Number of attention heads in the cross-attention and self-attention layers.",
     "minimum": 1,
     "type": "integer"
    },
    "num_latents": {
     "default": 32,
     "description": "Number of learned latent query vectors.",
     "minimum": 1,
     "type": "integer"
    },
    "num_self_attention_layers": {
     "default": 2,
     "description": "Number of self-attention layers applied to the latent queries after cross-attention.",
     "minimum": 1,
     "type": "integer"
    },
    "output_size": {
     "default": 256,
     "description": "Output size of the fully connected layer after the Perceiver block.",
     "minimum": 1,
     "type": "integer"
    },
    "reduce_output": {
     "default": "mean",
     "description": "Strategy to use to aggregate the latent vectors before the FC stack.",
     "enum": [
      "last",
      "sum",
      "mean",
      "avg",
      "max",
      "concat",
      "attention",
      "attention_pooling",
      "none",
      "None",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "use_bias": {
     "default": true,
     "description": "Whether the layer uses a bias vector.",
     "type": "boolean"
    },
    "weights_initializer": {
     "default": "xavier_uniform",
     "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    }
   },
   "project_aggregate": {
    "activation": {
     "default": "relu",
     "description": "Activation to apply to each fully connected layer.",
     "enum": [
      "elu",
      "leakyRelu",
      "logSigmoid",
      "relu",
      "sigmoid",
      "tanh",
      "softmax",
      "gelu",
      "silu",
      "swish",
      "mish",
      "selu",
      "prelu",
      "relu6",
      "hardswish",
      "hardsigmoid",
      "softplus",
      "celu",
      "swiglu",
      "geglu",
      "reglu",
      "sparsemax",
      "entmax15",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "bias_initializer": {
     "default": "zeros",
     "description": "Initializer to use for the bias of the projection and for the fully connected layers.",
     "type": null
    },
    "dropout": {
     "default": 0.0,
     "description": "Dropout rate to apply to each fully connected layer.",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "fc_layers": {
     "default": null,
     "description": "Full specification of the fully connected layers after the aggregation. It should be a list of dict, each dict representing one layer of the fully connected layer stack.",
     "type": null
    },
    "norm": {
     "default": "layer",
     "description": "Normalization to apply to each projection and fully connected layer.",
     "enum": [
      "batch",
      "layer"
     ],
     "nullable": true,
     "type": "string"
    },
    "norm_params": {
     "default": null,
     "description": "Parameters of the normalization to apply to each projection and fully connected layer.",
     "nullable": true,
     "type": "object"
    },
    "num_fc_layers": {
     "default": 2,
     "description": "Number of fully connected layers after aggregation.",
     "minimum": 0,
     "type": "integer"
    },
    "output_size": {
     "default": 128,
     "description": "Output size of each layer of the stack of fully connected layers.",
     "minimum": 1,
     "type": "integer"
    },
    "projection_size": {
     "default": 128,
     "description": "All combiner inputs are projected to this size before being aggregated.",
     "minimum": 1,
     "type": "integer"
    },
    "residual": {
     "default": true,
     "description": "Whether to add residual skip connection between the fully connected layers in the stack.",
     "type": "boolean"
    },
    "use_bias": {
     "default": true,
     "description": "Whether the layers use a bias vector.",
     "type": "boolean"
    },
    "weights_initializer": {
     "default": "xavier_uniform",
     "description": "Initializer to use for the weights of the projection and for the fully connected layers.",
     "type": null
    }
   },
   "sequence": {
    "encoder": {
     "type": "object"
    },
    "main_sequence_feature": {
     "default": null,
     "description": "Name of a sequence, text, or time series feature to concatenate the outputs of the other features to. If no `main_sequence_feature` is specified, the combiner will look through all the features in the order they are defined in the configuration and will look for a feature with a rank 3 tensor output (sequence, text or time series). If it cannot find one it will raise an exception, otherwise the output of that feature will be used for concatenating the other features along the sequence `s` dimension. If there are other input features with a rank 3 output tensor, the combiner will concatenate them alongside the `s` dimension. All sequence-like input features must have identical `s` dimension, otherwise an error will be thrown.",
     "nullable": true,
     "type": "string"
    },
    "reduce_output": {
     "default": null,
     "description": "Strategy to use to aggregate the embeddings of the items of the set.",
     "enum": [
      "last",
      "sum",
      "mean",
      "avg",
      "max",
      "concat",
      "attention",
      "attention_pooling",
      "none",
      "None",
      null
     ],
     "nullable": true,
     "type": "string"
    }
   },
   "sequence_concat": {
    "main_sequence_feature": {
     "default": null,
     "description": "Name of a sequence, text, or time series feature to concatenate the outputs of the other features to. If no `main_sequence_feature` is specified, the combiner will look through all the features in the order they are defined in the configuration and will look for a feature with a rank 3 tensor output (sequence, text or time series). If it cannot find one it will raise an exception, otherwise the output of that feature will be used for concatenating the other features along the sequence `s` dimension. If there are other input features with a rank 3 output tensor, the combiner will concatenate them alongside the `s` dimension. All sequence-like input features must have identical `s` dimension, otherwise an error will be thrown.",
     "nullable": true,
     "type": "string"
    },
    "reduce_output": {
     "default": null,
     "description": "Strategy to use to aggregate the embeddings of the items of the set.",
     "enum": [
      "last",
      "sum",
      "mean",
      "avg",
      "max",
      "concat",
      "attention",
      "attention_pooling",
      "none",
      "None",
      null
     ],
     "nullable": true,
     "type": "string"
    }
   },
   "tabnet": {
    "bn_epsilon": {
     "default": 0.001,
     "description": "Epsilon to be added to the batch norm denominator.",
     "type": "number"
    },
    "bn_momentum": {
     "default": 0.05,
     "description": "Momentum of the batch norm. 1 - `m_B` from the TabNet paper.",
     "type": "number"
    },
    "bn_virtual_bs": {
     "default": 1024,
     "description": "Size of the virtual batch size used by ghost batch norm. If null, regular batch norm is used instead. `B_v` from the TabNet paper.",
     "minimum": 1,
     "nullable": true,
     "type": "integer"
    },
    "dropout": {
     "default": 0.05,
     "description": "Dropout rate for the transformer block.",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "entmax_alpha": {
     "default": 1.5,
     "description": "Must be a number between 1.0 and 2.0. If entmax_mode is `adaptive`, `entmax_alpha` is used as the initial value for the learnable parameter. 1 corresponds to softmax, 2 is sparsemax.",
     "maximum": 2,
     "minimum": 1,
     "type": "number"
    },
    "entmax_mode": {
     "default": "sparsemax",
     "description": "Entmax is a sparse family of probability mapping which generalizes softmax and sparsemax. `entmax_mode` controls the sparsity",
     "enum": [
      "entmax15",
      "sparsemax",
      "constant",
      "adaptive"
     ],
     "type": "string"
    },
    "num_shared_blocks": {
     "default": 2,
     "description": "Number of shared feature transformer blocks across the steps.",
     "minimum": 0,
     "type": "integer"
    },
    "num_steps": {
     "default": 3,
     "description": "Number of steps / repetitions of the the attentive transformer and feature transformer computations. `N_steps` in (Arik and Pfister, 2019).",
     "minimum": 0,
     "type": "integer"
    },
    "num_total_blocks": {
     "default": 4,
     "description": "Total number of feature transformer blocks at each step.",
     "minimum": 0,
     "type": "integer"
    },
    "output_size": {
     "default": 128,
     "description": "Output size of a fully connected layer. `N_d` in (Arik and Pfister, 2019).",
     "minimum": 1,
     "type": "integer"
    },
    "relaxation_factor": {
     "default": 1.5,
     "description": "Factor that influences how many times a feature should be used across the steps of computation. a value of 1 implies it each feature should be use once, a higher value allows for multiple usages. `gamma` in (Arik and Pfister, 2019).",
     "type": "number"
    },
    "size": {
     "default": 32,
     "description": "Size of the hidden layers. `N_a` in (Arik and Pfister, 2019).",
     "minimum": 1,
     "type": "integer"
    },
    "sparsity": {
     "default": 0.0001,
     "description": "Multiplier of the sparsity inducing loss. `lambda_sparse` in (Arik and Pfister, 2019).",
     "type": "number"
    }
   },
   "tabpfn_v2": {
    "device": {
     "default": "auto",
     "description": "Device used for TabPFN inference. 'auto' picks CUDA if available.",
     "enum": [
      "auto",
      "cpu",
      "cuda"
     ],
     "type": null
    },
    "n_estimators": {
     "default": 4,
     "description": "Number of TabPFN ensemble members to use during prediction. Higher values improve accuracy at the cost of inference latency.",
     "minimum": 1,
     "type": null
    },
    "output_size": {
     "default": 128,
     "description": "Width of the learnable projection head applied to TabPFN's encoder output.",
     "minimum": 1,
     "type": null
    },
    "tabpfn_hidden_size": {
     "default": 512,
     "description": "TabPFN v2's internal hidden width. The v2 default is 512; only change this if loading a variant.",
     "minimum": 1,
     "type": null
    }
   },
   "tabtransformer": {
    "bias_initializer": {
     "default": "zeros",
     "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    },
    "dropout": {
     "default": 0.1,
     "description": "Dropout rate for the transformer block.",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "embed_input_feature_name": {
     "default": null,
     "description": "This value controls the size of the embeddings. Valid values are `add` which uses the `hidden_size` value or an integer that is set to a specific value. In the case of an integer value, it must be smaller than hidden_size.",
     "type": null
    },
    "fc_activation": {
     "default": "relu",
     "description": "Default activation function applied to the output of the fully connected layers.",
     "enum": [
      "elu",
      "leakyRelu",
      "logSigmoid",
      "relu",
      "sigmoid",
      "tanh",
      "softmax",
      "gelu",
      "silu",
      "swish",
      "mish",
      "selu",
      "prelu",
      "relu6",
      "hardswish",
      "hardsigmoid",
      "softplus",
      "celu",
      "swiglu",
      "geglu",
      "reglu",
      "sparsemax",
      "entmax15",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "fc_dropout": {
     "default": 0.0,
     "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "fc_layers": {
     "default": null,
     "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
     "type": null
    },
    "fc_residual": {
     "default": false,
     "description": "Whether to add a residual connection to each fully connected layer block. Requires all fully connected layers to have the same `output_size`.",
     "type": "boolean"
    },
    "hidden_size": {
     "default": 256,
     "description": "The number of hidden units of the TransformerStack as well as the dimension that each incoming input feature is projected to before feeding to the TransformerStack.",
     "minimum": 0,
     "type": "integer"
    },
    "norm": {
     "default": null,
     "description": "Default normalization applied at the beginnging of fully connected layers.",
     "enum": [
      "batch",
      "layer",
      "ghost",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "norm_params": {
     "default": null,
     "description": "Default parameters passed to the `norm` module.",
     "nullable": true,
     "type": "object"
    },
    "num_fc_layers": {
     "default": 0,
     "description": "The number of stacked fully connected layers (only applies if `reduce_output` is not null).",
     "minimum": 0,
     "type": "integer"
    },
    "num_heads": {
     "default": 8,
     "description": "Number of heads of the self attention in the transformer block.",
     "minimum": 0,
     "type": "integer"
    },
    "num_layers": {
     "default": 1,
     "description": "The number of transformer layers.",
     "minimum": 1,
     "type": "integer"
    },
    "output_size": {
     "default": 256,
     "description": "Output size of a fully connected layer.",
     "minimum": 1,
     "type": "integer"
    },
    "reduce_output": {
     "default": "concat",
     "description": "Strategy to use to aggregate the output of the transformer.",
     "enum": [
      "last",
      "sum",
      "mean",
      "avg",
      "max",
      "concat",
      "attention",
      "attention_pooling",
      "none",
      "None",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "transformer_output_size": {
     "default": 256,
     "description": "Size of the fully connected layer after self attention in the transformer block. This is usually the same as `hidden_size` and `embedding_size`.",
     "minimum": 0,
     "type": "integer"
    },
    "use_bias": {
     "default": true,
     "description": "Whether the layer uses a bias vector.",
     "type": "boolean"
    },
    "weights_initializer": {
     "default": "xavier_uniform",
     "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    }
   },
   "transformer": {
    "bias_initializer": {
     "default": "zeros",
     "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    },
    "dropout": {
     "default": 0.1,
     "description": "Dropout rate for the transformer block.",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "fc_activation": {
     "default": "relu",
     "description": "Default activation function applied to the output of the fully connected layers.",
     "enum": [
      "elu",
      "leakyRelu",
      "logSigmoid",
      "relu",
      "sigmoid",
      "tanh",
      "softmax",
      "gelu",
      "silu",
      "swish",
      "mish",
      "selu",
      "prelu",
      "relu6",
      "hardswish",
      "hardsigmoid",
      "softplus",
      "celu",
      "swiglu",
      "geglu",
      "reglu",
      "sparsemax",
      "entmax15",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "fc_dropout": {
     "default": 0.0,
     "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "fc_layers": {
     "default": null,
     "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
     "type": null
    },
    "fc_residual": {
     "default": false,
     "description": "Whether to add a residual connection to each fully connected layer block. Requires all fully connected layers to have the same `output_size`.",
     "type": "boolean"
    },
    "hidden_size": {
     "default": 256,
     "description": "The number of hidden units of the TransformerStack as well as the dimension that each incoming input feature is projected to before feeding to the TransformerStack.",
     "minimum": 0,
     "type": "integer"
    },
    "norm": {
     "default": null,
     "description": "Default normalization applied at the beginnging of fully connected layers.",
     "enum": [
      "batch",
      "layer",
      "ghost",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "norm_params": {
     "default": null,
     "description": "Default parameters passed to the `norm` module.",
     "nullable": true,
     "type": "object"
    },
    "num_fc_layers": {
     "default": 0,
     "description": "The number of stacked fully connected layers (only applies if `reduce_output` is not null).",
     "minimum": 0,
     "type": "integer"
    },
    "num_heads": {
     "default": 8,
     "description": "Number of heads of the self attention in the transformer block.",
     "minimum": 0,
     "type": "integer"
    },
    "num_layers": {
     "default": 1,
     "description": "The number of transformer layers.",
     "minimum": 1,
     "type": "integer"
    },
    "output_size": {
     "default": 256,
     "description": "Output size of a fully connected layer.",
     "minimum": 1,
     "type": "integer"
    },
    "reduce_output": {
     "default": "mean",
     "description": "Strategy to use to aggregate the output of the transformer.",
     "enum": [
      "last",
      "sum",
      "mean",
      "avg",
      "max",
      "concat",
      "attention",
      "attention_pooling",
      "none",
      "None",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "transformer_output_size": {
     "default": 256,
     "description": "Size of the fully connected layer after self attention in the transformer block. This is usually the same as `hidden_size` and `embedding_size`.",
     "minimum": 0,
     "type": "integer"
    },
    "use_bias": {
     "default": true,
     "description": "Whether the layer uses a bias vector.",
     "type": "boolean"
    },
    "weights_initializer": {
     "default": "xavier_uniform",
     "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
     "type": null
    }
   }
  },
  "types": [
   "comparator",
   "concat",
   "cross_attention",
   "ft_transformer",
   "gated_fusion",
   "hypernetwork",
   "perceiver",
   "project_aggregate",
   "sequence",
   "sequence_concat",
   "tabnet",
   "tabpfn_v2",
   "tabtransformer",
   "transformer"
  ]
 },
 "feature_abbrev": {
  "anomaly": "anom",
  "audio": "aud",
  "bag": "bag",
  "binary": "bin",
  "category": "cat",
  "category_distribution": "catdist",
  "date": "dt",
  "h3": "h3",
  "image": "img",
  "number": "num",
  "sequence": "seq",
  "set": "set",
  "text": "txt",
  "timeseries": "ts",
  "vector": "vec"
 },
 "global_preprocessing": {
  "global_max_sequence_length": {
   "default": null,
   "description": "Specifically for LLMs. This is the maximum length of the input sequence going into the model's forward pass during training. Sequences will be truncated to this length after merging inputs and targets. If not set, the total length of the merged input and target token sequences will be used.",
   "minimum": 1,
   "nullable": true,
   "type": "integer"
  },
  "oversample_minority": {
   "default": null,
   "description": "If not None, the minority class will be oversampled to reach the specified ratio respective to the majority class.",
   "minimum": 0.0,
   "nullable": true,
   "type": "number"
  },
  "sample_ratio": {
   "default": 1.0,
   "description": "The ratio of the dataset to use. For instance, if 0.5, half of the dataset provided will be used.",
   "minimum": 0.0,
   "type": "number"
  },
  "sample_size": {
   "default": null,
   "description": "The maximum number of samples from the dataset to use. Cannot be set if sample_ratio is set to be < 1.0. If sample_ratio is set to 1.0, this will override the number of samples to used.",
   "minimum": 0,
   "nullable": true,
   "type": "number"
  },
  "split": {
   "type": "object"
  },
  "undersample_majority": {
   "default": null,
   "description": "If not None, the majority class will be undersampled to reach the specified ratio respective to the minority class.",
   "minimum": 0.0,
   "nullable": true,
   "type": "number"
  }
 },
 "hyperopt": {
  "executor": {
   "fields": {
    "cpu_resources_per_trial": {
     "default": 1,
     "description": "The number of CPU cores allocated to each trial",
     "minimum": 1,
     "type": "integer"
    },
    "gpu_resources_per_trial": {
     "default": 0,
     "description": "The number of GPU devices allocated to each trial",
     "minimum": 0,
     "type": "integer"
    },
    "kubernetes_namespace": {
     "default": null,
     "description": "When running on Kubernetes, provide the namespace of the Ray cluster to sync results between pods. See the Ray docs for more info.",
     "nullable": true,
     "type": "string"
    },
    "max_concurrent_trials": {
     "default": "auto",
     "description": "The maximum number of trials to train concurrently. Defaults to auto if not specified.",
     "type": null
    },
    "num_samples": {
     "default": null,
     "description": "This parameter, along with the `space` specifications in the `parameters` section, controls how many trials are generated.",
     "minimum": 1,
     "nullable": true,
     "type": "integer"
    },
    "scheduler": {
     "type": "object"
    },
    "time_budget_s": {
     "default": 3600,
     "description": "The number of seconds for the entire hyperopt run.",
     "minimum": 1,
     "nullable": true,
     "type": "integer"
    },
    "trial_driver_resources": {
     "default": null,
     "description": "The resources reserved by each trial driver. This differs from cpu_resources_per_trial and gpu_resources_per_trial because these resources are reserved for the driver, not its subsequent workers. Only used when the trials themselves are on the Ray backend. Defaults to 1 CPU.",
     "type": null
    }
   },
   "types": [
    "ray"
   ]
  },
  "fields": {
   "eval_split": {
    "default": "validation",
    "description": "The split of data that we want to run evaluation on. By default it is the validation split, but you have the flexibility to specify also train or test splits.",
    "enum": [
     "train",
     "validation",
     "test"
    ],
    "type": "string"
   },
   "executor": {
    "description": "specifies how to execute the hyperparameter optimization. The execution could happen locally in a serial manner or in parallel across multiple workers and with GPUs as well if available. The executor section includes specification for work scheduling and the number of samples to generate.",
    "type": "object"
   },
   "goal": {
    "default": "minimize",
    "description": "Indicates if to minimize or maximize a metric or a loss of any of the output features on any of the dataset splits. Available values are: minimize (default) or maximize.",
    "enum": [
     "minimize",
     "maximize"
    ],
    "type": "string"
   },
   "metric": {
    "default": "loss",
    "description": "The metric that we want to optimize for. The default one is loss, but depending on the type of the feature defined in output_feature, different metrics and losses are available. Check the metrics section of the specific output feature type to figure out what metrics are available to use.",
    "enum": [
     "root_mean_squared_error",
     "precision",
     "recall",
     "roc_auc",
     "specificity",
     "root_mean_squared_percentage_error",
     "r2",
     "loss",
     "binary_weighted_cross_entropy",
     "softmax_cross_entropy",
     "sequence_softmax_cross_entropy",
     "next_token_softmax_cross_entropy",
     "sigmoid_cross_entropy",
     "token_accuracy",
     "sequence_accuracy",
     "perplexity",
     "next_token_perplexity",
     "accuracy",
     "accuracy_micro",
     "hits_at_k",
     "mean_absolute_error",
     "mean_squared_error",
     "mean_absolute_percentage_error",
     "mean_absolute_scaled_error",
     "symmetric_mean_absolute_percentage_error",
     "jaccard",
     "huber",
     "corn",
     "anomaly_auroc",
     "f1_max"
    ],
    "type": "string"
   },
   "output_feature": {
    "default": "combined",
    "description": "The name of the output feature that we want to optimize the metric or loss of. Available values are `combined` or the name of any output feature provided in the configuration. `combined` is a special output feature that allows to optimize for the aggregated loss and metrics of all output features.",
    "type": "string"
   },
   "parameters": {
    "default": null,
    "description": "This section consists of a set of hyperparameters to optimize. They are provided as keys (the names of the parameters) and values associated with them (that define the search space). The values vary depending on the type of the hyperparameter. Syntax for this section is based on [Ray Tune's Search Space parameters](https://docs.ray.io/en/latest/tune/api/search_space.html).",
    "nullable": true,
    "type": "object"
   },
   "search_alg": {
    "description": "Specifies the algorithm to sample the defined parameters space. Candidate algorithms are those found in [Ray Tune's Search Algorithms](https://docs.ray.io/en/latest/tune/api/suggestion.html).",
    "type": "object"
   },
   "split": {
    "default": "validation",
    "description": "The split of data that we want to compute our metric on. By default it is the validation split, but you have the flexibility to specify also train or test splits.",
    "enum": [
     "train",
     "validation",
     "test"
    ],
    "type": "string"
   }
  },
  "scheduler": {
   "options": {
    "asha": {
     "brackets": {
      "default": 1,
      "description": "Number of brackets. Each bracket has a different halving rate, specified by the reduction factor.",
      "minimum": 1,
      "type": "integer"
     },
     "grace_period": {
      "default": 1,
      "description": "Only stop trials at least this old in time. The units are the same as the attribute named by `time_attr`.",
      "minimum": 1,
      "type": "integer"
     },
     "max_t": {
      "default": 100,
      "description": "max time units per trial. Trials will be stopped after max_t time units (determined by time_attr) have passed.",
      "minimum": 1,
      "type": "integer"
     },
     "metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "mode": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "reduction_factor": {
      "default": 4,
      "description": "Used to set halving rate and amount. This is simply a unit-less scalar.",
      "minimum": 0.0,
      "type": "integer"
     },
     "stop_last_trials": {
      "default": true,
      "description": "Whether to terminate the trials after reaching `max_t`.",
      "type": "boolean"
     },
     "time_attr": {
      "default": "training_iteration",
      "type": "string"
     }
    },
    "async_hyperband": {
     "brackets": {
      "default": 1,
      "description": "Number of brackets. Each bracket has a different halving rate, specified by the reduction factor.",
      "minimum": 1,
      "type": "integer"
     },
     "grace_period": {
      "default": 1,
      "description": "Only stop trials at least this old in time. The units are the same as the attribute named by `time_attr`.",
      "minimum": 1,
      "type": "integer"
     },
     "max_t": {
      "default": 100,
      "description": "max time units per trial. Trials will be stopped after max_t time units (determined by time_attr) have passed.",
      "minimum": 1,
      "type": "integer"
     },
     "metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "mode": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "reduction_factor": {
      "default": 4,
      "description": "Used to set halving rate and amount. This is simply a unit-less scalar.",
      "minimum": 0.0,
      "type": "integer"
     },
     "stop_last_trials": {
      "default": true,
      "description": "Whether to terminate the trials after reaching `max_t`.",
      "type": "boolean"
     },
     "time_attr": {
      "default": "training_iteration",
      "type": "string"
     }
    },
    "asynchyperband": {
     "brackets": {
      "default": 1,
      "description": "Number of brackets. Each bracket has a different halving rate, specified by the reduction factor.",
      "minimum": 1,
      "type": "integer"
     },
     "grace_period": {
      "default": 1,
      "description": "Only stop trials at least this old in time. The units are the same as the attribute named by `time_attr`.",
      "minimum": 1,
      "type": "integer"
     },
     "max_t": {
      "default": 100,
      "description": "max time units per trial. Trials will be stopped after max_t time units (determined by time_attr) have passed.",
      "minimum": 1,
      "type": "integer"
     },
     "metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "mode": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "reduction_factor": {
      "default": 4,
      "description": "Used to set halving rate and amount. This is simply a unit-less scalar.",
      "minimum": 0.0,
      "type": "integer"
     },
     "stop_last_trials": {
      "default": true,
      "description": "Whether to terminate the trials after reaching `max_t`.",
      "type": "boolean"
     },
     "time_attr": {
      "default": "training_iteration",
      "type": "string"
     }
    },
    "fifo": {
     "metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "mode": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "time_attr": {
      "default": "training_iteration",
      "type": "string"
     }
    },
    "hb_bohb": {
     "max_t": {
      "default": 81,
      "description": "max time units per trial. Trials will be stopped after max_t time units (determined by time_attr) have passed.",
      "minimum": 1,
      "type": "integer"
     },
     "metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "mode": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "reduction_factor": {
      "default": 3,
      "description": "Used to set halving rate and amount. This is simply a unit-less scalar.",
      "minimum": 0.0,
      "type": "integer"
     },
     "stop_last_trials": {
      "default": true,
      "description": "Whether to terminate the trials after reaching `max_t`. Defaults to True.",
      "type": "boolean"
     },
     "time_attr": {
      "default": "training_iteration",
      "type": "string"
     }
    },
    "hyperband": {
     "max_t": {
      "default": 81,
      "description": "max time units per trial. Trials will be stopped after max_t time units (determined by time_attr) have passed.",
      "minimum": 1,
      "type": "integer"
     },
     "metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "mode": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "reduction_factor": {
      "default": 3,
      "description": "Used to set halving rate and amount. This is simply a unit-less scalar.",
      "minimum": 0.0,
      "type": "integer"
     },
     "stop_last_trials": {
      "default": true,
      "description": "Whether to terminate the trials after reaching max_t. Defaults to True.",
      "type": "boolean"
     },
     "time_attr": {
      "default": "training_iteration",
      "type": "string"
     }
    },
    "median_stopping_rule": {
     "grace_period": {
      "default": 60.0,
      "description": "Only stop trials at least this old in time. The mean will only be computed from this time onwards. The units are the same as the attribute named by `time_attr`.",
      "minimum": 0.0,
      "type": "number"
     },
     "hard_stop": {
      "default": true,
      "description": "If False, pauses trials instead of stopping them. When all other trials are complete, paused trials will be resumed and allowed to run FIFO.",
      "type": "boolean"
     },
     "metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "min_samples_required": {
      "default": 3,
      "description": "Minimum number of trials to compute median over.",
      "minimum": 1,
      "type": "integer"
     },
     "min_time_slice": {
      "default": 0,
      "description": "Each trial runs at least this long before yielding (assuming it isn't stopped). Note: trials ONLY yield if there are not enough samples to evaluate performance for the current result AND there are other trials waiting to run. The units are the same as the attribute named by `time_attr`.",
      "minimum": 0,
      "type": "integer"
     },
     "mode": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "time_attr": {
      "default": "time_total_s",
      "description": "A training result attr to use for comparing time. Note that you can pass in something non-temporal such as training_iteration as a measure of progress, the only requirement is that the attribute should increase monotonically.",
      "enum": [
       "training_iteration",
       "time_total_s",
       "timesteps_total",
       "mean_accuracy",
       "mean_loss"
      ],
      "type": "string"
     }
    },
    "medianstoppingrule": {
     "grace_period": {
      "default": 60.0,
      "description": "Only stop trials at least this old in time. The mean will only be computed from this time onwards. The units are the same as the attribute named by `time_attr`.",
      "minimum": 0.0,
      "type": "number"
     },
     "hard_stop": {
      "default": true,
      "description": "If False, pauses trials instead of stopping them. When all other trials are complete, paused trials will be resumed and allowed to run FIFO.",
      "type": "boolean"
     },
     "metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "min_samples_required": {
      "default": 3,
      "description": "Minimum number of trials to compute median over.",
      "minimum": 1,
      "type": "integer"
     },
     "min_time_slice": {
      "default": 0,
      "description": "Each trial runs at least this long before yielding (assuming it isn't stopped). Note: trials ONLY yield if there are not enough samples to evaluate performance for the current result AND there are other trials waiting to run. The units are the same as the attribute named by `time_attr`.",
      "minimum": 0,
      "type": "integer"
     },
     "mode": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "time_attr": {
      "default": "time_total_s",
      "description": "A training result attr to use for comparing time. Note that you can pass in something non-temporal such as training_iteration as a measure of progress, the only requirement is that the attribute should increase monotonically.",
      "enum": [
       "training_iteration",
       "time_total_s",
       "timesteps_total",
       "mean_accuracy",
       "mean_loss"
      ],
      "type": "string"
     }
    },
    "pb2": {
     "hyperparam_bounds": {
      "default": null,
      "description": "Hyperparameters to mutate. The format is as follows: for each key, enter a list of the form [min, max] representing the minimum and maximum possible hyperparameter values.",
      "nullable": true,
      "type": "object"
     },
     "log_config": {
      "default": true,
      "description": "Whether to log the ray config of each model to `local_dir` at each exploit. Allows config schedule to be reconstructed.",
      "type": "boolean"
     },
     "metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "mode": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "perturbation_interval": {
      "default": 60.0,
      "description": "Models will be considered for perturbation at this interval of `time_attr`. Note that perturbation incurs checkpoint overhead, so you shouldn't set this to be too frequent.",
      "minimum": 0.0,
      "type": "number"
     },
     "quantile_fraction": {
      "default": 0.25,
      "description": "Parameters are transferred from the top `quantile_fraction` fraction of trials to the bottom `quantile_fraction` fraction. Needs to be between 0 and 0.5. Setting it to 0 essentially implies doing no exploitation at all.",
      "maximum": 0.5,
      "minimum": 0,
      "type": "number"
     },
     "require_attrs": {
      "default": true,
      "description": "Whether to require `time_attr` and metric to appear in result for every iteration. If True, error will be raised if these values are not present in trial result.",
      "type": "boolean"
     },
     "synch": {
      "default": false,
      "description": "If False, will use asynchronous implementation of PBT. Trial perturbations occur every `perturbation_interval` for each trial independently. If True, will use synchronous implementation of PBT. Perturbations will occur only after all trials are synced at the same `time_attr` every `perturbation_interval`. Defaults to False. See Appendix A.1 here https://arxiv.org/pdf/1711.09846.pdf.",
      "type": "boolean"
     },
     "time_attr": {
      "default": "time_total_s",
      "description": "A training result attr to use for comparing time. Note that you can pass in something non-temporal such as training_iteration as a measure of progress, the only requirement is that the attribute should increase monotonically.",
      "enum": [
       "training_iteration",
       "time_total_s",
       "timesteps_total",
       "mean_accuracy",
       "mean_loss"
      ],
      "type": "string"
     }
    },
    "pbt": {
     "burn_in_period": {
      "default": 60.0,
      "description": "Models will not be considered for perturbation before this interval of time_attr has passed. This guarantees that models are trained for at least a certain amount of time or timesteps before being perturbed.",
      "minimum": 0.0,
      "type": "number"
     },
     "custom_explore_fn": {
      "default": null,
      "description": "You can also specify a custom exploration function. This function is invoked as `f(config)` after built-in perturbations from `hyperparam_mutations` are applied, and should return config updated as needed. You must specify at least one of `hyperparam_mutations` or `custom_explore_fn`.",
      "type": null
     },
     "hyperparam_mutations": {
      "default": null,
      "description": "Hyperparams to mutate. The format is as follows: for each key, either a list, function, or a tune search space object (`tune.loguniform`, tune.uniform, etc.) can be provided. A list specifies an allowed set of categorical values. A function or tune search space object specifies the distribution of a continuous parameter. You must use `tune.choice`, `tune.uniform`, `tune.loguniform`, etc.. Arbitrary `tune.sample_from` objects are not supported. A key can also hold a dict for nested hyperparameters. You must specify at least one of `hyperparam_mutations` or `custom_explore_fn`. Tune will sample the search space provided by `hyperparam_mutations` for the initial hyperparameter values if the corresponding hyperparameters are not present in a trial's initial config.",
      "nullable": true,
      "type": "object"
     },
     "log_config": {
      "default": true,
      "description": "Whether to log the ray config of each model to `local_dir` at each exploit. Allows config schedule to be reconstructed.",
      "type": "boolean"
     },
     "metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "mode": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "perturbation_factors": {
      "default": [
       1.2,
       0.8
      ],
      "description": "Scaling factors to choose between when mutating a continuous hyperparameter.",
      "type": null
     },
     "perturbation_interval": {
      "default": 60.0,
      "description": "Models will be considered for perturbation at this interval of `time_attr`. Note that perturbation incurs checkpoint overhead, so you shouldn't set this to be too frequent.",
      "minimum": 0.0,
      "type": "number"
     },
     "quantile_fraction": {
      "default": 0.25,
      "description": "Parameters are transferred from the top `quantile_fraction` fraction of trials to the bottom `quantile_fraction` fraction. Needs to be between 0 and 0.5. Setting it to 0 essentially implies doing no exploitation at all.",
      "maximum": 0.5,
      "minimum": 0,
      "type": "number"
     },
     "require_attrs": {
      "default": true,
      "description": "Whether to require `time_attr` and metric to appear in result for every iteration. If True, error will be raised if these values are not present in trial result.",
      "type": "boolean"
     },
     "resample_probability": {
      "default": 0.25,
      "description": "The probability of resampling from the original distribution when applying `hyperparam_mutations`. If not resampled, the value will be perturbed by a factor chosen from `perturbation_factors` if continuous, or changed to an adjacent value if discrete.",
      "minimum": 0.0,
      "type": "number"
     },
     "synch": {
      "default": false,
      "description": "If False, will use asynchronous implementation of PBT. Trial perturbations occur every `perturbation_interval` for each trial independently. If True, will use synchronous implementation of PBT. Perturbations will occur only after all trials are synced at the same `time_attr` every `perturbation_interval`. Defaults to False. See Appendix A.1 here https://arxiv.org/pdf/1711.09846.pdf.",
      "type": "boolean"
     },
     "time_attr": {
      "default": "time_total_s",
      "description": "A training result attr to use for comparing time. Note that you can pass in something non-temporal such as training_iteration as a measure of progress, the only requirement is that the attribute should increase monotonically.",
      "enum": [
       "training_iteration",
       "time_total_s",
       "timesteps_total",
       "mean_accuracy",
       "mean_loss"
      ],
      "type": "string"
     }
    },
    "pbt_replay": {
     "metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "mode": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "policy_file": {
      "default": null,
      "description": "The PBT policy file. Usually this is stored in `~/ray_results/experiment_name/pbt_policy_xxx.txt` where `xxx` is the trial ID.",
      "nullable": true,
      "type": "string"
     },
     "time_attr": {
      "default": "training_iteration",
      "type": "string"
     }
    },
    "resource_changing": {
     "base_scheduler": {
      "default": null,
      "description": "The scheduler to provide decisions about trials. If None, a default FIFOScheduler will be used.",
      "type": null
     },
     "metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "mode": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "resources_allocation_function": {
      "default": null,
      "description": "The callable used to change live trial resource requiements during tuning. This callable will be called on each trial as it finishes one step of training. The callable must take four arguments: `TrialRunner`, current `Trial`, current result `dict` and the `ResourceChangingScheduler` calling it. The callable must return a `PlacementGroupFactory`, `Resources`, `dict` or None (signifying no need for an update). If `resources_allocation_function` is None, no resource requirements will be changed at any time. By default, `DistributeResources` will be used, distributing available CPUs and GPUs over all running trials in a robust way, without any prioritization.",
      "type": null
     },
     "time_attr": {
      "default": "training_iteration",
      "type": "string"
     }
    }
   },
   "types": [
    "asha",
    "asynchyperband",
    "async_hyperband",
    "hyperband",
    "medianstoppingrule",
    "median_stopping_rule",
    "pbt",
    "pbt_replay",
    "pb2",
    "hb_bohb",
    "fifo",
    "resource_changing"
   ]
  },
  "search_alg": {
   "fields": {},
   "types": [
    "variant_generator",
    "random",
    "ax",
    "bayesopt",
    "blendsearch",
    "bohb",
    "cfo",
    "dragonfly",
    "hebo",
    "hyperopt",
    "nevergrad",
    "optuna",
    "skopt",
    "zoopt"
   ]
  }
 },
 "llm": {
  "adapter": {
   "options": {
    "adalora": {
     "alpha": {
      "default": null,
      "nullable": true,
      "type": "integer"
     },
     "alpha_pattern": {
      "default": null,
      "nullable": true,
      "type": "object"
     },
     "beta1": {
      "default": 0.85,
      "description": "The hyperparameter of EMA for sensitivity smoothing.",
      "maximum": 1.0,
      "minimum": 0.0,
      "type": "number"
     },
     "beta2": {
      "default": 0.85,
      "description": "The hyperparameter of EMA for undertainty quantification.",
      "maximum": 1.0,
      "minimum": 0.0,
      "type": "number"
     },
     "bias_type": {
      "default": "none",
      "type": "string"
     },
     "delta_t": {
      "default": 1,
      "description": "The time internval between two budget allocations. The step interval of rank allocation.",
      "minimum": 0,
      "type": "integer"
     },
     "dropout": {
      "default": 0.05,
      "type": "number"
     },
     "eva_config": {
      "default": null,
      "type": null
     },
     "init_lora_weights": {
      "default": "default",
      "type": null
     },
     "init_r": {
      "default": 12,
      "description": "Initial Lora Matrix Dimension. The initial rank for each incremental matrix.",
      "minimum": 1,
      "type": "integer"
     },
     "layer_replication": {
      "default": null,
      "nullable": true,
      "type": "array"
     },
     "loftq_config": {
      "default": null,
      "type": null
     },
     "loraplus_lr_ratio": {
      "default": null,
      "nullable": true,
      "type": "number"
     },
     "orth_reg_weight": {
      "default": 0.5,
      "description": "The coefficient of orthogonality regularization.",
      "maximum": 1.0,
      "minimum": 0.0,
      "type": "number"
     },
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "r": {
      "default": 8,
      "type": "integer"
     },
     "rank_pattern": {
      "default": null,
      "description": "The allocated rank for each weight matrix by RankAllocator.",
      "nullable": true,
      "type": "object"
     },
     "target_modules": {
      "default": null,
      "type": null
     },
     "target_r": {
      "default": 8,
      "description": "Target Lora Matrix Dimension. The target average rank of incremental matrix.",
      "minimum": 1,
      "type": "integer"
     },
     "tfinal": {
      "default": 0,
      "description": "The steps of final fine-tuning warmup.",
      "minimum": 0,
      "type": "integer"
     },
     "tinit": {
      "default": 0,
      "description": "The steps of initial fine-tuning warmup.",
      "minimum": 0,
      "type": "integer"
     },
     "total_step": {
      "default": 10000,
      "description": "The total training steps for AdaLoRA rank allocation scheduling. Must be a positive integer (required by peft >= 0.14).",
      "minimum": 1,
      "type": "integer"
     },
     "use_dora": {
      "default": false,
      "type": "boolean"
     },
     "use_rslora": {
      "default": false,
      "type": "boolean"
     }
    },
    "boft": {
     "boft_block_size": {
      "default": 4,
      "description": "Block size for butterfly factorization.",
      "minimum": 1,
      "type": "integer"
     },
     "boft_dropout": {
      "default": 0.05,
      "description": "Dropout for BOFT layers.",
      "minimum": 0.0,
      "type": "number"
     },
     "boft_n_butterfly_factor": {
      "default": 1,
      "description": "Number of butterfly factors.",
      "minimum": 1,
      "type": "integer"
     },
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "target_modules": {
      "default": null,
      "description": "List of module names to apply BOFT to.",
      "type": null
     }
    },
    "c3a": {
     "bias_type": {
      "default": "none",
      "description": "Bias type for C3A. 'none' trains no biases; 'all' or 'c3a_only' trains the adapter biases.",
      "enum": [
       "none",
       "all",
       "c3a_only"
      ],
      "type": "string"
     },
     "block_size": {
      "default": 256,
      "description": "Block size for C3A, must be divisible by both the input size and the output size of each target layer. Setting this to the GCD of all target layer dimensions is a safe default. Larger block sizes mean fewer parameters.",
      "minimum": 1,
      "type": "integer"
     },
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "target_modules": {
      "default": null,
      "description": "List of module names or regex to apply C3A to.",
      "type": null
     }
    },
    "fourierft": {
     "n_frequency": {
      "default": 1000,
      "description": "Number of frequency components.",
      "minimum": 1,
      "type": "integer"
     },
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "scaling": {
      "default": 150.0,
      "description": "Scaling factor for FourierFT.",
      "type": "number"
     },
     "target_modules": {
      "default": null,
      "description": "List of module names to apply FourierFT to.",
      "type": null
     }
    },
    "hra": {
     "apply_GS": {
      "default": false,
      "description": "Whether to apply Gram-Schmidt orthogonalization to the Householder vectors. Improves numerical stability at the cost of a small overhead.",
      "type": "boolean"
     },
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "r": {
      "default": 8,
      "description": "Number of Householder reflections (rank). More reflections = more expressive adaptation.",
      "minimum": 1,
      "type": "integer"
     },
     "target_modules": {
      "default": null,
      "description": "List of module names or regex to apply HRA to.",
      "type": null
     }
    },
    "ia3": {
     "fan_in_fan_out": {
      "default": false,
      "description": "Set this to True if the layer to replace stores weight like (fan_in, fan_out). For example, gpt-2 uses Conv1D which stores weights like (fan_in, fan_out) and hence this should be set to True.",
      "type": "boolean"
     },
     "feedforward_modules": {
      "default": null,
      "description": "The names of the modules to be treated as feedforward modules, as in the original paper. These modules will have (IA)^3 vectors multiplied to the input, instead of the output. feedforward_modules must be a name or a subset of names present in target_modules.",
      "type": null
     },
     "init_ia3_weights": {
      "default": true,
      "description": "Whether to initialize the vectors in the (IA)^3 layers, defaults to True.",
      "type": "boolean"
     },
     "modules_to_save": {
      "default": null,
      "description": "List of modules apart from (IA)^3 layers to be set as trainable and saved in the final checkpoint.",
      "type": null
     },
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "target_modules": {
      "default": null,
      "description": "The names of the modules to apply (IA)^3 to.",
      "type": null
     }
    },
    "ln_tuning": {
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "target_modules": {
      "default": null,
      "description": "List of layer norm module names or regex to tune. Defaults to all LayerNorm / RMSNorm modules in the model.",
      "type": null
     }
    },
    "loha": {
     "alpha": {
      "default": 8,
      "description": "Scaling factor for LoHa.",
      "type": "number"
     },
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "r": {
      "default": 8,
      "description": "LoHa rank dimension.",
      "minimum": 1,
      "type": "integer"
     },
     "target_modules": {
      "default": null,
      "description": "List of module names to apply LoHa to.",
      "type": null
     }
    },
    "lokr": {
     "alpha": {
      "default": 8,
      "description": "Scaling factor for LoKr.",
      "type": "number"
     },
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "r": {
      "default": 8,
      "description": "LoKr rank dimension.",
      "minimum": 1,
      "type": "integer"
     },
     "target_modules": {
      "default": null,
      "description": "List of module names to apply LoKr to.",
      "type": null
     }
    },
    "lora": {
     "alpha": {
      "default": null,
      "description": "The alpha parameter for Lora scaling. Defaults to `2 * r`.",
      "minimum": 1,
      "nullable": true,
      "type": "integer"
     },
     "alpha_pattern": {
      "default": null,
      "description": "Per-layer alpha (scaling) overrides as a mapping of layer name (or regex) to float. Overrides the global `alpha` for matched layers.",
      "nullable": true,
      "type": "object"
     },
     "bias_type": {
      "default": "none",
      "description": "Bias type for Lora.",
      "enum": [
       "none",
       "all",
       "lora_only"
      ],
      "type": "string"
     },
     "dropout": {
      "default": 0.05,
      "description": "The dropout probability for Lora layers.",
      "minimum": 0.0,
      "type": "number"
     },
     "eva_config": {
      "default": null,
      "type": null
     },
     "init_lora_weights": {
      "default": "default",
      "description": "Initialization strategy for LoRA weight matrices. 'default' uses the standard Kaiming uniform init (A) and zeros (B). 'gaussian' uses Gaussian init for A. 'pissa' (Principal Singular values and Singular vectors Adaptation) initializes using SVD of the pretrained weight, converging faster and often outperforming standard LoRA. Paper: https://arxiv.org/abs/2404.02948. 'eva' (Explained Variance Adaptation) initializes from the SVD of layer input activations \u2014 requires `eva_config` to be set. Paper: https://arxiv.org/abs/2410.07170. 'corda' (Context-Oriented Decomposition Adaptation) combines PiSSA and full fine-tuning signals, converging faster than PiSSA. Paper: https://arxiv.org/abs/2406.05223. 'olora' (Orthonormal LoRA) uses QR decomposition for better conditioning. 'loftq' (LoftQ) jointly quantizes base weights and initializes LoRA \u2014 requires `loftq_config` to be set. Paper: https://arxiv.org/abs/2310.08659. 'orthogonal' uses orthogonal initialization.",
      "enum": [
       "default",
       "gaussian",
       "eva",
       "olora",
       "pissa",
       "corda",
       "loftq",
       "orthogonal"
      ],
      "type": null
     },
     "layer_replication": {
      "default": null,
      "description": "Layer replication configuration as a list of [start, end] index pairs. Enables depth-wise parameter efficiency by sharing LoRA weights across layer ranges. Example: [[0, 4], [2, 5]] creates two overlapping groups.",
      "nullable": true,
      "type": "array"
     },
     "loftq_config": {
      "default": null,
      "type": null
     },
     "loraplus_lr_ratio": {
      "default": null,
      "description": "LoRA+ learning rate ratio (Hayou et al., ICML 2024). When set, the B matrices use lr * loraplus_lr_ratio while A matrices use the base lr. Typical values: 2-16. Provides 1-2%% accuracy gain and up to 2x speedup over standard LoRA. Paper: https://arxiv.org/abs/2402.12354",
      "nullable": true,
      "type": "number"
     },
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "r": {
      "default": 8,
      "description": "Lora attention dimension.",
      "minimum": 1,
      "type": "integer"
     },
     "rank_pattern": {
      "default": null,
      "description": "Per-layer rank overrides as a mapping of layer name (or regex) to rank integer. Overrides the global `r` for matched layers. Useful for LoRA-XS style configurations where different layers benefit from different ranks. Example: {'model.layers.0.self_attn.q_proj': 4, 'model.layers.0.self_attn.v_proj': 2}",
      "nullable": true,
      "type": "object"
     },
     "target_modules": {
      "default": null,
      "description": "List of module names or regex expression of the module names to replace with LoRA. For example, ['q', 'v'] or '.*decoder.*(SelfAttention|EncDecAttention).*(q|v)$'. Defaults to targeting the query and value matrices of all self-attention and encoder-decoder attention layers.",
      "type": null
     },
     "use_dora": {
      "default": false,
      "description": "Enable 'Weight-Decomposed Low-Rank Adaptation' (DoRA). This technique decomposes the updates of the weights into two parts, magnitude and direction. Direction is handled by normal LoRA, whereas the magnitude is handled by a separate learnable parameter. This can improve the performance of LoRA, especially at low ranks. Right now, DoRA only supports non-quantized linear layers. DoRA introduces a bigger overhead than pure LoRA, so it is recommended to merge weights for inference. For more information, see https://arxiv.org/abs/2402.09353",
      "type": "boolean"
     },
     "use_rslora": {
      "default": false,
      "description": "When set to True, uses Rank-Stabilized LoRA which sets the adapter scaling factor to lora_alpha/math.sqrt(r), since it was proven to work better. Otherwise, it will use the original default value of lora_alpha/r. Paper: https://arxiv.org/abs/2312.03732.",
      "type": "boolean"
     }
    },
    "oft": {
     "coft": {
      "default": false,
      "description": "Whether to use Constrained OFT (COFT), which enforces the constraint ||I - R^T R||_F <= eps.",
      "type": "boolean"
     },
     "eps": {
      "default": 6e-05,
      "description": "Constraint strength for COFT (only used when `coft=True`).",
      "minimum": 0.0,
      "type": "number"
     },
     "module_dropout": {
      "default": 0.0,
      "description": "Probability of randomly zeroing an OFT block during training.",
      "minimum": 0.0,
      "type": "number"
     },
     "oft_block_size": {
      "default": 32,
      "description": "Block size for the butterfly factorization of the orthogonal transform.",
      "minimum": 1,
      "type": "integer"
     },
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "r": {
      "default": 0,
      "description": "OFT rank. When 0, the block size (`oft_block_size`) controls the granularity instead. Cannot be set simultaneously with `oft_block_size`.",
      "minimum": 0,
      "type": "integer"
     },
     "target_modules": {
      "default": null,
      "description": "List of module names or regex to apply OFT to.",
      "type": null
     }
    },
    "tinylora": {
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "projection_seed": {
      "default": 42,
      "description": "Random seed for generating the fixed projection matrices.",
      "minimum": 0,
      "type": "integer"
     },
     "r": {
      "default": 2,
      "description": "SVD rank for the frozen U, Sigma, V decomposition. The paper recommends r=2.",
      "minimum": 1,
      "type": "integer"
     },
     "save_projection": {
      "default": true,
      "description": "Whether to save the projection tensors in the state dict.",
      "type": "boolean"
     },
     "target_modules": {
      "default": null,
      "description": "List of module names or regex to apply TinyLoRA to.",
      "type": null
     },
     "tinylora_dropout": {
      "default": 0.0,
      "description": "Dropout probability for TinyLoRA layers.",
      "minimum": 0.0,
      "type": "number"
     },
     "u": {
      "default": 64,
      "description": "Trainable vector dimension per group. Controls the expressivity of the adaptation. Can be as low as 1\u201313 for extreme parameter efficiency.",
      "minimum": 1,
      "type": "integer"
     },
     "weight_tying": {
      "default": 0.0,
      "description": "Degree of weight tying across target modules (0.0 = no sharing, 1.0 = full sharing). Sharing trainable vectors across modules further reduces parameter count.",
      "maximum": 1.0,
      "minimum": 0.0,
      "type": "number"
     }
    },
    "vblora": {
     "num_vectors": {
      "default": 256,
      "description": "Number of vectors in the global vector bank shared across all layers.",
      "minimum": 1,
      "type": "integer"
     },
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "r": {
      "default": 4,
      "description": "LoRA rank dimension. Controls the bottleneck size of each adaptation.",
      "minimum": 1,
      "type": "integer"
     },
     "save_only_topk_weights": {
      "default": false,
      "description": "Whether to save only the top-k selection logits rather than the full bank weights.",
      "type": "boolean"
     },
     "target_modules": {
      "default": null,
      "description": "List of module names or regex to apply VBLoRA to.",
      "type": null
     },
     "topk": {
      "default": 2,
      "description": "Number of top-k vectors selected from the bank for each LoRA matrix column. Higher k increases expressivity but also parameter count.",
      "minimum": 1,
      "type": "integer"
     },
     "vblora_dropout": {
      "default": 0.0,
      "description": "Dropout probability for VBLoRA layers.",
      "minimum": 0.0,
      "type": "number"
     },
     "vector_length": {
      "default": 256,
      "description": "Length (dimension) of each vector in the bank. Usually set to the hidden size or head dim.",
      "minimum": 1,
      "type": "integer"
     }
    },
    "vera": {
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "projection_prng_key": {
      "default": 0,
      "description": "PRNG key for shared random projection matrices.",
      "minimum": 0,
      "type": "integer"
     },
     "r": {
      "default": 256,
      "description": "VeRA rank dimension.",
      "minimum": 1,
      "type": "integer"
     },
     "target_modules": {
      "default": null,
      "description": "List of module names to apply VeRA to.",
      "type": null
     }
    },
    "waveft": {
     "n_frequency": {
      "default": 2592,
      "description": "Number of wavelet frequency components to learn. Fewer = more parameter efficient.",
      "minimum": 1,
      "type": "integer"
     },
     "postprocessor": {
      "type": "object"
     },
     "pretrained_adapter_weights": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "scaling": {
      "default": 25.0,
      "description": "Scaling factor applied to the wavelet-domain updates.",
      "minimum": 0.0,
      "type": "number"
     },
     "target_modules": {
      "default": null,
      "description": "List of module names or regex to apply WaveFT to.",
      "type": null
     },
     "wavelet_family": {
      "default": "db1",
      "description": "Wavelet family to use for the discrete wavelet transform. 'db1'/'haar' are simplest; higher-order Daubechies ('db2', 'db3') capture smoother features.",
      "enum": [
       "db1",
       "db2",
       "db3",
       "haar",
       "sym2",
       "coif1"
      ],
      "type": "string"
     }
    }
   },
   "types": [
    "lora",
    "adalora",
    "ia3",
    "vera",
    "loha",
    "lokr",
    "fourierft",
    "boft",
    "tinylora",
    "c3a",
    "oft",
    "hra",
    "waveft",
    "ln_tuning",
    "vblora"
   ]
  },
  "base_model": {
   "fields": {},
   "values": [
    "bloomz-3b",
    "bloomz-7b1",
    "codellama-7b",
    "codellama-13b",
    "codellama-34b",
    "codellama-7b-instruct",
    "codellama-13b-instruct",
    "codellama-34b-instruct",
    "gpt-neo-2.7B",
    "gpt-j-6b",
    "llama-2-7b",
    "llama-2-13b",
    "llama-2-70b",
    "llama-2-7b-chat",
    "llama-2-13b-chat",
    "llama-2-70b-chat",
    "mistral-7b",
    "mistral-7b-instruct",
    "mixtral-8x7b",
    "mixtral-8x7b-instruct",
    "opt-350m",
    "opt-1.3b",
    "opt-6.7b",
    "pythia-2.8b",
    "pythia-12b",
    "vicuna-7b",
    "vicuna-13b",
    "zephyr-7b-alpha",
    "zephyr-7b-beta",
    "phi-1",
    "phi-1_5",
    "phi-2"
   ]
  },
  "generation": {
   "fields": {
    "bad_words_ids": {
     "default": null,
     "description": "List of token ids that are not allowed to be generated. In order to get the tokens of the words that should not appear in the generated text, use tokenizer(bad_word, add_prefix_space=True).input_ids.",
     "type": null
    },
    "begin_suppress_tokens": {
     "default": null,
     "description": "A list of tokens that will be suppressed at the beginning of the generation. The SupressBeginTokens logit processor will set their log probs to -inf so that they are not sampled.",
     "type": null
    },
    "bos_token_id": {
     "default": null,
     "description": "The id of the beginning of sentence token. If not set, the bos token id of the tokenizer is used.",
     "nullable": true,
     "type": "integer"
    },
    "diversity_penalty": {
     "default": 0.0,
     "description": "The value used to control the diversity of the generated text. The higher the value, the more diverse the text will be. If set to 0, no diversity is enforced.This value is subtracted from a beam(s) score if it generates a token same as any beam from other group at aparticular time. Note that diversity_penalty is only effective if group beam search is enabled.",
     "minimum": 0.0,
     "nullable": true,
     "type": "number"
    },
    "do_sample": {
     "default": true,
     "description": "Whether or not to use sampling ; use greedy decoding otherwise.",
     "nullable": true,
     "type": "boolean"
    },
    "early_stopping": {
     "default": false,
     "description": "Controls the stopping condition for beam-based methods, like beam-search. It accepts the following values: True, where the generation stops as soon as there are num_beams complete candidates; False, where an heuristic is applied and the generation stops when is it very unlikely to find better candidates; `never`, where the beam search procedure only stops when there cannot be better candidates (canonical beam search algorithm)",
     "type": null
    },
    "encoder_repetition_penalty": {
     "default": 1.0,
     "description": "The paramater for encoder_repetition_penalty. An exponential penalty on sequences that are not in the original input. 1.0 means no penalty.",
     "minimum": 0.0,
     "nullable": true,
     "type": "number"
    },
    "eos_token_id": {
     "default": null,
     "description": "The id of the end of sentence token. If not set, the eos token id of the tokenizer is used.",
     "type": null
    },
    "epsilon_cutoff": {
     "default": 0.0,
     "description": "If set to float strictly between 0 and 1, only tokens with a conditional probability greater than epsilon_cutoff will be sampled. In the paper, suggested values range from 3e-4 to 9e-4, depending on the size of the model.",
     "maximum": 1.0,
     "minimum": 0.0,
     "nullable": true,
     "type": "number"
    },
    "eta_cutoff": {
     "default": 0.0,
     "description": "Eta sampling is a hybrid of locally typical sampling and epsilon sampling. If set to float strictly between 0 and 1, a token is only considered if it is greater than either eta_cutoff or sqrt(eta_cutoff) * exp(-entropy(softmax(next_token_logits))). The latter term is intuitively the expected next token probability, scaled by sqrt(eta_cutoff). In the paper, suggested values range from 3e-4 to 2e-3, depending on the size of the model.",
     "maximum": 1.0,
     "minimum": 0.0,
     "nullable": true,
     "type": "number"
    },
    "exponential_decay_length_penalty": {
     "default": null,
     "description": "This Tuple adds an exponentially increasing length penalty, after a certain amount of tokens have been generated. The tuple shall consist of: (start_index, decay_factor) where start_index indicates where penalty starts and decay_factor represents the factor of exponential decay",
     "maximum": 1.0,
     "minimum": 0.0,
     "type": null
    },
    "force_words_ids": {
     "default": null,
     "description": "List of token ids that are forced to be generated by the model. In order to get the tokens of the words that should appear in the generated text, use tokenizer(force_word, add_prefix_space=True).input_ids.",
     "type": null
    },
    "forced_bos_token_id": {
     "default": null,
     "description": "The id of the token to force as the first generated token after the decoder_start_token_id.Useful for multilingual models like mBART where the first generated token needs to be the target languagetoken.",
     "nullable": true,
     "type": "integer"
    },
    "forced_decoder_ids": {
     "default": null,
     "description": "A list of forced decoder ids. The ForcedDecoderIds logit processor will set the log probs of all tokens that are not in the list to -inf so that they are not sampled.",
     "type": null
    },
    "forced_eos_token_id": {
     "default": null,
     "description": "The id of the token to force as the last generated token when max_length is reached. Optionally, use a list to set multiple end-of-sequence tokens.",
     "type": null
    },
    "guidance_scale": {
     "default": null,
     "description": "The guidance scale for classifier free guidance (CFG). CFG is enabled by setting guidance_scale > 1. Higher guidance scale encourages the model to generate samples that are more closely linked to the input prompt, usually at the expense of poorer quality.",
     "minimum": 0.0,
     "nullable": true,
     "type": "number"
    },
    "length_penalty": {
     "default": 1.0,
     "description": "Exponential penalty to the length that is used with beam-based generation. It is applied as an exponent to the sequence length, which in turn is used to divide the score of the sequence. Since the score is the log likelihood of the sequence (i.e. negative), length_penalty > 0.0 promotes longer sequences, while length_penalty < 0.0 encourages shorter sequences.",
     "nullable": true,
     "type": "number"
    },
    "max_length": {
     "default": 32,
     "description": "The maximum length the generated tokens can have. Corresponds to the length of the input prompt + max_new_tokens. Its effect is overridden by max_new_tokens, if also set.",
     "minimum": 1,
     "nullable": true,
     "type": "integer"
    },
    "max_new_tokens": {
     "default": 32,
     "description": "The maximum number of new tokens to generate, ignoring the number of tokens in the input prompt. If not set, this is dynamically determined by Ludwig based on either the `max_sequence_length` of the ouput feature, the global_max_sequence_length specified in preprocessing (if specified), or the maximum context length supported by the model (in the order specified).",
     "minimum": 1,
     "nullable": true,
     "type": "integer"
    },
    "max_time": {
     "default": null,
     "description": "The maximum amount of time you allow the computation to run for in seconds. generation will still finish the current pass after allocated time has been passed.",
     "nullable": true,
     "type": "number"
    },
    "min_length": {
     "default": 0,
     "description": "The minimum length of the sequence to be generated. Corresponds to the length of the input prompt + min_new_tokens. Its effect is overridden by min_new_tokens, if also set.",
     "minimum": 0,
     "nullable": true,
     "type": "integer"
    },
    "min_new_tokens": {
     "default": null,
     "description": "The minimum number of new tokens to generate, ignoring the number of tokens in the input prompt.",
     "minimum": 0,
     "nullable": true,
     "type": "integer"
    },
    "no_repeat_ngram_size": {
     "default": 0,
     "description": "If set to int > 0, all ngrams of that size can only occur once.",
     "minimum": 0,
     "nullable": true,
     "type": "integer"
    },
    "num_beam_groups": {
     "default": 1,
     "description": "Number of groups to divide num_beams into in order to ensure diversity among different groups of beams. 1 means no group beam search.",
     "minimum": 1,
     "nullable": true,
     "type": "integer"
    },
    "num_beams": {
     "default": 1,
     "description": "Number of beams for beam search. 1 means no beam search and is the default value. The beam search strategy generates the translation word by word from left-to-right while keeping a fixed number (beam) of active candidates at each time step during token generation. By increasing the beam size, the translation performance can increase at the expense of significantly reducing the decoder speed.",
     "minimum": 1,
     "nullable": true,
     "type": "integer"
    },
    "pad_token_id": {
     "default": null,
     "description": "The id of the padding token. If not set, the padding token id of the tokenizer is used.",
     "nullable": true,
     "type": "integer"
    },
    "penalty_alpha": {
     "default": null,
     "description": "The values balance the model confidence and the degeneration penalty in contrastive search decoding.",
     "minimum": 0.0,
     "nullable": true,
     "type": "number"
    },
    "prompt_lookup_num_tokens": {
     "default": null,
     "description": "The number of tokens to consider as a candidate from the prompt for prompt lookup decoding, an alternate way of performing assisted generation. If set to 0, the prompt lookup decoding is not used.",
     "minimum": 0,
     "nullable": true,
     "type": "integer"
    },
    "remove_invalid_values": {
     "default": false,
     "description": "Whether to remove possible nan and inf outputs of the model to prevent the generation method to crash. Note that using remove_invalid_values can slow down generation.",
     "nullable": true,
     "type": "boolean"
    },
    "renormalize_logits": {
     "default": false,
     "description": "Whether to renormalize the logits after temperature and top_k/top_p filtering.",
     "nullable": true,
     "type": "boolean"
    },
    "repetition_penalty": {
     "default": 1.0,
     "description": "The parameter for repetition penalty. 1.0 means no penalty. See [this paper](https://arxiv.org/pdf/1909.05858.pdf) for more details.",
     "minimum": 0.0,
     "nullable": true,
     "type": "number"
    },
    "sequence_bias": {
     "default": null,
     "description": "A dictionary of token ids to bias the generation towards. The SequenceBias logit processor will add the bias to the log probs of the tokens in the dictionary. Positive biases increase the odds of the sequence being selected, while negative biases do the opposite.",
     "type": null
    },
    "suppress_tokens": {
     "default": null,
     "description": "A list of tokens that will be suppressed at generation. The SupressTokens logit processor will set their log probs to -inf so that they are not sampled.",
     "type": null
    },
    "temperature": {
     "default": 0.1,
     "description": "Temperature is used to control the randomness of predictions. A high temperature value (closer to 1) makes the output more diverse and random, while a lower temperature (closer to 0) makes the model's responses more deterministic and focused on the most likely outcome. In other words, temperature adjusts the probability distribution from which the model picks the next token.",
     "minimum": 0.0,
     "nullable": true,
     "type": "number"
    },
    "top_k": {
     "default": 50,
     "description": "The number of highest probability vocabulary tokens to keep for top-k-filtering.",
     "minimum": 1,
     "nullable": true,
     "type": "integer"
    },
    "top_p": {
     "default": 1.0,
     "description": "If set to float < 1, only the most probable tokens with probabilities that add up to top_p or higher are kept for generation.",
     "maximum": 1.0,
     "minimum": 0.0,
     "nullable": true,
     "type": "number"
    },
    "typical_p": {
     "default": 1.0,
     "description": "Local typicality measures how similar the conditional probability of predicting a target token next is to the expected conditional probability of predicting a random token next, given the partial text already generated. If set to float < 1, the smallest set of the most locally typical tokens with probabilities that add up to typical_p or higher are kept for generation.",
     "maximum": 1.0,
     "minimum": 0.0,
     "nullable": true,
     "type": "number"
    },
    "use_cache": {
     "default": true,
     "description": "Whether or not the model should use the past last key/values attentions (if applicable to the model) to speed up decoding.",
     "nullable": true,
     "type": "boolean"
    }
   }
  },
  "model_parameters": {
   "fields": {
    "neftune_noise_alpha": {
     "default": 0,
     "description": "The alpha parameter for the embedding noise, which controls the amount of noise added to the embeddings. The higher the value, the more noise is added. This is based on the paper NEFTune: Noisy Embeddings Improve Instruction Finetuning. Paper: https://arxiv.org/pdf/2310.05914.pdf. Default: 0.Suggested values: 5, 10",
     "minimum": 0,
     "nullable": true,
     "type": "integer"
    },
    "rope_scaling": {
     "type": "object"
    }
   }
  },
  "prompt": {
   "fields": {
    "retrieval": {
     "type": "object"
    },
    "task": {
     "default": null,
     "description": "The task to use for the prompt. Required if `template` is not set.",
     "nullable": true,
     "type": "string"
    },
    "template": {
     "default": null,
     "description": "The template to use for the prompt. Must contain at least one of the columns from the input dataset or `__sample__` as a variable surrounded in curly brackets {} to indicate where to insert the current feature. Multiple columns can be inserted, e.g.: `The {color} {animal} jumped over the {size} {object}`, where every term in curly brackets is a column in the dataset. If a `task` is specified, then the template must also contain the `__task__` variable. If `retrieval` is specified, then the template must also contain the `__context__` variable. If no template is provided, then a default will be used based on the retrieval settings, and a task must be set in the config.",
     "nullable": true,
     "type": "string"
    }
   }
  },
  "quantization": {
   "fields": {
    "backend": {
     "default": "bitsandbytes",
     "description": "Quantization backend. 'bitsandbytes' (default) applies 4-bit / 8-bit quantization at model load time via the bitsandbytes library \u2014 the existing QLoRA fine-tuning path. 'torchao' applies PyTorch-native quantization via torchao after model load, and can additionally run quantization-aware training (QAT) when `qat: true` is set.",
     "enum": [
      "bitsandbytes",
      "torchao"
     ],
     "type": "string"
    },
    "bits": {
     "default": 4,
     "description": "The quantization level to apply to weights on load.",
     "enum": [
      4,
      8
     ],
     "type": "integer"
    },
    "bnb_4bit_compute_dtype": {
     "default": "float16",
     "description": "This sets the computational type which might be different than the input type. For example, inputs might be fp32, but computation can be set to bf16 for speedups.",
     "enum": [
      "float32",
      "float16",
      "bfloat16"
     ],
     "type": "string"
    },
    "bnb_4bit_quant_type": {
     "default": "nf4",
     "description": "This sets the quantization data type in the bnb.nn.Linear4Bit layers.",
     "enum": [
      "fp4",
      "nf4"
     ],
     "type": "string"
    },
    "bnb_4bit_use_double_quant": {
     "default": true,
     "description": "This flag is used for nested quantization where the quantization constants from the first quantization are quantized again.",
     "type": "boolean"
    },
    "llm_int8_has_fp16_weight": {
     "default": false,
     "description": "This flag runs LLM.int8() with 16-bit main weights. This is useful for fine-tuning as the weights do not have to be converted back and forth for the backward pass.",
     "type": "boolean"
    },
    "llm_int8_threshold": {
     "default": 6.0,
     "description": "This corresponds to the outlier threshold for outlier detection as described in `LLM.int8() : 8-bit Matrix Multiplication for Transformers at Scale` paper: https://arxiv.org/abs/2208.07339. Any hidden states value that is above this threshold will be considered an outlier and the operation on those values will be done in fp16. Values are usually normally distributed, that is, most values are in the range [-3.5, 3.5], but there are some exceptional systematic outliers that are very differently distributed for large models. These outliers are often in the interval [-60, -6] or [6, 60]. Int8 quantization works well for values of magnitude ~5, but beyond that, there is a significant performance penalty. A good default threshold is 6, but a lower threshold might be needed for more unstable models (small models, fine-tuning).",
     "minimum": 0.0,
     "type": "number"
    },
    "mode": {
     "default": null,
     "description": "torchao-only quantization mode. Ignored when `backend` is 'bitsandbytes'. 'int4_weight_only' and 'int8_weight_only' quantize only the weight matrices (activations stay in fp16/bf16). 'int8_dynamic' quantizes activations to int8 dynamically per-forward. 'float8' stores weights in fp8 (useful on H100+).",
     "enum": [
      "int4_weight_only",
      "int8_weight_only",
      "int8_dynamic",
      "float8",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "qat": {
     "default": false,
     "description": "torchao-only. When true, inserts fake-quant observers into the model before training (QAT). The model is trained in the target low-precision numerical regime, then converted to actually-quantized weights at save time. Ignored when `backend` is 'bitsandbytes'.",
     "type": "boolean"
    }
   }
  }
 },
 "ludwig_version": "0.17.8",
 "models": {
  "ecd": {
   "input_features": {
    "audio": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "name": {
       "description": "Name of the input feature.",
       "type": "string"
      },
      "tied": {
       "default": null,
       "nullable": true,
       "type": "string"
      }
     },
     "encoder": {
      "options": {
       "cnnrnn": {
        "activation": {
         "default": "tanh",
         "description": "The default activation function to use.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "bidirectional": {
         "default": false,
         "description": "If true, two recurrent networks will perform encoding in the forward and backward direction and their outputs will be concatenated.",
         "type": "boolean"
        },
        "cell_type": {
         "default": "rnn",
         "description": "The type of recurrent cell to use. Available values are: `rnn`, `lstm`, `gru`. For reference about the differences between the cells please refer to [torch.nn Recurrent Layers](https://pytorch.org/docs/stable/nn.html#recurrent-layers).",
         "enum": [
          "rnn",
          "lstm",
          "gru"
         ],
         "type": "string"
        },
        "conv_activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each convolutional layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "conv_dropout": {
         "default": 0.0,
         "description": "The dropout rate for the convolutional layers",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "conv_layers": {
         "default": null,
         "description": "A list of dictionaries containing the parameters of all the convolutional layers. The length of the list determines the number of stacked convolutional layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `num_filters`, `filter_size`, `strides`, `padding`, `dilation_rate`, `use_bias`, `pool_function`, `pool_padding`, `pool_size`, `pool_strides`, `bias_initializer`, `weights_initializer`. If any of those values is missing from the dictionary, the default one specified as a parameter of the encoder will be used instead. If both `conv_layers` and `num_conv_layers` are `null`, a default list will be assigned to `conv_layers` with the value `[{filter_size: 7, pool_size: 3}, {filter_size: 7, pool_size: 3}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: 3}]`.",
         "type": null
        },
        "dilation_rate": {
         "default": 1,
         "description": "Dilation rate to use for dilated convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "filter_size": {
         "default": 5,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_conv_layers": {
         "default": null,
         "description": "The number of stacked convolutional layers when `conv_layers` is `null`.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of parallel fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "num_rec_layers": {
         "default": 1,
         "description": "The number of stacked recurrent layers.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pool_strides": {
         "default": null,
         "description": "Factor to scale down.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "recurrent_activation": {
         "default": "sigmoid",
         "description": "The activation function to use in the recurrent step",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "recurrent_dropout": {
         "default": 0.0,
         "description": "The dropout rate for the recurrent state",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "recurrent_initializer": {
         "default": "orthogonal",
         "description": "The initializer for recurrent matrix weights",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "reduce_output": {
         "default": "last",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "state_size": {
         "default": 256,
         "description": "The size of the state of the rnn.",
         "minimum": 1,
         "type": "integer"
        },
        "strides": {
         "default": 1,
         "description": "Stride length of the convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "unit_forget_bias": {
         "default": true,
         "description": "If true, add 1 to the bias of the forget gate at initialization",
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "hubert": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "facebook/hubert-base-ls960",
         "description": "Name or path of the pretrained model. Can be a HuggingFace model hub identifier (e.g. 'facebook/hubert-base-ls960', 'facebook/hubert-large-ls960-ft') or a local path to a saved model directory.",
         "type": "string"
        },
        "reduce_output": {
         "default": "mean",
         "nullable": true,
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "jamba": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "attention_every_k": {
         "default": 4,
         "description": "Every `attention_every_k`-th block is attention, the remainder are SSM. Default 4 gives a 1:3 attention:SSM ratio matching the Jamba paper.",
         "minimum": 1,
         "type": null
        },
        "d_conv": {
         "default": 4,
         "description": "Width of the depthwise 1D convolution inside each SSM block.",
         "minimum": 1,
         "type": null
        },
        "d_model": {
         "default": 256,
         "description": "Hidden width of every block \u2014 SSM and attention share the same d_model.",
         "minimum": 1,
         "type": null
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": null
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": null
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": null
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": null
        },
        "expand_factor": {
         "default": 2,
         "description": "Inner expansion factor inside each SSM block.",
         "minimum": 1,
         "type": null
        },
        "ffn_size": {
         "default": 1024,
         "description": "Feed-forward width inside each attention block.",
         "minimum": 1,
         "type": null
        },
        "n_layers": {
         "default": 8,
         "description": "Total number of stacked blocks (SSM + attention combined).",
         "minimum": 1,
         "type": null
        },
        "num_heads": {
         "default": 8,
         "description": "Number of attention heads (and SSD heads, shared).",
         "minimum": 1,
         "type": null
        },
        "output_size": {
         "default": 256,
         "description": "Output feature width emitted by the encoder.",
         "minimum": 1,
         "type": null
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "type": null
        },
        "reduce_output": {
         "default": "mean",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "type": null
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": null
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings.",
         "type": null
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "mamba": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "d_conv": {
         "default": 4,
         "description": "Width of the 1D convolution in each SSM layer.",
         "minimum": 1,
         "type": "integer"
        },
        "d_model": {
         "default": 256,
         "description": "Dimensionality of the model (hidden size for SSM layers).",
         "minimum": 1,
         "type": "integer"
        },
        "d_state": {
         "default": 16,
         "description": "Dimensionality of the state space in each SSM layer.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "expand_factor": {
         "default": 2,
         "description": "Expansion factor for the inner dimension of SSM layers.",
         "minimum": 1,
         "type": "integer"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "n_layers": {
         "default": 4,
         "description": "Number of stacked SSM layers.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each FC layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "mean",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use bias in linear projections.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "mamba2": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "d_conv": {
         "default": 4,
         "description": "Width of the depthwise 1D convolution inside each block.",
         "minimum": 1,
         "type": null
        },
        "d_model": {
         "default": 256,
         "description": "Hidden width of each Mamba-2 block.",
         "minimum": 1,
         "type": null
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": null
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": null
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": null
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": null
        },
        "expand_factor": {
         "default": 2,
         "description": "Inner expansion factor for each block.",
         "minimum": 1,
         "type": null
        },
        "n_layers": {
         "default": 4,
         "description": "Number of stacked Mamba-2 blocks.",
         "minimum": 1,
         "type": null
        },
        "num_heads": {
         "default": 8,
         "description": "Number of SSD heads. `d_model * expand_factor` must be divisible by `num_heads`.",
         "minimum": 1,
         "type": null
        },
        "output_size": {
         "default": 256,
         "description": "Output feature width emitted by the encoder.",
         "minimum": 1,
         "type": null
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "type": null
        },
        "reduce_output": {
         "default": "mean",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "type": null
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": null
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings.",
         "type": null
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "parallel_cnn": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "conv_layers": {
         "default": null,
         "description": "A list of dictionaries containing the parameters of all the convolutional layers. The length of the list determines the number of stacked convolutional layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `num_filters`, `filter_size`, `strides`, `padding`, `dilation_rate`, `use_bias`, `pool_function`, `pool_padding`, `pool_size`, `pool_strides`, `bias_initializer`, `weights_initializer`. If any of those values is missing from the dictionary, the default one specified as a parameter of the encoder will be used instead. If both `conv_layers` and `num_conv_layers` are `null`, a default list will be assigned to `conv_layers` with the value `[{filter_size: 7, pool_size: 3}, {filter_size: 7, pool_size: 3}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: 3}]`.",
         "type": null
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate applied to the embedding. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "filter_size": {
         "default": 3,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_conv_layers": {
         "default": null,
         "description": "The number of stacked convolutional layers when `conv_layers` is `null`.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": null,
         "description": "Number of parallel fully connected layers to use.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "Whether to embed the input sequence.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "rnn": {
        "activation": {
         "default": "tanh",
         "description": "The default activation function.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "bidirectional": {
         "default": false,
         "description": "If true, two recurrent networks will perform encoding in the forward and backward direction and their outputs will be concatenated.",
         "type": "boolean"
        },
        "cell_type": {
         "default": "rnn",
         "description": "The type of recurrent cell to use. Available values are: `rnn`, `lstm`, `gru`. For reference about the differences between the cells please refer to [torch.nn Recurrent Layers](https://pytorch.org/docs/stable/nn.html#recurrent-layers).",
         "enum": [
          "rnn",
          "lstm",
          "gru"
         ],
         "type": "string"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of parallel fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "num_layers": {
         "default": 1,
         "description": "The number of stacked recurrent layers.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "recurrent_activation": {
         "default": "sigmoid",
         "description": "The activation function to use in the recurrent step",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "recurrent_dropout": {
         "default": 0.0,
         "description": "The dropout rate for the recurrent state",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "recurrent_initializer": {
         "default": "orthogonal",
         "description": "The initializer for recurrent matrix weights",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "reduce_output": {
         "default": "last",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "state_size": {
         "default": 256,
         "description": "The size of the state of the rnn.",
         "minimum": 1,
         "type": "integer"
        },
        "unit_forget_bias": {
         "default": true,
         "description": "If true, add 1 to the bias of the forget gate at initialization",
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "stacked_cnn": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "conv_layers": {
         "default": null,
         "description": "A list of dictionaries containing the parameters of all the convolutional layers. The length of the list determines the number of stacked convolutional layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `num_filters`, `filter_size`, `strides`, `padding`, `dilation_rate`, `use_bias`, `pool_function`, `pool_padding`, `pool_size`, `pool_strides`, `bias_initializer`, `weights_initializer`. If any of those values is missing from the dictionary, the default one specified as a parameter of the encoder will be used instead. If both `conv_layers` and `num_conv_layers` are `null`, a default list will be assigned to `conv_layers` with the value `[{filter_size: 7, pool_size: 3}, {filter_size: 7, pool_size: 3}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: 3}]`.",
         "type": null
        },
        "dilation_rate": {
         "default": 1,
         "description": "Dilation rate to use for dilated convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate applied to the embedding. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "filter_size": {
         "default": 3,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_conv_layers": {
         "default": null,
         "description": "The number of stacked convolutional layers when `conv_layers` is `null`.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": null,
         "description": "Number of parallel fully connected layers to use.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pool_strides": {
         "default": null,
         "description": "Factor to scale down.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "Whether to embed the input sequence.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "strides": {
         "default": 1,
         "description": "Stride length of the convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "stacked_parallel_cnn": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate applied to the embedding. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "filter_size": {
         "default": 3,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": null,
         "description": "Number of parallel fully connected layers to use.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "num_stacked_layers": {
         "default": null,
         "description": "If stacked_layers is null, this is the number of elements in the stack of parallel convolutional layers.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "stacked_layers": {
         "default": null,
         "description": "a nested list of lists of dictionaries containing the parameters of the stack of parallel convolutional layers. The length of the list determines the number of stacked parallel convolutional layers, length of the sub-lists determines the number of parallel conv layers and the content of each dictionary determines the parameters for a specific layer.",
         "type": null
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "wav2vec2": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "facebook/wav2vec2-base",
         "description": "Name or path of the pretrained model. Can be a HuggingFace model hub identifier (e.g. 'facebook/wav2vec2-base', 'facebook/wav2vec2-large-xlsr-53') or a local path to a saved model directory.",
         "type": "string"
        },
        "reduce_output": {
         "default": "mean",
         "nullable": true,
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "whisper": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "openai/whisper-base",
         "description": "Name or path of the pretrained model. Can be a HuggingFace model hub identifier (e.g. 'openai/whisper-base', 'openai/whisper-small', 'openai/whisper-medium', 'openai/whisper-large-v3') or a local path to a saved model directory.",
         "type": "string"
        },
        "reduce_output": {
         "default": "mean",
         "nullable": true,
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       }
      },
      "types": [
       "wav2vec2",
       "whisper",
       "hubert",
       "parallel_cnn",
       "stacked_cnn",
       "stacked_parallel_cnn",
       "rnn",
       "cnnrnn",
       "mamba",
       "mamba2",
       "jamba"
      ]
     },
     "preprocessing": {
      "fields": {
       "audio_file_length_limit_in_s": {
        "default": 7.5,
        "description": "Float value that defines the maximum limit of the audio file in seconds. All files longer than this limit are cut off. All files shorter than this limit are padded with padding_value",
        "minimum": 0.0,
        "type": null
       },
       "computed_fill_value": {
        "default": null,
        "description": "The internally computed fill value to replace missing values with in case the missing_value_strategy is fill_with_mode or fill_with_mean",
        "minimum": 0.0,
        "nullable": true,
        "type": "number"
       },
       "fill_value": {
        "default": null,
        "description": "The value to replace missing values with in case the missing_value_strategy is fill_with_const",
        "minimum": 0.0,
        "nullable": true,
        "type": "number"
       },
       "in_memory": {
        "default": true,
        "description": "Defines whether the audio dataset will reside in memory during the training process or will be dynamically fetched from disk (useful for large datasets). In the latter case a training batch of input audio will be fetched from disk each training iteration.",
        "type": "boolean"
       },
       "lazy_cache_dir": {
        "default": null,
        "description": "Directory in which to cache audio files when the source data is in-memory (e.g. a HuggingFace dataset). Only used when mode is 'lazy' or 'lazy_cached' and the input entries are not already paths to existing files. When None, defaults to ~/.cache/ludwig/lazy_media/<feature_name>/. Has no effect when the input column already contains local file paths. Note: this controls the file cache for in-memory sources; the decoded memmap for 'lazy_cached' mode is placed next to the Parquet cache instead.",
        "nullable": true,
        "type": "string"
       },
       "missing_value_strategy": {
        "default": "bfill",
        "description": "What strategy to follow when there's a missing value in an audio column",
        "enum": [
         "fill_with_const",
         "fill_with_mode",
         "bfill",
         "ffill",
         "drop_row"
        ],
        "type": "string"
       },
       "mode": {
        "default": "lazy",
        "description": "Preprocessing mode for audio features. 'eager' decodes all files during preprocessing and stores tensors in the Parquet cache. 'lazy' stores file paths and decodes per batch during training, keeping peak memory bounded to batch_size \u00d7 clip_size. 'lazy_cached' behaves like 'lazy' on the first training epoch but writes decoded tensors to a numpy memmap alongside the Parquet cache; subsequent epochs read from the memmap directly, eliminating decode overhead.",
        "enum": [
         "eager",
         "lazy",
         "lazy_cached"
        ],
        "type": "string"
       },
       "norm": {
        "default": null,
        "description": "Normalization strategy for the audio files. If None, no normalization is performed. If per_file, z-norm is applied on a 'per file' level",
        "enum": [
         "per_file",
         null
        ],
        "nullable": true,
        "type": "string"
       },
       "num_fft_points": {
        "default": null,
        "description": "Defines the number of fft points used for the short time Fourier transformation",
        "minimum": 0.0,
        "nullable": true,
        "type": "number"
       },
       "num_filter_bands": {
        "default": 80,
        "description": "Defines the number of filters used in the filterbank. Only needed if audio_feature_type is 'fbank'",
        "minimum": 1,
        "type": "integer"
       },
       "padding_value": {
        "default": 0.0,
        "description": "Float value that is used for padding.",
        "minimum": 0.0,
        "type": "number"
       },
       "prefetch_size": {
        "default": null,
        "description": "Number of batches to prefetch in a background thread while the GPU processes the current batch. None (default) selects automatically: 0 for 'eager' mode, 4 for 'lazy' and 'lazy_cached' (epoch 1). After the first epoch in 'lazy_cached' mode, prefetch is automatically disabled since memmap reads are fast enough. Set to 0 to disable prefetch entirely, or to a positive integer to override the automatic selection.",
        "minimum": 0,
        "nullable": true,
        "type": "integer"
       },
       "window_length_in_s": {
        "default": 0.04,
        "description": "Defines the window length used for the short time Fourier transformation. This is only needed if the audio_feature_type is 'raw'.",
        "minimum": 0.0,
        "type": "number"
       },
       "window_shift_in_s": {
        "default": 0.02,
        "description": "Defines the window shift used for the short time Fourier transformation (also called hop_length). This is only needed if the audio_feature_type is 'raw'.",
        "minimum": 0.0,
        "type": "number"
       },
       "window_type": {
        "default": "hamming",
        "description": "Defines the type window the signal is weighted before the short time Fourier transformation.",
        "enum": [
         "bartlett",
         "blackman",
         "hamming",
         "hann"
        ],
        "type": "string"
       }
      },
      "types": [
       "fbank",
       "group_delay",
       "raw",
       "stft",
       "stft_phase"
      ]
     }
    },
    "bag": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "name": {
       "description": "Name of the input feature.",
       "type": "string"
      },
      "tied": {
       "default": null,
       "nullable": true,
       "type": "string"
      }
     },
     "encoder": {
      "options": {
       "embed": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer to use for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout probability for the embedding.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 50,
         "description": "The maximum embedding size, the actual size will be min(vocabulary_size, embedding_size) for dense representations and exactly vocabulary_size for the sparse encoding, where vocabulary_size is the number of different strings appearing in the training set in the input column (plus 1 for the unknown token placeholder <UNK>).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If true embeddings are trained during the training process, if false embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding fine tuning them. This parameter has effect only when representation is dense as sparse one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "force_embedding_size": {
         "default": false,
         "description": "Force the embedding size to be equal to the vocabulary size. This parameter has effect only if representation is dense.",
         "type": "boolean"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "This is the number of stacked fully connected layers that the input to the feature passes through. Their output is projected in the feature's output space.",
         "minimum": 0,
         "type": "integer"
        },
        "output_size": {
         "default": 10,
         "description": "If output_size is not already specified in fc_layers this is the default output_size that will be used for each layer. It indicates the size of the output of a fully connected layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "By default dense embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the GloVe format. When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if representation is dense.",
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "The representation of the embedding. Either dense or sparse.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary of the encoder",
         "type": null
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer to use for the weights matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       }
      },
      "types": [
       "embed"
      ]
     },
     "preprocessing": {
      "fields": {
       "computed_fill_value": {
        "default": "<UNK>",
        "description": "The internally computed fill value to replace missing values with in case the missing_value_strategy is fill_with_mode or fill_with_mean",
        "type": "string"
       },
       "fill_value": {
        "default": "<UNK>",
        "description": "The value to replace missing values with in case the missing_value_strategy is fill_with_const",
        "type": "string"
       },
       "lowercase": {
        "default": false,
        "description": "If true, converts the string to lowercase before tokenizing.",
        "type": "boolean"
       },
       "missing_value_strategy": {
        "default": "fill_with_const",
        "description": "What strategy to follow when there's a missing value in a set column",
        "enum": [
         "fill_with_const",
         "fill_with_mode",
         "bfill",
         "ffill",
         "drop_row"
        ],
        "type": "string"
       },
       "most_common": {
        "default": 10000,
        "description": "The maximum number of most common tokens to be considered. If the data contains more than this amount, the most infrequent tokens will be treated as unknown.",
        "minimum": 1,
        "nullable": true,
        "type": "integer"
       },
       "tokenizer": {
        "default": "space",
        "description": "Defines how to transform the raw text content of the dataset column to a set of elements. The default value space splits the string on spaces. Common options include: underscore (splits on underscore), comma (splits on comma), json (decodes the string into a set or a list through a JSON parser).",
        "enum": [
         "space",
         "space_punct",
         "characters",
         "underscore",
         "comma",
         "untokenized",
         "stripped",
         "english_tokenize",
         "english_tokenize_filter",
         "english_tokenize_remove_stopwords",
         "english_lemmatize",
         "english_lemmatize_filter",
         "english_lemmatize_remove_stopwords",
         "italian_tokenize",
         "italian_tokenize_filter",
         "italian_tokenize_remove_stopwords",
         "italian_lemmatize",
         "italian_lemmatize_filter",
         "italian_lemmatize_remove_stopwords",
         "spanish_tokenize",
         "spanish_tokenize_filter",
         "spanish_tokenize_remove_stopwords",
         "spanish_lemmatize",
         "spanish_lemmatize_filter",
         "spanish_lemmatize_remove_stopwords",
         "german_tokenize",
         "german_tokenize_filter",
         "german_tokenize_remove_stopwords",
         "german_lemmatize",
         "german_lemmatize_filter",
         "german_lemmatize_remove_stopwords",
         "french_tokenize",
         "french_tokenize_filter",
         "french_tokenize_remove_stopwords",
         "french_lemmatize",
         "french_lemmatize_filter",
         "french_lemmatize_remove_stopwords",
         "portuguese_tokenize",
         "portuguese_tokenize_filter",
         "portuguese_tokenize_remove_stopwords",
         "portuguese_lemmatize",
         "portuguese_lemmatize_filter",
         "portuguese_lemmatize_remove_stopwords",
         "dutch_tokenize",
         "dutch_tokenize_filter",
         "dutch_tokenize_remove_stopwords",
         "dutch_lemmatize",
         "dutch_lemmatize_filter",
         "dutch_lemmatize_remove_stopwords",
         "greek_tokenize",
         "greek_tokenize_filter",
         "greek_tokenize_remove_stopwords",
         "greek_lemmatize",
         "greek_lemmatize_filter",
         "greek_lemmatize_remove_stopwords",
         "norwegian_tokenize",
         "norwegian_tokenize_filter",
         "norwegian_tokenize_remove_stopwords",
         "norwegian_lemmatize",
         "norwegian_lemmatize_filter",
         "norwegian_lemmatize_remove_stopwords",
         "lithuanian_tokenize",
         "lithuanian_tokenize_filter",
         "lithuanian_tokenize_remove_stopwords",
         "lithuanian_lemmatize",
         "lithuanian_lemmatize_filter",
         "lithuanian_lemmatize_remove_stopwords",
         "danish_tokenize",
         "danish_tokenize_filter",
         "danish_tokenize_remove_stopwords",
         "danish_lemmatize",
         "danish_lemmatize_filter",
         "danish_lemmatize_remove_stopwords",
         "polish_tokenize",
         "polish_tokenize_filter",
         "polish_tokenize_remove_stopwords",
         "polish_lemmatize",
         "polish_lemmatize_filter",
         "polish_lemmatize_remove_stopwords",
         "romanian_tokenize",
         "romanian_tokenize_filter",
         "romanian_tokenize_remove_stopwords",
         "romanian_lemmatize",
         "romanian_lemmatize_filter",
         "romanian_lemmatize_remove_stopwords",
         "japanese_tokenize",
         "japanese_tokenize_filter",
         "japanese_tokenize_remove_stopwords",
         "japanese_lemmatize",
         "japanese_lemmatize_filter",
         "japanese_lemmatize_remove_stopwords",
         "chinese_tokenize",
         "chinese_tokenize_filter",
         "chinese_tokenize_remove_stopwords",
         "chinese_lemmatize",
         "chinese_lemmatize_filter",
         "chinese_lemmatize_remove_stopwords",
         "multi_tokenize",
         "multi_tokenize_filter",
         "multi_tokenize_remove_stopwords",
         "multi_lemmatize",
         "multi_lemmatize_filter",
         "multi_lemmatize_remove_stopwords",
         "sentencepiece",
         "clip",
         "gpt2bpe",
         "bert",
         "hf_tokenizer"
        ],
        "type": "string"
       }
      }
     }
    },
    "binary": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "name": {
       "description": "Name of the input feature.",
       "type": "string"
      },
      "tied": {
       "default": null,
       "nullable": true,
       "type": "string"
      }
     },
     "encoder": {
      "options": {
       "dense": {
        "activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the dense encoder.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "Default normalization applied at the beginnging of fully connected layers.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_layers": {
         "default": 1,
         "description": "Number of stacked fully connected layers to apply. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "Size of the output of the feature.",
         "minimum": 1,
         "type": "integer"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "passthrough": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       }
      },
      "types": [
       "passthrough",
       "dense"
      ]
     },
     "preprocessing": {
      "fields": {
       "computed_fill_value": {
        "default": null,
        "description": "The internally computed fill value to replace missing values with in case the missing_value_strategy is fill_with_mode or fill_with_mean",
        "type": null
       },
       "fallback_true_label": {
        "default": null,
        "description": "The label to interpret as 1 (True) when the binary feature doesn't have a conventional boolean value",
        "nullable": true,
        "type": "string"
       },
       "fill_value": {
        "default": null,
        "description": "The value to replace missing values with in case the missing_value_strategy is fill_with_const",
        "type": null
       },
       "missing_value_strategy": {
        "default": "fill_with_false",
        "description": "What strategy to follow when there's a missing value in a binary column",
        "enum": [
         "fill_with_mode",
         "bfill",
         "ffill",
         "drop_row",
         "fill_with_false",
         "fill_with_true"
        ],
        "type": "string"
       }
      }
     }
    },
    "category": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "name": {
       "description": "Name of the input feature.",
       "type": "string"
      },
      "tied": {
       "default": null,
       "nullable": true,
       "type": "string"
      }
     },
     "encoder": {
      "options": {
       "dense": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_initializer": {
         "default": null,
         "description": "Initializer for the embedding matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "embedding_size": {
         "default": 50,
         "description": "The maximum embedding size, the actual size will be min(vocabulary_size, embedding_size) for dense representations and exactly vocabulary_size for the sparse encoding, where vocabulary_size is the number of different strings appearing in the training set in the column the feature is named after (plus 1 for <UNK>).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "hash": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "embedding_size": {
         "default": 50,
         "description": "Size of the embedding for each hash bucket.",
         "minimum": 1,
         "type": "integer"
        },
        "num_hash_buckets": {
         "default": 1024,
         "description": "Number of hash buckets to map categories into.",
         "minimum": 1,
         "type": "integer"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "onehot": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "passthrough": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "sparse": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_initializer": {
         "default": null,
         "description": "Initializer for the embedding matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": false,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "target": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "output_size": {
         "default": 1,
         "description": "Size of the target encoding output per category.",
         "minimum": 1,
         "type": "integer"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       }
      },
      "types": [
       "passthrough",
       "dense",
       "sparse",
       "onehot",
       "target",
       "hash"
      ]
     },
     "preprocessing": {
      "fields": {
       "cache_encoder_embeddings": {
        "default": false,
        "description": "For fixed encoders, compute encoder embeddings in preprocessing to avoid this step at train time. Can speed up the time taken per step during training, but will invalidate the preprocessed data if the encoder type is changed.",
        "type": "boolean"
       },
       "computed_fill_value": {
        "default": "<UNK>",
        "description": "The internally computed fill value to replace missing values with in case the missing_value_strategy is fill_with_mode or fill_with_mean",
        "type": "string"
       },
       "fill_value": {
        "default": "<UNK>",
        "description": "The value to replace missing values with in case the `missing_value_strategy` is `fill_with_const`",
        "type": "string"
       },
       "lowercase": {
        "default": false,
        "description": "Whether the string has to be lowercased before being handled by the tokenizer.",
        "type": "boolean"
       },
       "missing_value_strategy": {
        "default": "fill_with_const",
        "description": "What strategy to follow when there's a missing value in a category column",
        "enum": [
         "fill_with_const",
         "fill_with_mode",
         "bfill",
         "ffill",
         "drop_row"
        ],
        "type": "string"
       },
       "most_common": {
        "default": 10000,
        "description": "The maximum number of most common tokens to be considered. if the data contains more than this amount, the most infrequent tokens will be treated as unknown.",
        "minimum": 1,
        "nullable": true,
        "type": "integer"
       }
      }
     }
    },
    "date": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "name": {
       "description": "Name of the input feature.",
       "type": "string"
      },
      "tied": {
       "default": null,
       "nullable": true,
       "type": "string"
      }
     },
     "encoder": {
      "options": {
       "embed": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer to use for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout probability for the embedding.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 10,
         "description": "The maximum embedding size adopted.",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "The number of stacked fully connected layers.",
         "minimum": 0,
         "type": "integer"
        },
        "output_size": {
         "default": 10,
         "description": "If an output_size is not already specified in fc_layers this is the default output_size that will be used for each layer. It indicates the size of the output of a fully connected layer.",
         "minimum": 1,
         "type": "integer"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer to use for the weights matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       },
       "wave": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer to use for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout probability for the embedding.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 1,
         "description": "The number of stacked fully connected layers.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 10,
         "description": "If an output_size is not already specified in fc_layers this is the default output_size that will be used for each layer. It indicates the size of the output of a fully connected layer.",
         "minimum": 1,
         "type": "integer"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer to use for the weights matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       }
      },
      "types": [
       "embed",
       "wave"
      ]
     },
     "preprocessing": {
      "fields": {
       "computed_fill_value": {
        "default": "",
        "description": "The internally computed fill value to replace missing values with in case the missing_value_strategy is fill_with_mode or fill_with_mean",
        "type": "string"
       },
       "datetime_format": {
        "default": null,
        "description": "This parameter can either be a datetime format string, or null, in which case the datetime format will be inferred automatically.",
        "nullable": true,
        "type": "string"
       },
       "fill_value": {
        "default": "",
        "description": "The value to replace missing values with in case the missing_value_strategy is fill_with_const",
        "type": "string"
       },
       "missing_value_strategy": {
        "default": "fill_with_const",
        "description": "What strategy to follow when there's a missing value in a date column",
        "enum": [
         "fill_with_const",
         "bfill",
         "ffill",
         "drop_row"
        ],
        "type": "string"
       }
      }
     }
    },
    "h3": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "name": {
       "description": "Name of the input feature.",
       "type": "string"
      },
      "tied": {
       "default": null,
       "nullable": true,
       "type": "string"
      }
     },
     "encoder": {
      "options": {
       "embed": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer to use for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout probability for the embedding.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 10,
         "description": "The maximum embedding size adopted.",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "The number of stacked fully connected layers.",
         "minimum": 0,
         "type": "integer"
        },
        "output_size": {
         "default": 10,
         "description": "If an output_size is not already specified in fc_layers this is the default output_size that will be used for each layer. It indicates the size of the output of a fully connected layer.",
         "minimum": 1,
         "type": "integer"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer to use for the weights matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       },
       "rnn": {
        "activation": {
         "default": "tanh",
         "description": "The activation function to use",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer to use for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "bidirectional": {
         "default": false,
         "description": "If true, two recurrent networks will perform encoding in the forward and backward direction and their outputs will be concatenated.",
         "type": "boolean"
        },
        "cell_type": {
         "default": "rnn",
         "description": "The type of recurrent cell to use. Available values are: `rnn`, `lstm`, `lstm_block`, `lstm`, `ln`, `lstm_cudnn`, `gru`, `gru_block`, `gru_cudnn`. For reference about the differences between the cells please refer to PyTorch's documentation. We suggest to use the `block` variants on CPU and the `cudnn` variants on GPU because of their increased speed.",
         "enum": [
          "rnn",
          "lstm",
          "lstm_block",
          "ln",
          "lstm_cudnn",
          "gru",
          "gru_block",
          "gru_cudnn"
         ],
         "type": "string"
        },
        "dropout": {
         "default": 0.0,
         "description": "The dropout rate",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 10,
         "description": "The maximum embedding size adopted.",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them.",
         "type": "boolean"
        },
        "hidden_size": {
         "default": 10,
         "description": "The size of the hidden representation within the transformer block. It is usually the same as the embedding_size, but if the two values are different, a projection layer will be added before the first transformer block.",
         "minimum": 1,
         "type": "integer"
        },
        "num_layers": {
         "default": 1,
         "description": "The number of stacked recurrent layers.",
         "minimum": 1,
         "type": "integer"
        },
        "recurrent_activation": {
         "default": "sigmoid",
         "description": "The activation function to use in the recurrent step",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "recurrent_dropout": {
         "default": 0.0,
         "description": "The dropout rate for the recurrent state",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "recurrent_initializer": {
         "default": "orthogonal",
         "description": "The initializer for recurrent matrix weights",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "reduce_output": {
         "default": "last",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "unit_forget_bias": {
         "default": true,
         "description": "If true, add 1 to the bias of the forget gate at initialization",
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer to use for the weights matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       },
       "weighted_sum": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer to use for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout probability for the embedding.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 10,
         "description": "The maximum embedding size adopted.",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "The number of stacked fully connected layers.",
         "minimum": 0,
         "type": "integer"
        },
        "output_size": {
         "default": 10,
         "description": "If an output_size is not already specified in fc_layers this is the default output_size that will be used for each layer. It indicates the size of the output of a fully connected layer.",
         "minimum": 1,
         "type": "integer"
        },
        "should_softmax": {
         "default": false,
         "description": "Determines if the weights of the weighted sum should be passed though a softmax layer before being used.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer to use for the weights matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       }
      },
      "types": [
       "embed",
       "weighted_sum",
       "rnn"
      ]
     },
     "preprocessing": {
      "fields": {
       "computed_fill_value": {
        "default": 576495936675512319,
        "description": "The internally computed fill value to replace missing values with in case the missing_value_strategy is fill_with_mode or fill_with_mean",
        "minimum": 1,
        "type": "integer"
       },
       "fill_value": {
        "default": 576495936675512319,
        "description": "The value to replace missing values with in case the missing_value_strategy is fill_with_const",
        "minimum": 1,
        "type": "integer"
       },
       "missing_value_strategy": {
        "default": "fill_with_const",
        "description": "What strategy to follow when there's a missing value in an h3 column",
        "enum": [
         "fill_with_const",
         "fill_with_mode",
         "bfill",
         "ffill",
         "drop_row"
        ],
        "type": "string"
       }
      }
     }
    },
    "image": {
     "common": {
      "augmentation": {
       "type": null
      },
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "name": {
       "description": "Name of the input feature.",
       "type": "string"
      },
      "tied": {
       "default": null,
       "nullable": true,
       "type": "string"
      }
     },
     "encoder": {
      "options": {
       "alexnet": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "base",
         "description": "Pretrained model variant to use.",
         "enum": [
          "base"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "caformer": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_name": {
         "default": "caformer_s18",
         "description": "CAFormer model variant. Hybrid Conv+Attention MetaFormer achieving SOTA accuracy. Variants with '.sail_in22k_ft_in1k' are pretrained on ImageNet-21K and finetuned on ImageNet-1K. Variants with '_384' use 384x384 input resolution.",
         "enum": [
          "caformer_s18",
          "caformer_s36",
          "caformer_m36",
          "caformer_b36",
          "caformer_s18.sail_in22k_ft_in1k",
          "caformer_s18.sail_in22k_ft_in1k_384",
          "caformer_s36.sail_in22k_ft_in1k",
          "caformer_s36.sail_in22k_ft_in1k_384",
          "caformer_m36.sail_in22k_ft_in1k",
          "caformer_m36.sail_in22k_ft_in1k_384",
          "caformer_b36.sail_in22k_ft_in1k",
          "caformer_b36.sail_in22k_ft_in1k_384"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "clip": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "openai/clip-vit-base-patch32",
         "description": "HuggingFace model path or name for the CLIP vision model.",
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "convformer": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_name": {
         "default": "convformer_s18",
         "description": "ConvFormer model variant. Pure CNN MetaFormer that outperforms ConvNeXt. Variants with '.sail_in22k_ft_in1k' are pretrained on ImageNet-21K and finetuned on ImageNet-1K.",
         "enum": [
          "convformer_s18",
          "convformer_s36",
          "convformer_m36",
          "convformer_b36",
          "convformer_s18.sail_in22k_ft_in1k",
          "convformer_s18.sail_in22k_ft_in1k_384",
          "convformer_s36.sail_in22k_ft_in1k",
          "convformer_s36.sail_in22k_ft_in1k_384",
          "convformer_m36.sail_in22k_ft_in1k",
          "convformer_m36.sail_in22k_ft_in1k_384",
          "convformer_b36.sail_in22k_ft_in1k",
          "convformer_b36.sail_in22k_ft_in1k_384"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "convnext": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "base",
         "description": "Pretrained model variant to use.",
         "enum": [
          "tiny",
          "small",
          "base",
          "large"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "convnextv2": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_name": {
         "default": "convnextv2_base",
         "description": "ConvNeXt V2 model variant. Improved ConvNeXt with Global Response Normalization (GRN) and FCMAE pre-training. Variants with '.fcmae_ft_in1k' are fine-tuned on ImageNet-1K. Variants with '.fcmae_ft_in22k_in1k' are pre-trained on ImageNet-22K and fine-tuned on ImageNet-1K. Variants with '_384' use 384x384 input resolution.",
         "enum": [
          "convnextv2_atto",
          "convnextv2_femto",
          "convnextv2_pico",
          "convnextv2_nano",
          "convnextv2_tiny",
          "convnextv2_base",
          "convnextv2_large",
          "convnextv2_huge",
          "convnextv2_atto.fcmae_ft_in1k",
          "convnextv2_femto.fcmae_ft_in1k",
          "convnextv2_pico.fcmae_ft_in1k",
          "convnextv2_nano.fcmae_ft_in1k",
          "convnextv2_tiny.fcmae_ft_in1k",
          "convnextv2_base.fcmae_ft_in1k",
          "convnextv2_large.fcmae_ft_in1k",
          "convnextv2_huge.fcmae_ft_in1k",
          "convnextv2_base.fcmae_ft_in22k_in1k",
          "convnextv2_large.fcmae_ft_in22k_in1k",
          "convnextv2_huge.fcmae_ft_in22k_in1k",
          "convnextv2_base.fcmae_ft_in22k_in1k_384",
          "convnextv2_large.fcmae_ft_in22k_in1k_384",
          "convnextv2_huge.fcmae_ft_in22k_in1k_384"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "densenet": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": 121,
         "description": "Pretrained model variant to use.",
         "enum": [
          121,
          161,
          169,
          201
         ],
         "type": "integer"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "dinov2": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "facebook/dinov2-base",
         "description": "HuggingFace model path or name for the DINOv2 model.",
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "efficientnet": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "b0",
         "description": "Pretrained model variant to use.",
         "enum": [
          "b0",
          "b1",
          "b2",
          "b3",
          "b4",
          "b5",
          "b6",
          "b7",
          "v2_s",
          "v2_m",
          "v2_l"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "googlenet": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "base",
         "description": "Pretrained model variant to use.",
         "enum": [
          "base"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "inceptionv3": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "base",
         "description": "Pretrained model variant to use.",
         "enum": [
          "base"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "maxvit": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "t",
         "description": "Pretrained model variant to use.",
         "enum": [
          "t"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "mlp_mixer": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "avg_pool": {
         "default": true,
         "description": "If true, pools output over patch dimension, outputs a vector of shape (embed_size). If false, the output tensor is of shape (n_patches, embed_size), where n_patches is img_height x img_width / patch_size\u00b2.",
         "type": "boolean"
        },
        "channel_dim": {
         "default": 256,
         "description": "Number of channels in hidden layer.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embed_size": {
         "default": 512,
         "description": "The patch embedding size, the output size of the mixer if avg_pool is true.",
         "minimum": 1,
         "type": "integer"
        },
        "height": {
         "default": null,
         "description": "Height of the input image.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "num_channels": {
         "default": null,
         "description": "Number of channels to use in the encoder.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "num_layers": {
         "default": 8,
         "description": "The depth of the network (the number of Mixer blocks).",
         "minimum": 1,
         "type": "integer"
        },
        "patch_size": {
         "default": 16,
         "description": "The image patch size. Each patch is patch_size\u00b2 pixels. Must evenly divide the image width and height.",
         "minimum": 1,
         "type": "integer"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "token_size": {
         "default": 2048,
         "description": "The per-patch embedding size.",
         "minimum": 1,
         "type": "integer"
        },
        "width": {
         "default": null,
         "description": "Width of the input image.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        }
       },
       "mnasnet": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "0_5",
         "description": "Pretrained model variant to use.",
         "enum": [
          "0_5",
          "0_75",
          "1_0",
          "1_3"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "mobilenetv2": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "base",
         "description": "Pretrained model variant to use.",
         "enum": [
          "base"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "mobilenetv3": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "small",
         "description": "Pretrained model variant to use.",
         "enum": [
          "small",
          "large"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "poolformer": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_name": {
         "default": "poolformerv2_s12",
         "description": "PoolFormer model variant. MetaFormer using simple average pooling as token mixer. V2 variants use StarReLU activation and improved training recipe.",
         "enum": [
          "poolformerv2_s12",
          "poolformerv2_s24",
          "poolformerv2_s36",
          "poolformerv2_m36",
          "poolformerv2_m48",
          "poolformer_s12",
          "poolformer_s24",
          "poolformer_s36",
          "poolformer_m36",
          "poolformer_m48"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "regnet": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "x_1_6gf",
         "description": "Pretrained model variant to use.",
         "enum": [
          "x_1_6gf",
          "x_16gf",
          "x_32gf",
          "x_3_2gf",
          "x_400mf",
          "x_800mf",
          "x_8gf",
          "y_128gf",
          "y_16gf",
          "y_1_6gf",
          "y_32gf",
          "y_3_2gf",
          "y_400mf",
          "y_800mf",
          "y_8gf"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "resnet": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": 50,
         "description": "Pretrained model variant to use.",
         "enum": [
          18,
          34,
          50,
          101,
          152
         ],
         "type": "integer"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "resnext": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "50_32x4d",
         "description": "Pretrained model variant to use.",
         "enum": [
          "50_32x4d",
          "101_32x8d",
          "101_64x4d"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "shufflenet_v2": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "x0_5",
         "description": "Pretrained model variant to use.",
         "enum": [
          "x0_5",
          "x1_0",
          "x1_5",
          "x2_0"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "siglip": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "google/siglip-base-patch16-224",
         "description": "HuggingFace model path or name for the SigLIP vision model.",
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "squeezenet": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "1_0",
         "description": "Pretrained model variant to use.",
         "enum": [
          "1_0",
          "1_1"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "stacked_cnn": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "conv_activation": {
         "default": "relu",
         "description": "If an activation is not already specified in conv_layers this is the default activation that will be used for each layer. It indicates the activation function applied to the output.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "conv_dropout": {
         "default": 0.0,
         "description": "Dropout rate",
         "maximum": 1,
         "minimum": 0,
         "type": null
        },
        "conv_layers": {
         "default": null,
         "description": "List of convolutional layers to use in the encoder.",
         "type": null
        },
        "conv_norm": {
         "default": null,
         "description": "If a norm is not already specified in conv_layers this is the default norm that will be used for each layer. It indicates the normalization applied to the activations and can be null, batch or layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "conv_norm_params": {
         "default": null,
         "description": "Parameters used if conv_norm is either batch or layer.",
         "type": null
        },
        "conv_use_bias": {
         "default": true,
         "description": "If bias not already specified in conv_layers, specifies if the 2D convolutional kernel should have a bias term.",
         "nullable": true,
         "type": "boolean"
        },
        "dilation": {
         "default": 1,
         "description": "An int or pair of ints specifying the dilation rate to use for dilated convolution. If dilation is not already specified in conv_layers, specifies the default dilation of the 2D convolutional kernel that will be used for each layer.",
         "type": null
        },
        "fc_activation": {
         "default": "relu",
         "description": "If an activation is not already specified in fc_layers this is the default activation that will be used for each layer. It indicates the activation function applied to the output.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector.",
         "enum": [
          "constant",
          "dirac",
          "eye",
          "identity",
          "kaiming_normal",
          "kaiming_uniform",
          "normal",
          "ones",
          "orthogonal",
          "sparse",
          "uniform",
          "xavier_normal",
          "xavier_uniform",
          "zeros"
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Dropout rate",
         "maximum": 1,
         "minimum": 0,
         "nullable": true,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "A list of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: activation, dropout, norm, norm_params, output_size, use_bias, bias_initializer and weights_initializer. If any of those values is missing from the dictionary, the default one specified as a parameter of the encoder will be used instead.",
         "type": null
        },
        "fc_norm": {
         "default": null,
         "description": "If a norm is not already specified in fc_layers this is the default norm that will be used for each layer. It indicates the norm of the output and can be null, batch or layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "description": "Parameters used if norm is either batch or layer. For information on parameters used with batch see Torch's documentation on batch normalization or for layer see Torch's documentation on layer normalization.",
         "type": null
        },
        "fc_use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "nullable": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weights matrix.",
         "enum": [
          "constant",
          "dirac",
          "eye",
          "identity",
          "kaiming_normal",
          "kaiming_uniform",
          "normal",
          "ones",
          "orthogonal",
          "sparse",
          "uniform",
          "xavier_normal",
          "xavier_uniform",
          "zeros"
         ],
         "nullable": true,
         "type": "string"
        },
        "groups": {
         "default": 1,
         "description": "Groups controls the connectivity between convolution inputs and outputs. When groups = 1, each output channel depends on every input channel. When groups > 1, input and output channels are divided into groups separate groups, where each output channel depends only on the inputs in its respective input channel group. in_channels and out_channels must both be divisible by groups.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "height": {
         "default": null,
         "description": "Height of the input image.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "kernel_size": {
         "default": 3,
         "description": "An integer or pair of integers specifying the kernel size. A single integer specifies a square kernel, while a pair of integers specifies the height and width of the kernel in that order (h, w). If a kernel_size is not specified in conv_layers this kernel_size that will be used for each layer.",
         "type": null
        },
        "num_channels": {
         "default": null,
         "description": "Number of channels to use in the encoder.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "num_conv_layers": {
         "default": null,
         "description": "Number of convolutional layers to use in the encoder.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 1,
         "description": "The number of stacked fully connected layers.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "out_channels": {
         "default": 32,
         "description": "Indicates the number of filters, and by consequence the output channels of the 2d convolution. If out_channels is not already specified in conv_layers this is the default out_channels that will be used for each layer.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "output_size": {
         "default": 128,
         "description": "If output_size is not already specified in fc_layers this is the default output_size that will be used for each layer. It indicates the size of the output of a fully connected layer.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "padding": {
         "default": "valid",
         "description": "An int, pair of ints (h, w), or one of ['valid', 'same'] specifying the padding used forconvolution kernels.",
         "type": null
        },
        "padding_mode": {
         "default": "zeros",
         "description": "If padding_mode is not already specified in conv_layers, specifies the default padding_mode of the 2D convolutional kernel that will be used for each layer.",
         "enum": [
          "zeros",
          "reflect",
          "replicate",
          "circular"
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_dilation": {
         "default": 1,
         "description": "An integer or pair of ints specifying pooling dilation rate (h, w).",
         "type": null
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use.",
         "enum": [
          "max",
          "average",
          "avg",
          "mean"
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_kernel_size": {
         "default": 2,
         "description": "An integer or pair of integers specifying the pooling size. If pool_kernel_size is not specified in conv_layers this is the default value that will be used for each layer.",
         "type": null
        },
        "pool_padding": {
         "default": 0,
         "description": "An integer or pair of ints specifying pooling padding (h, w).",
         "type": null
        },
        "pool_stride": {
         "default": null,
         "description": "An integer or pair of integers specifying the pooling stride, which is the factor by which the pooling layer downsamples the feature map. Defaults to pool_kernel_size.",
         "type": null
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "stride": {
         "default": 1,
         "description": "An integer or pair of integers specifying the stride of the convolution along the height and width. If a stride is not already specified in conv_layers, specifies the default stride of the 2D convolutional kernel that will be used for each layer.",
         "type": null
        },
        "width": {
         "default": null,
         "description": "Width of the input image.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        }
       },
       "swin_transformer": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "t",
         "description": "Pretrained model variant to use.",
         "enum": [
          "t",
          "s",
          "b"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "timm": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_name": {
         "default": "caformer_s18",
         "description": "Name of the timm model to use. Any model from the timm library is supported. See https://huggingface.co/docs/timm for available models.",
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "unet": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "conv_norm": {
         "default": "batch",
         "description": "This is the default norm that will be used for each double conv layer.It can be null or batch.",
         "enum": [
          "batch",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "height": {
         "default": null,
         "description": "Height of the input image.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "num_channels": {
         "default": null,
         "description": "Number of channels in the input image.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "width": {
         "default": null,
         "description": "Width of the input image.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        }
       },
       "vgg": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": 11,
         "description": "Pretrained model variant to use.",
         "type": null
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "vit": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "b_16",
         "description": "Pretrained model variant to use.",
         "enum": [
          "b_16",
          "b_32",
          "l_16",
          "l_32",
          "h_14"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       },
       "wide_resnet": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "model_cache_dir": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "model_variant": {
         "default": "50_2",
         "description": "Pretrained model variant to use.",
         "enum": [
          "50_2",
          "101_2"
         ],
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": true,
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        }
       }
      },
      "types": [
       "stacked_cnn",
       "mlp_mixer",
       "unet",
       "clip",
       "dinov2",
       "siglip",
       "timm",
       "caformer",
       "convformer",
       "poolformer",
       "convnextv2",
       "alexnet",
       "convnext",
       "densenet",
       "efficientnet",
       "googlenet",
       "inceptionv3",
       "maxvit",
       "mnasnet",
       "mobilenetv2",
       "mobilenetv3",
       "regnet",
       "resnet",
       "resnext",
       "shufflenet_v2",
       "squeezenet",
       "swin_transformer",
       "vit",
       "vgg",
       "wide_resnet"
      ]
     },
     "preprocessing": {
      "fields": {
       "computed_fill_value": {
        "default": null,
        "description": "The internally computed fill value to replace missing values with in case the missing_value_strategy is fill_with_mode or fill_with_mean",
        "minimum": 0.0,
        "nullable": true,
        "type": "number"
       },
       "fill_value": {
        "default": null,
        "description": "The maximum number of most common tokens to be considered. If the data contains more than this amount, the most infrequent tokens will be treated as unknown.",
        "minimum": 0.0,
        "nullable": true,
        "type": "number"
       },
       "height": {
        "default": null,
        "description": "The image height in pixels. If this parameter is set, images will be resized to the specified height using the resize_method parameter. If None, images will be resized to the size of the first image in the dataset.",
        "minimum": 1,
        "nullable": true,
        "type": "integer"
       },
       "in_memory": {
        "default": true,
        "description": "Defines whether image dataset will reside in memory during the training process or will be dynamically fetched from disk (useful for large datasets). In the latter case a training batch of input images will be fetched from disk each training iteration.",
        "type": "boolean"
       },
       "infer_image_dimensions": {
        "default": true,
        "description": "If true, then the height and width of images in the dataset will be inferred from a sample of the first image in the dataset. Each image that doesn't conform to these dimensions will be resized according to resize_method. If set to false, then the height and width of images in the dataset will be specified by the user.",
        "type": "boolean"
       },
       "infer_image_max_height": {
        "default": 256,
        "description": "If infer_image_dimensions is set, this is used as the maximum height of the images in the dataset.",
        "minimum": 1,
        "type": "integer"
       },
       "infer_image_max_width": {
        "default": 256,
        "description": "If infer_image_dimensions is set, this is used as the maximum width of the images in the dataset.",
        "minimum": 1,
        "type": "integer"
       },
       "infer_image_num_channels": {
        "default": true,
        "description": "If true, then the number of channels in the dataset is inferred from a sample of the first image in the dataset.",
        "type": "boolean"
       },
       "infer_image_num_classes": {
        "default": false,
        "description": "If true, then the number of channel classes in the dataset will be inferred from a sample of the first image in the dataset. Each unique channel value will be mapped to a class and preprocessing will create a masked image based on the channel classes.",
        "type": "boolean"
       },
       "infer_image_sample_size": {
        "default": 100,
        "description": "The sample size used for inferring dimensions of images in infer_image_dimensions.",
        "minimum": 1,
        "type": "integer"
       },
       "lazy_cache_dir": {
        "default": null,
        "description": "Directory in which to cache image files when the source data is in-memory (e.g. a HuggingFace dataset). Only used when mode is 'lazy' or 'lazy_cached' and the input entries are not already paths to existing files. When None, defaults to ~/.cache/ludwig/lazy_media/<feature_name>/. Has no effect when the input column already contains local file paths.",
        "nullable": true,
        "type": "string"
       },
       "missing_value_strategy": {
        "default": "bfill",
        "description": "What strategy to follow when there's a missing value in an image column",
        "enum": [
         "fill_with_const",
         "fill_with_mode",
         "bfill",
         "ffill",
         "drop_row"
        ],
        "type": "string"
       },
       "mode": {
        "default": "lazy",
        "description": "Preprocessing mode for image features. 'eager' decodes all files during preprocessing and stores tensors in the Parquet cache. 'lazy' stores file paths and decodes per batch during training, keeping peak memory bounded to batch_size \u00d7 image_size. 'lazy_cached' behaves like 'lazy' on the first training epoch but writes decoded tensors to a numpy memmap alongside the Parquet cache; subsequent epochs read from the memmap directly, eliminating decode overhead. Lazy mode is disabled automatically when a torchvision pretrained encoder is used.",
        "enum": [
         "eager",
         "lazy",
         "lazy_cached"
        ],
        "type": "string"
       },
       "num_channels": {
        "default": null,
        "description": "Number of channels in the images. If specified, images will be read in the mode specified by the number of channels. If not specified, the number of channels will be inferred from the image format of the first valid image in the dataset.",
        "minimum": 1,
        "nullable": true,
        "type": "integer"
       },
       "num_classes": {
        "default": null,
        "description": "Number of channel classes in the images. If specified, this value will be validated against the inferred number of classes. Use 2 to convert grayscale images to binary images.",
        "minimum": 1,
        "nullable": true,
        "type": "integer"
       },
       "num_processes": {
        "default": 1,
        "description": "Specifies the number of processes to run for preprocessing images.",
        "minimum": 1,
        "type": "integer"
       },
       "prefetch_size": {
        "default": null,
        "description": "Number of batches to prefetch in a background thread while the GPU processes the current batch. None (default) selects automatically: 0 for 'eager' mode, 4 for 'lazy' and 'lazy_cached' (epoch 1). After the first epoch in 'lazy_cached' mode, prefetch is automatically disabled since memmap reads are fast enough. Set to 0 to disable prefetch entirely, or to a positive integer to override the automatic selection.",
        "minimum": 0,
        "nullable": true,
        "type": "integer"
       },
       "requires_equal_dimensions": {
        "default": false,
        "description": "If true, then width and height must be equal.",
        "type": "boolean"
       },
       "resize_method": {
        "default": "interpolate",
        "description": "The method to use for resizing images.",
        "enum": [
         "crop_or_pad",
         "interpolate"
        ],
        "type": "string"
       },
       "standardize_image": {
        "default": null,
        "description": "Standardize image by per channel mean centering and standard deviation scaling .",
        "enum": [
         "imagenet1k",
         null
        ],
        "nullable": true,
        "type": "string"
       },
       "width": {
        "default": null,
        "description": "The image width in pixels. If this parameter is set, images will be resized to the specified width using the resize_method parameter. If None, images will be resized to the size of the first image in the dataset.",
        "minimum": 1,
        "nullable": true,
        "type": "integer"
       }
      }
     }
    },
    "number": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "name": {
       "description": "Name of the input feature.",
       "type": "string"
      },
      "tied": {
       "default": null,
       "nullable": true,
       "type": "string"
      }
     },
     "encoder": {
      "options": {
       "bins": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bins_bin_edges": {
         "default": null,
         "description": "[Internal] Bin edges computed from training data. Set automatically.",
         "type": null
        },
        "num_bins": {
         "default": 32,
         "description": "Number of bins to discretize numeric values into.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "Size of the output embedding per bin.",
         "minimum": 1,
         "type": "integer"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "dense": {
        "activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the dense encoder.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "Default normalization applied at the beginnging of fully connected layers.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_layers": {
         "default": 1,
         "description": "Number of stacked fully connected layers to apply. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "Size of the output of the feature.",
         "minimum": 1,
         "type": "integer"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "passthrough": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "periodic": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "num_frequencies": {
         "default": 64,
         "description": "Number of learnable sinusoidal frequencies.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "Size of the output embedding.",
         "minimum": 1,
         "type": "integer"
        },
        "sigma": {
         "default": 1.0,
         "description": "Standard deviation for initializing frequency parameters.",
         "type": "number"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "ple": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "num_bins": {
         "default": 64,
         "description": "Number of quantile bins for piecewise linear encoding.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "Size of the output embedding.",
         "minimum": 1,
         "type": "integer"
        },
        "ple_bin_edges": {
         "default": null,
         "description": "[Internal] Quantile bin edges computed from training data. Set automatically.",
         "type": null
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       }
      },
      "types": [
       "passthrough",
       "dense",
       "ple",
       "periodic",
       "bins"
      ]
     },
     "preprocessing": {
      "fields": {
       "computed_fill_value": {
        "default": 0.0,
        "description": "The internally computed fill value to replace missing values with in case the missing_value_strategy is fill_with_mode or fill_with_mean",
        "type": "number"
       },
       "computed_outlier_fill_value": {
        "default": 0.0,
        "description": "The internally computed fill value to replace outliers with in case the outlier_strategy is fill_with_mode or fill_with_mean",
        "type": "number"
       },
       "fill_value": {
        "default": 0.0,
        "description": "The value to replace missing values with in case the missing_value_strategy is fill_with_const",
        "type": "number"
       },
       "missing_value_strategy": {
        "default": "fill_with_const",
        "description": "What strategy to follow when there's a missing value in a number column",
        "enum": [
         "fill_with_const",
         "fill_with_mode",
         "bfill",
         "ffill",
         "drop_row",
         "fill_with_mean"
        ],
        "type": "string"
       },
       "normalization": {
        "default": "zscore",
        "description": "Normalization strategy to use for this number feature. If the value is `null` no normalization is performed.",
        "enum": [
         "zscore",
         "minmax",
         "log1p",
         "iq",
         null
        ],
        "nullable": true,
        "type": "string"
       },
       "outlier_strategy": {
        "default": null,
        "description": "Determines how outliers will be handled in the dataset. In most cases, replacing outliers with the column mean (`fill_with_mean`) will be sufficient, but in others the outliers may be damaging enough to merit dropping the entire row of data (`drop_row`). In some cases, the best way to handle outliers is to leave them in the data, which is the behavior when this parameter is left as `null`.",
        "enum": [
         "fill_with_const",
         "fill_with_mode",
         "bfill",
         "ffill",
         "drop_row",
         "fill_with_mean",
         null
        ],
        "nullable": true,
        "type": "string"
       },
       "outlier_threshold": {
        "default": 3.0,
        "description": "Standard deviations from the mean past which a value is considered an outlier. The 3-sigma rule in statistics tells us that when data is normally distributed, 95% of the data will lie within 2 standard deviations of the mean, and greater than 99% of the data will lie within 3 standard deviations of the mean (see: [68\u201395\u201399.7 rule](https://en.wikipedia.org/wiki/68%E2%80%9395%E2%80%9399.7_rule)). As such anything farther away than that is highly likely to be an outlier, and may distort the learning process by disproportionately affecting the model.",
        "minimum": 0.0,
        "nullable": true,
        "type": "number"
       }
      }
     }
    },
    "sequence": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "name": {
       "description": "Name of the input feature.",
       "type": "string"
      },
      "tied": {
       "default": null,
       "nullable": true,
       "type": "string"
      }
     },
     "encoder": {
      "options": {
       "cnnrnn": {
        "activation": {
         "default": "tanh",
         "description": "The default activation function to use.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "bidirectional": {
         "default": false,
         "description": "If true, two recurrent networks will perform encoding in the forward and backward direction and their outputs will be concatenated.",
         "type": "boolean"
        },
        "cell_type": {
         "default": "rnn",
         "description": "The type of recurrent cell to use. Available values are: `rnn`, `lstm`, `gru`. For reference about the differences between the cells please refer to [torch.nn Recurrent Layers](https://pytorch.org/docs/stable/nn.html#recurrent-layers).",
         "enum": [
          "rnn",
          "lstm",
          "gru"
         ],
         "type": "string"
        },
        "conv_activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each convolutional layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "conv_dropout": {
         "default": 0.0,
         "description": "The dropout rate for the convolutional layers",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "conv_layers": {
         "default": null,
         "description": "A list of dictionaries containing the parameters of all the convolutional layers. The length of the list determines the number of stacked convolutional layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `num_filters`, `filter_size`, `strides`, `padding`, `dilation_rate`, `use_bias`, `pool_function`, `pool_padding`, `pool_size`, `pool_strides`, `bias_initializer`, `weights_initializer`. If any of those values is missing from the dictionary, the default one specified as a parameter of the encoder will be used instead. If both `conv_layers` and `num_conv_layers` are `null`, a default list will be assigned to `conv_layers` with the value `[{filter_size: 7, pool_size: 3}, {filter_size: 7, pool_size: 3}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: 3}]`.",
         "type": null
        },
        "dilation_rate": {
         "default": 1,
         "description": "Dilation rate to use for dilated convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "filter_size": {
         "default": 5,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_conv_layers": {
         "default": null,
         "description": "The number of stacked convolutional layers when `conv_layers` is `null`.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of parallel fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "num_rec_layers": {
         "default": 1,
         "description": "The number of stacked recurrent layers.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pool_strides": {
         "default": null,
         "description": "Factor to scale down.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "recurrent_activation": {
         "default": "sigmoid",
         "description": "The activation function to use in the recurrent step",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "recurrent_dropout": {
         "default": 0.0,
         "description": "The dropout rate for the recurrent state",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "recurrent_initializer": {
         "default": "orthogonal",
         "description": "The initializer for recurrent matrix weights",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "reduce_output": {
         "default": "last",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "state_size": {
         "default": 256,
         "description": "The size of the state of the rnn.",
         "minimum": 1,
         "type": "integer"
        },
        "strides": {
         "default": 1,
         "description": "Stride length of the convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "unit_forget_bias": {
         "default": true,
         "description": "If true, add 1 to the bias of the forget gate at initialization",
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "embed": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate applied to the embedding. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "jamba": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "attention_every_k": {
         "default": 4,
         "description": "Every `attention_every_k`-th block is attention, the remainder are SSM. Default 4 gives a 1:3 attention:SSM ratio matching the Jamba paper.",
         "minimum": 1,
         "type": null
        },
        "d_conv": {
         "default": 4,
         "description": "Width of the depthwise 1D convolution inside each SSM block.",
         "minimum": 1,
         "type": null
        },
        "d_model": {
         "default": 256,
         "description": "Hidden width of every block \u2014 SSM and attention share the same d_model.",
         "minimum": 1,
         "type": null
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": null
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": null
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": null
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": null
        },
        "expand_factor": {
         "default": 2,
         "description": "Inner expansion factor inside each SSM block.",
         "minimum": 1,
         "type": null
        },
        "ffn_size": {
         "default": 1024,
         "description": "Feed-forward width inside each attention block.",
         "minimum": 1,
         "type": null
        },
        "n_layers": {
         "default": 8,
         "description": "Total number of stacked blocks (SSM + attention combined).",
         "minimum": 1,
         "type": null
        },
        "num_heads": {
         "default": 8,
         "description": "Number of attention heads (and SSD heads, shared).",
         "minimum": 1,
         "type": null
        },
        "output_size": {
         "default": 256,
         "description": "Output feature width emitted by the encoder.",
         "minimum": 1,
         "type": null
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "type": null
        },
        "reduce_output": {
         "default": "mean",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "type": null
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": null
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings.",
         "type": null
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "mamba": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "d_conv": {
         "default": 4,
         "description": "Width of the 1D convolution in each SSM layer.",
         "minimum": 1,
         "type": "integer"
        },
        "d_model": {
         "default": 256,
         "description": "Dimensionality of the model (hidden size for SSM layers).",
         "minimum": 1,
         "type": "integer"
        },
        "d_state": {
         "default": 16,
         "description": "Dimensionality of the state space in each SSM layer.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "expand_factor": {
         "default": 2,
         "description": "Expansion factor for the inner dimension of SSM layers.",
         "minimum": 1,
         "type": "integer"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "n_layers": {
         "default": 4,
         "description": "Number of stacked SSM layers.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each FC layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "mean",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use bias in linear projections.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "mamba2": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "d_conv": {
         "default": 4,
         "description": "Width of the depthwise 1D convolution inside each block.",
         "minimum": 1,
         "type": null
        },
        "d_model": {
         "default": 256,
         "description": "Hidden width of each Mamba-2 block.",
         "minimum": 1,
         "type": null
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": null
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": null
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": null
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": null
        },
        "expand_factor": {
         "default": 2,
         "description": "Inner expansion factor for each block.",
         "minimum": 1,
         "type": null
        },
        "n_layers": {
         "default": 4,
         "description": "Number of stacked Mamba-2 blocks.",
         "minimum": 1,
         "type": null
        },
        "num_heads": {
         "default": 8,
         "description": "Number of SSD heads. `d_model * expand_factor` must be divisible by `num_heads`.",
         "minimum": 1,
         "type": null
        },
        "output_size": {
         "default": 256,
         "description": "Output feature width emitted by the encoder.",
         "minimum": 1,
         "type": null
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "type": null
        },
        "reduce_output": {
         "default": "mean",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "type": null
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": null
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings.",
         "type": null
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "parallel_cnn": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "conv_layers": {
         "default": null,
         "description": "A list of dictionaries containing the parameters of all the convolutional layers. The length of the list determines the number of stacked convolutional layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `num_filters`, `filter_size`, `strides`, `padding`, `dilation_rate`, `use_bias`, `pool_function`, `pool_padding`, `pool_size`, `pool_strides`, `bias_initializer`, `weights_initializer`. If any of those values is missing from the dictionary, the default one specified as a parameter of the encoder will be used instead. If both `conv_layers` and `num_conv_layers` are `null`, a default list will be assigned to `conv_layers` with the value `[{filter_size: 7, pool_size: 3}, {filter_size: 7, pool_size: 3}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: 3}]`.",
         "type": null
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate applied to the embedding. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "filter_size": {
         "default": 3,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_conv_layers": {
         "default": null,
         "description": "The number of stacked convolutional layers when `conv_layers` is `null`.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": null,
         "description": "Number of parallel fully connected layers to use.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "Whether to embed the input sequence.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "rnn": {
        "activation": {
         "default": "tanh",
         "description": "The default activation function.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "bidirectional": {
         "default": false,
         "description": "If true, two recurrent networks will perform encoding in the forward and backward direction and their outputs will be concatenated.",
         "type": "boolean"
        },
        "cell_type": {
         "default": "rnn",
         "description": "The type of recurrent cell to use. Available values are: `rnn`, `lstm`, `gru`. For reference about the differences between the cells please refer to [torch.nn Recurrent Layers](https://pytorch.org/docs/stable/nn.html#recurrent-layers).",
         "enum": [
          "rnn",
          "lstm",
          "gru"
         ],
         "type": "string"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of parallel fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "num_layers": {
         "default": 1,
         "description": "The number of stacked recurrent layers.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "recurrent_activation": {
         "default": "sigmoid",
         "description": "The activation function to use in the recurrent step",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "recurrent_dropout": {
         "default": 0.0,
         "description": "The dropout rate for the recurrent state",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "recurrent_initializer": {
         "default": "orthogonal",
         "description": "The initializer for recurrent matrix weights",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "reduce_output": {
         "default": "last",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "state_size": {
         "default": 256,
         "description": "The size of the state of the rnn.",
         "minimum": 1,
         "type": "integer"
        },
        "unit_forget_bias": {
         "default": true,
         "description": "If true, add 1 to the bias of the forget gate at initialization",
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "stacked_cnn": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "conv_layers": {
         "default": null,
         "description": "A list of dictionaries containing the parameters of all the convolutional layers. The length of the list determines the number of stacked convolutional layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `num_filters`, `filter_size`, `strides`, `padding`, `dilation_rate`, `use_bias`, `pool_function`, `pool_padding`, `pool_size`, `pool_strides`, `bias_initializer`, `weights_initializer`. If any of those values is missing from the dictionary, the default one specified as a parameter of the encoder will be used instead. If both `conv_layers` and `num_conv_layers` are `null`, a default list will be assigned to `conv_layers` with the value `[{filter_size: 7, pool_size: 3}, {filter_size: 7, pool_size: 3}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: 3}]`.",
         "type": null
        },
        "dilation_rate": {
         "default": 1,
         "description": "Dilation rate to use for dilated convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate applied to the embedding. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "filter_size": {
         "default": 3,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_conv_layers": {
         "default": null,
         "description": "The number of stacked convolutional layers when `conv_layers` is `null`.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": null,
         "description": "Number of parallel fully connected layers to use.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pool_strides": {
         "default": null,
         "description": "Factor to scale down.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "Whether to embed the input sequence.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "strides": {
         "default": 1,
         "description": "Stride length of the convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "stacked_parallel_cnn": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate applied to the embedding. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "filter_size": {
         "default": 3,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": null,
         "description": "Number of parallel fully connected layers to use.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "num_stacked_layers": {
         "default": null,
         "description": "If stacked_layers is null, this is the number of elements in the stack of parallel convolutional layers.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "stacked_layers": {
         "default": null,
         "description": "a nested list of lists of dictionaries containing the parameters of the stack of parallel convolutional layers. The length of the list determines the number of stacked parallel convolutional layers, length of the sub-lists determines the number of parallel conv layers and the content of each dictionary determines the parameters for a specific layer.",
         "type": null
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "transformer": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "dropout": {
         "default": 0.1,
         "description": "The dropout rate for the transformer block. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "hidden_size": {
         "default": 256,
         "description": "The size of the hidden representation within the transformer block. It is usually the same as the embedding_size, but if the two values are different, a projection layer will be added before the first transformer block.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of parallel fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "num_heads": {
         "default": 8,
         "description": "Number of attention heads in each transformer block.",
         "minimum": 1,
         "type": "integer"
        },
        "num_layers": {
         "default": 1,
         "description": "The number of transformer layers.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "last",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "transformer_output_size": {
         "default": 256,
         "description": "Size of the fully connected layer after self attention in the transformer block. This is usually the same as hidden_size and embedding_size.",
         "minimum": 1,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "use_rope": {
         "default": false,
         "description": "If True, use Rotary Position Embeddings (RoPE) instead of absolute positional embeddings. RoPE encodes position by rotating query and key vectors, providing better length generalization and relative position awareness. Used by modern LLMs like LLaMA and Mistral.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       }
      },
      "types": [
       "embed",
       "parallel_cnn",
       "stacked_cnn",
       "stacked_parallel_cnn",
       "rnn",
       "cnnrnn",
       "mamba",
       "transformer",
       "mamba2",
       "jamba"
      ]
     },
     "preprocessing": {
      "fields": {
       "cache_encoder_embeddings": {
        "default": false,
        "description": "Compute encoder embeddings in preprocessing, speeding up training time considerably.",
        "type": "boolean"
       },
       "computed_fill_value": {
        "default": "<UNK>",
        "description": "The internally computed fill value to replace missing values with in case the missing_value_strategy is fill_with_mode or fill_with_mean",
        "type": "string"
       },
       "fill_value": {
        "default": "<UNK>",
        "description": "The value to replace missing values with in case the missing_value_strategy is fill_with_const",
        "type": "string"
       },
       "lowercase": {
        "default": false,
        "description": "If true, converts the string to lowercase before tokenizing.",
        "type": "boolean"
       },
       "max_sequence_length": {
        "default": 256,
        "description": "The maximum length (number of tokens) of the sequence. Sequences that are longer than this value will be truncated. Useful as a stopgap measure if `sequence_length` is set to `None`. If `None`, max sequence length will be inferred from the training dataset.",
        "minimum": 1,
        "nullable": true,
        "type": "integer"
       },
       "missing_value_strategy": {
        "default": "fill_with_const",
        "description": "What strategy to follow when there's a missing value in a text column",
        "enum": [
         "fill_with_const",
         "fill_with_mode",
         "bfill",
         "ffill",
         "drop_row"
        ],
        "type": "string"
       },
       "most_common": {
        "default": 20000,
        "description": "The maximum number of most common tokens in the vocabulary. If the data contains more than this amount, the most infrequent symbols will be treated as unknown.",
        "minimum": 1,
        "type": "integer"
       },
       "ngram_size": {
        "default": 2,
        "description": "The size of the ngram when using the `ngram` tokenizer (e.g, 2 = bigram, 3 = trigram, etc.).",
        "minimum": 1,
        "type": "integer"
       },
       "padding": {
        "default": "right",
        "description": "The direction of the padding.",
        "enum": [
         "left",
         "right"
        ],
        "type": "string"
       },
       "padding_symbol": {
        "default": "<PAD>",
        "description": "The string used as a padding symbol. This special token is mapped to the integer ID 0 in the vocabulary.",
        "type": "string"
       },
       "sequence_length": {
        "default": null,
        "description": "The desired length (number of tokens) of the sequence. Sequences that are longer than this value will be truncated and sequences shorter than this value will be padded. If None, sequence length will be inferred from the training dataset.",
        "minimum": 1,
        "nullable": true,
        "type": "integer"
       },
       "tokenizer": {
        "default": "space",
        "description": "Defines how to map from the raw string content of the dataset column to a sequence of elements.",
        "type": "string"
       },
       "unknown_symbol": {
        "default": "<UNK>",
        "description": "The string used as an unknown placeholder. This special token is mapped to the integer ID 1 in the vocabulary.",
        "type": "string"
       },
       "vocab_file": {
        "default": null,
        "description": "Filepath string to a UTF-8 encoded file containing the sequence's vocabulary. On each line the first string until or is considered a word.",
        "nullable": true,
        "type": "string"
       }
      }
     }
    },
    "set": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "name": {
       "description": "Name of the input feature.",
       "type": "string"
      },
      "tied": {
       "default": null,
       "nullable": true,
       "type": "string"
      }
     },
     "encoder": {
      "options": {
       "embed": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer to use for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout probability for the embedding.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 50,
         "description": "The maximum embedding size, the actual size will be min(vocabulary_size, embedding_size) for dense representations and exactly vocabulary_size for the sparse encoding, where vocabulary_size is the number of different strings appearing in the training set in the input column (plus 1 for the unknown token placeholder <UNK>).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If true embeddings are trained during the training process, if false embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when representation is dense as sparse one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "This is the number of stacked fully connected layers that the input to the feature passes through. Their output is projected in the feature's output space.",
         "minimum": 0,
         "type": "integer"
        },
        "output_size": {
         "default": 10,
         "description": "If output_size is not already specified in fc_layers this is the default output_size that will be used for each layer. It indicates the size of the output of a fully connected layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "By default dense embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the GloVe format. When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if representation is dense.",
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "The representation of the embedding. Either dense or sparse.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary of the encoder",
         "type": null
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer to use for the weights matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       }
      },
      "types": [
       "embed"
      ]
     },
     "preprocessing": {
      "fields": {
       "computed_fill_value": {
        "default": "<UNK>",
        "description": "The internally computed fill value to replace missing values with in case the missing_value_strategy is fill_with_mode or fill_with_mean",
        "type": "string"
       },
       "fill_value": {
        "default": "<UNK>",
        "description": "The value to replace missing values with in case the missing_value_strategy is fill_with_const",
        "type": "string"
       },
       "lowercase": {
        "default": false,
        "description": "If true, converts the string to lowercase before tokenizing.",
        "type": "boolean"
       },
       "missing_value_strategy": {
        "default": "fill_with_const",
        "description": "What strategy to follow when there's a missing value in a set column",
        "enum": [
         "fill_with_const",
         "fill_with_mode",
         "bfill",
         "ffill",
         "drop_row"
        ],
        "type": "string"
       },
       "most_common": {
        "default": 10000,
        "description": "The maximum number of most common tokens to be considered. If the data contains more than this amount, the most infrequent tokens will be treated as unknown.",
        "minimum": 1,
        "nullable": true,
        "type": "integer"
       },
       "tokenizer": {
        "default": "space",
        "description": "Defines how to transform the raw text content of the dataset column to a set of elements. The default value space splits the string on spaces. Common options include: underscore (splits on underscore), comma (splits on comma), json (decodes the string into a set or a list through a JSON parser).",
        "type": "string"
       }
      }
     }
    },
    "text": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "name": {
       "description": "Name of the input feature.",
       "type": "string"
      },
      "tied": {
       "default": null,
       "nullable": true,
       "type": "string"
      }
     },
     "encoder": {
      "options": {
       "albert": {
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "attention_probs_dropout_prob": {
         "default": 0.0,
         "description": "The dropout ratio for the attention probabilities.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "bos_token_id": {
         "default": 2,
         "description": "The beginning of sequence token ID.",
         "type": "integer"
        },
        "classifier_dropout_prob": {
         "default": 0.1,
         "description": "The dropout ratio for attached classifiers.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 128,
         "description": "Dimensionality of vocabulary embeddings.",
         "minimum": 1,
         "type": "integer"
        },
        "eos_token_id": {
         "default": 3,
         "description": "The end of sequence token ID.",
         "type": "integer"
        },
        "hidden_act": {
         "default": "gelu_new",
         "description": "The non-linear activation function (function or string) in the encoder and pooler.",
         "enum": [
          "gelu",
          "relu",
          "silu",
          "gelu_new"
         ],
         "type": "string"
        },
        "hidden_dropout_prob": {
         "default": 0.0,
         "description": "The dropout probability for all fully connected layers in the embeddings, encoder, and pooler.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "hidden_size": {
         "default": 768,
         "description": "Dimensionality of the encoder layers and the pooler layer.",
         "minimum": 1,
         "type": "integer"
        },
        "initializer_range": {
         "default": 0.02,
         "description": "The standard deviation of the truncated_normal_initializer for initializing all weight matrices.",
         "minimum": 0.0,
         "type": "number"
        },
        "inner_group_num": {
         "default": 1,
         "description": "The number of inner repetition of attention and ffn.",
         "minimum": 1,
         "type": "integer"
        },
        "intermediate_size": {
         "default": 3072,
         "description": "The dimensionality of the \u201cintermediate\u201d (often named feed-forward) layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "layer_norm_eps": {
         "default": 1e-12,
         "description": "The epsilon used by the layer normalization layers.",
         "minimum": 0.0,
         "type": "number"
        },
        "max_position_embeddings": {
         "default": 512,
         "description": "The maximum sequence length that this model might ever be used with. Typically set this to something large (e.g., 512 or 1024 or 2048).",
         "minimum": 1,
         "type": "integer"
        },
        "max_sequence_length": {
         "default": null,
         "description": "Maximum length of the input sequence.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_attention_heads": {
         "default": 12,
         "description": "Number of attention heads for each attention layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "num_hidden_groups": {
         "default": 1,
         "description": "Number of groups for the hidden layers, parameters in the same group are shared.",
         "minimum": 1,
         "type": "integer"
        },
        "num_hidden_layers": {
         "default": 12,
         "description": "Number of hidden layers in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "pad_token_id": {
         "default": 0,
         "description": "The ID of the token to use as padding.",
         "type": "integer"
        },
        "position_embedding_type": {
         "default": "absolute",
         "enum": [
          "absolute",
          "relative_key",
          "relative_key_query"
         ],
         "type": "string"
        },
        "pretrained_kwargs": {
         "default": null,
         "description": "Additional kwargs to pass to the pretrained model.",
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "albert-base-v2",
         "description": "Name or path of the pretrained model.",
         "type": "string"
        },
        "reduce_output": {
         "default": "cls_pooled",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "description": "Are the pretrained encoder weights saved in this model's checkpoint? Automatically set toTrue for trained models to prevent loading pretrained encoder weights from model hub.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "description": "Whether to finetune the model on your dataset.",
         "type": "boolean"
        },
        "type_vocab_size": {
         "default": 2,
         "description": "The vocabulary size of the token_type_ids passed when calling AlbertModel or TFAlbertModel.",
         "minimum": 1,
         "type": "integer"
        },
        "use_pretrained": {
         "default": true,
         "description": "Whether to use the pretrained weights for the model. If false, the model will train from scratch which is very computationally expensive.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary for the encoder",
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": 30000,
         "description": "Vocabulary size of the ALBERT model. Defines the number of different tokens that can be represented by the inputs_ids passed.",
         "minimum": 1,
         "type": "integer"
        }
       },
       "auto_transformer": {
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "max_sequence_length": {
         "default": null,
         "description": "Maximum length of the input sequence.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_kwargs": {
         "default": null,
         "description": "Additional kwargs to pass to the pretrained model.",
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": null,
         "description": "Name or path of the pretrained model.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "description": "Whether to finetune the model on your dataset.",
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "description": "Whether to use the pretrained weights for the model. Always True for AutoTransformers.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary for the encoder",
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": null,
         "description": "Vocabulary size of the AutoTransformer model. If None, the vocab size will be inferred from the given pretrained model",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        }
       },
       "bert": {
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "attention_probs_dropout_prob": {
         "default": 0.1,
         "description": "The dropout ratio for the attention probabilities.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "classifier_dropout": {
         "default": null,
         "description": "The dropout ratio for the classification head.",
         "maximum": 1,
         "minimum": 0,
         "nullable": true,
         "type": "number"
        },
        "gradient_checkpointing": {
         "default": false,
         "description": "Whether to use gradient checkpointing.",
         "type": "boolean"
        },
        "hidden_act": {
         "default": "gelu",
         "description": "The non-linear activation function (function or string) in the encoder and pooler.",
         "enum": [
          "gelu",
          "relu",
          "silu",
          "gelu_new"
         ],
         "type": null
        },
        "hidden_dropout_prob": {
         "default": 0.1,
         "description": "The dropout probability for all fully connected layers in the embeddings, encoder, and pooler.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "hidden_size": {
         "default": 768,
         "description": "Dimensionality of the encoder layers and the pooler layer.",
         "minimum": 1,
         "type": "integer"
        },
        "initializer_range": {
         "default": 0.02,
         "description": "The standard deviation of the truncated_normal_initializer for initializing all weight matrices.",
         "minimum": 0.0,
         "type": "number"
        },
        "intermediate_size": {
         "default": 3072,
         "description": "Dimensionality of the \u201cintermediate\u201d (often named feed-forward) layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "layer_norm_eps": {
         "default": 1e-12,
         "description": "The epsilon used by the layer normalization layers.",
         "minimum": 0.0,
         "type": "number"
        },
        "max_position_embeddings": {
         "default": 512,
         "description": "The maximum sequence length that this model might ever be used with. Typically set this to something large just in case (e.g., 512 or 1024 or 2048).",
         "minimum": 1,
         "type": "integer"
        },
        "max_sequence_length": {
         "default": null,
         "description": "Maximum length of the input sequence.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_attention_heads": {
         "default": 12,
         "description": "Number of attention heads for each attention layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "num_hidden_layers": {
         "default": 12,
         "description": "Number of hidden layers in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "pad_token_id": {
         "default": 0,
         "description": "The ID of the token to use as padding.",
         "type": "integer"
        },
        "position_embedding_type": {
         "default": "absolute",
         "description": "Type of position embedding.",
         "enum": [
          "absolute",
          "relative_key",
          "relative_key_query"
         ],
         "type": "string"
        },
        "pretrained_kwargs": {
         "default": null,
         "description": "Additional kwargs to pass to the pretrained model.",
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "bert-base-uncased",
         "description": "Name or path of the pretrained model.",
         "type": "string"
        },
        "reduce_output": {
         "default": "cls_pooled",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "description": "Are the pretrained encoder weights saved in this model's checkpoint? Automatically set toTrue for trained models to prevent loading pretrained encoder weights from model hub.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "description": "Whether to finetune the model on your dataset.",
         "type": "boolean"
        },
        "type_vocab_size": {
         "default": 2,
         "description": "The vocabulary size of the token_type_ids passed when calling BertModel or TFBertModel.",
         "minimum": 1,
         "type": "integer"
        },
        "use_pretrained": {
         "default": true,
         "description": "Whether to use the pretrained weights for the model. If false, the model will train from scratch which is very computationally expensive.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary for the encoder",
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": 30522,
         "description": "Vocabulary size of the BERT model. Defines the number of different tokens that can be represented by the inputs_ids passed when calling BertModel or TFBertModel.",
         "minimum": 1,
         "type": "integer"
        }
       },
       "camembert": {
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "attention_probs_dropout_prob": {
         "default": 0.1,
         "description": "The dropout ratio for the attention probabilities.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "classifier_dropout": {
         "default": null,
         "description": "The dropout ratio for the classification head.",
         "maximum": 1,
         "minimum": 0,
         "nullable": true,
         "type": "number"
        },
        "gradient_checkpointing": {
         "default": false,
         "description": "Whether to use gradient checkpointing.",
         "type": "boolean"
        },
        "hidden_act": {
         "default": "gelu",
         "description": "The non-linear activation function (function or string) in the encoder and pooler.",
         "enum": [
          "gelu",
          "relu",
          "silu",
          "gelu_new"
         ],
         "type": null
        },
        "hidden_dropout_prob": {
         "default": 0.1,
         "description": "The dropout probability for all fully connected layers in the embeddings, encoder, and pooler.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "hidden_size": {
         "default": 768,
         "description": "Dimensionality of the encoder layers and the pooler layer.",
         "minimum": 1,
         "type": "integer"
        },
        "initializer_range": {
         "default": 0.02,
         "description": "The standard deviation of the truncated_normal_initializer for initializing all weight matrices.",
         "minimum": 0.0,
         "type": "number"
        },
        "intermediate_size": {
         "default": 3072,
         "description": "Dimensionality of the \u201cintermediate\u201d (often named feed-forward) layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "layer_norm_eps": {
         "default": 1e-05,
         "description": "The epsilon used by the layer normalization layers.",
         "minimum": 0.0,
         "type": "number"
        },
        "max_position_embeddings": {
         "default": 514,
         "description": "The maximum sequence length that this model might ever be used with. Typically set this to something large just in case (e.g., 512 or 1024 or 2048).",
         "minimum": 1,
         "type": "integer"
        },
        "max_sequence_length": {
         "default": null,
         "description": "Maximum length of the input sequence.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_attention_heads": {
         "default": 12,
         "description": "Number of attention heads for each attention layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "num_hidden_layers": {
         "default": 12,
         "description": "Number of hidden layers in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "pad_token_id": {
         "default": 1,
         "description": "The ID of the token to use as padding.",
         "type": "integer"
        },
        "position_embedding_type": {
         "default": "absolute",
         "description": "Type of position embedding.",
         "enum": [
          "absolute",
          "relative_key",
          "relative_key_query"
         ],
         "type": "string"
        },
        "pretrained_kwargs": {
         "default": null,
         "description": "Additional kwargs to pass to the pretrained model.",
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "camembert-base",
         "description": "Name or path of the pretrained model.",
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "description": "Are the pretrained encoder weights saved in this model's checkpoint? Automatically set toTrue for trained models to prevent loading pretrained encoder weights from model hub.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "description": "Whether to finetune the model on your dataset.",
         "type": "boolean"
        },
        "type_vocab_size": {
         "default": 1,
         "description": "The vocabulary size of the token_type_ids passed when calling BertModel or TFBertModel.",
         "minimum": 1,
         "type": "integer"
        },
        "use_pretrained": {
         "default": true,
         "description": "Whether to use the pretrained weights for the model. If false, the model will train from scratch which is very computationally expensive.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary for the encoder",
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": 32005,
         "description": "Vocabulary size of the CamemBERT model.",
         "minimum": 1,
         "type": "integer"
        }
       },
       "cnnrnn": {
        "activation": {
         "default": "tanh",
         "description": "The default activation function to use.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "bidirectional": {
         "default": false,
         "description": "If true, two recurrent networks will perform encoding in the forward and backward direction and their outputs will be concatenated.",
         "type": "boolean"
        },
        "cell_type": {
         "default": "rnn",
         "description": "The type of recurrent cell to use. Available values are: `rnn`, `lstm`, `gru`. For reference about the differences between the cells please refer to [torch.nn Recurrent Layers](https://pytorch.org/docs/stable/nn.html#recurrent-layers).",
         "enum": [
          "rnn",
          "lstm",
          "gru"
         ],
         "type": "string"
        },
        "conv_activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each convolutional layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "conv_dropout": {
         "default": 0.0,
         "description": "The dropout rate for the convolutional layers",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "conv_layers": {
         "default": null,
         "description": "A list of dictionaries containing the parameters of all the convolutional layers. The length of the list determines the number of stacked convolutional layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `num_filters`, `filter_size`, `strides`, `padding`, `dilation_rate`, `use_bias`, `pool_function`, `pool_padding`, `pool_size`, `pool_strides`, `bias_initializer`, `weights_initializer`. If any of those values is missing from the dictionary, the default one specified as a parameter of the encoder will be used instead. If both `conv_layers` and `num_conv_layers` are `null`, a default list will be assigned to `conv_layers` with the value `[{filter_size: 7, pool_size: 3}, {filter_size: 7, pool_size: 3}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: 3}]`.",
         "type": null
        },
        "dilation_rate": {
         "default": 1,
         "description": "Dilation rate to use for dilated convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "filter_size": {
         "default": 5,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_conv_layers": {
         "default": null,
         "description": "The number of stacked convolutional layers when `conv_layers` is `null`.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of parallel fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "num_rec_layers": {
         "default": 1,
         "description": "The number of stacked recurrent layers.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pool_strides": {
         "default": null,
         "description": "Factor to scale down.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "recurrent_activation": {
         "default": "sigmoid",
         "description": "The activation function to use in the recurrent step",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "recurrent_dropout": {
         "default": 0.0,
         "description": "The dropout rate for the recurrent state",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "recurrent_initializer": {
         "default": "orthogonal",
         "description": "The initializer for recurrent matrix weights",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "reduce_output": {
         "default": "last",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "state_size": {
         "default": 256,
         "description": "The size of the state of the rnn.",
         "minimum": 1,
         "type": "integer"
        },
        "strides": {
         "default": 1,
         "description": "Stride length of the convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "unit_forget_bias": {
         "default": true,
         "description": "If true, add 1 to the bias of the forget gate at initialization",
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "deberta": {
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "attention_probs_dropout_prob": {
         "default": 0.1,
         "type": "number"
        },
        "hidden_act": {
         "default": "gelu",
         "type": "string"
        },
        "hidden_dropout_prob": {
         "default": 0.1,
         "type": "number"
        },
        "hidden_size": {
         "default": 1536,
         "type": "integer"
        },
        "initializer_range": {
         "default": 0.02,
         "type": "number"
        },
        "intermediate_size": {
         "default": 6144,
         "type": "integer"
        },
        "layer_norm_eps": {
         "default": 1e-12,
         "type": "number"
        },
        "max_position_embeddings": {
         "default": 512,
         "type": "integer"
        },
        "max_relative_positions": {
         "default": -1,
         "type": "integer"
        },
        "max_sequence_length": {
         "default": null,
         "nullable": true,
         "type": "integer"
        },
        "norm_rel_ebd": {
         "default": "layer_norm",
         "type": "string"
        },
        "num_attention_heads": {
         "default": 24,
         "type": "integer"
        },
        "num_hidden_layers": {
         "default": 24,
         "type": "integer"
        },
        "pad_token_id": {
         "default": 0,
         "type": "integer"
        },
        "pooler_dropout": {
         "default": 0,
         "type": "number"
        },
        "pooler_hidden_act": {
         "default": "gelu",
         "type": "string"
        },
        "pooler_hidden_size": {
         "default": 1536,
         "type": "integer"
        },
        "pos_att_type": {
         "description": "The type of relative position attention, it can be a combination of `['p2c', 'c2p']`, e.g. `['p2c']`, `['p2c', 'c2p']`, `['p2c', 'c2p']`.",
         "type": null
        },
        "position_biased_input": {
         "default": false,
         "type": "boolean"
        },
        "position_buckets": {
         "default": 256,
         "type": "integer"
        },
        "pretrained_kwargs": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "tasksource/deberta-base-long-nli",
         "description": "Name or path of the pretrained model.",
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "enum": [
          "cls_pooled",
          "last",
          "sum",
          "mean",
          "max",
          "concat",
          "attention",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "relative_attention": {
         "default": true,
         "type": "boolean"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "type": "boolean"
        },
        "share_att_key": {
         "default": true,
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "type": "boolean"
        },
        "type_vocab_size": {
         "default": 0,
         "type": "integer"
        },
        "use_pretrained": {
         "default": true,
         "type": "boolean"
        },
        "vocab_size": {
         "default": null,
         "nullable": true,
         "type": "integer"
        }
       },
       "distilbert": {
        "activation": {
         "default": "gelu",
         "description": "The non-linear activation function (function or string) in the encoder and pooler. If string, 'gelu', 'relu', 'silu' and 'gelu_new' are supported.",
         "enum": [
          "gelu",
          "relu",
          "silu",
          "gelu_new"
         ],
         "type": null
        },
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "attention_dropout": {
         "default": 0.1,
         "description": "The dropout ratio for the attention probabilities.",
         "minimum": 0.0,
         "type": "number"
        },
        "dim": {
         "default": 768,
         "description": "Dimensionality of the encoder layers and the pooler layer.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.1,
         "description": "The dropout probability for all fully connected layers in the embeddings, encoder, and pooler.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "hidden_dim": {
         "default": 3072,
         "description": "The size of the \u201cintermediate\u201d (often named feed-forward) layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "initializer_range": {
         "default": 0.02,
         "description": "The standard deviation of the truncated_normal_initializer for initializing all weight matrices.",
         "minimum": 0.0,
         "type": "number"
        },
        "max_position_embeddings": {
         "default": 512,
         "description": "The maximum sequence length that this model might ever be used with. Typically set this to something large just in case (e.g., 512 or 1024 or 2048).",
         "minimum": 1,
         "type": "integer"
        },
        "max_sequence_length": {
         "default": null,
         "description": "Maximum length of the input sequence.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "n_heads": {
         "default": 12,
         "description": "Number of hidden layers in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "n_layers": {
         "default": 6,
         "description": "Number of hidden layers in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_kwargs": {
         "default": null,
         "description": "Additional kwargs to pass to the pretrained model.",
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "distilbert-base-uncased",
         "description": "Name or path of the pretrained model.",
         "type": "string"
        },
        "qa_dropout": {
         "default": 0.1,
         "description": "The dropout probabilities used in the question answering model DistilBertForQuestionAnswering.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "reduce_output": {
         "default": "sum",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "description": "Are the pretrained encoder weights saved in this model's checkpoint? Automatically set toTrue for trained models to prevent loading pretrained encoder weights from model hub.",
         "type": "boolean"
        },
        "seq_classif_dropout": {
         "default": 0.2,
         "description": "The dropout probabilities used in the sequence classification and the multiple choice model DistilBertForSequenceClassification.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "sinusoidal_pos_embds": {
         "default": false,
         "description": "Whether to use sinusoidal positional embeddings.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "description": "Whether to finetune the model on your dataset.",
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "description": "Whether to use the pretrained weights for the model. If false, the model will train from scratch which is very computationally expensive.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary for the encoder",
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": 30522,
         "description": "Vocabulary size of the DistilBERT model. Defines the number of different tokens that can be represented by the inputs_ids passed when calling DistilBertModel or TFDistilBertModel.",
         "minimum": 1,
         "type": "integer"
        }
       },
       "electra": {
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "attention_probs_dropout_prob": {
         "default": 0.1,
         "description": "The dropout ratio for the attention probabilities.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "classifier_dropout": {
         "default": null,
         "description": "The dropout ratio for the classification head.",
         "maximum": 1,
         "minimum": 0,
         "nullable": true,
         "type": "number"
        },
        "embedding_size": {
         "default": 128,
         "description": "Dimensionality of the encoder layers and the pooler layer.",
         "minimum": 1,
         "type": "integer"
        },
        "hidden_act": {
         "default": "gelu",
         "description": "The non-linear activation function (function or string) in the encoder and pooler.",
         "enum": [
          "gelu",
          "relu",
          "silu",
          "gelu_new"
         ],
         "type": null
        },
        "hidden_dropout_prob": {
         "default": 0.1,
         "description": "The dropout probability for all fully connected layers in the embeddings, encoder, and pooler.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "hidden_size": {
         "default": 256,
         "description": "Dimensionality of the encoder layers and the pooler layer.",
         "minimum": 1,
         "type": "integer"
        },
        "initializer_range": {
         "default": 0.02,
         "description": "The standard deviation of the truncated_normal_initializer for initializing all weight matrices.",
         "minimum": 0.0,
         "type": "number"
        },
        "intermediate_size": {
         "default": 1024,
         "description": "Dimensionality of the \u201cintermediate\u201d (i.e., feed-forward) layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "layer_norm_eps": {
         "default": 1e-12,
         "description": "The epsilon used by the layer normalization layers.",
         "minimum": 0.0,
         "type": "number"
        },
        "max_position_embeddings": {
         "default": 512,
         "description": "The maximum sequence length that this model might ever be used with. Typically set this to something large just in case (e.g., 512 or 1024 or 2048).",
         "minimum": 1,
         "type": "integer"
        },
        "max_sequence_length": {
         "default": null,
         "description": "Maximum length of the input sequence.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_attention_heads": {
         "default": 4,
         "description": "Number of attention heads for each attention layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "num_hidden_layers": {
         "default": 12,
         "description": "Number of hidden layers in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "position_embedding_type": {
         "default": "absolute",
         "description": "Type of position embedding.",
         "enum": [
          "absolute",
          "relative_key",
          "relative_key_query"
         ],
         "type": "string"
        },
        "pretrained_kwargs": {
         "default": null,
         "description": "Additional kwargs to pass to the pretrained model.",
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "google/electra-small-discriminator",
         "description": "Name or path of the pretrained model.",
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "description": "Are the pretrained encoder weights saved in this model's checkpoint? Automatically set toTrue for trained models to prevent loading pretrained encoder weights from model hub.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "description": "Whether to finetune the model on your dataset.",
         "type": "boolean"
        },
        "type_vocab_size": {
         "default": 2,
         "description": "The vocabulary size of the token_type_ids passed when calling ElectraModel or TFElectraModel.",
         "minimum": 1,
         "type": "integer"
        },
        "use_pretrained": {
         "default": true,
         "description": "Whether to use the pretrained weights for the model. If false, the model will train from scratch which is very computationally expensive.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary for the encoder",
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": 30522,
         "description": "Vocabulary size of the ELECTRA model. Defines the number of different tokens that can be represented by the inputs_ids passed when calling ElectraModel or TFElectraModel.",
         "minimum": 1,
         "type": "integer"
        }
       },
       "embed": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate applied to the embedding. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "gpt": {
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "afn": {
         "default": "gelu",
         "description": "The non-linear activation function (function or string) in the encoder and pooler.",
         "enum": [
          "gelu",
          "relu",
          "silu"
         ],
         "type": "string"
        },
        "attn_pdrop": {
         "default": 0.1,
         "description": "The dropout ratio for the attention.",
         "type": "number"
        },
        "embd_pdrop": {
         "default": 0.1,
         "description": "The dropout ratio for the embeddings.",
         "type": "number"
        },
        "initializer_range": {
         "default": 0.02,
         "description": "The standard deviation of the truncated_normal_initializer for initializing all weight matrices.",
         "minimum": 0.0,
         "type": "number"
        },
        "layer_norm_epsilon": {
         "default": 1e-05,
         "description": "The epsilon to use in the layer normalization layers",
         "minimum": 0.0,
         "type": "number"
        },
        "max_sequence_length": {
         "default": null,
         "description": "Maximum length of the input sequence.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "n_ctx": {
         "default": 512,
         "description": "Dimensionality of the causal mask (usually same as n_positions)",
         "minimum": 1,
         "type": "integer"
        },
        "n_embd": {
         "default": 768,
         "description": "Dimensionality of the embeddings and hidden states.",
         "minimum": 1,
         "type": "integer"
        },
        "n_head": {
         "default": 12,
         "description": "Number of attention heads for each attention layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "n_layer": {
         "default": 12,
         "description": "Number of hidden layers in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "n_positions": {
         "default": 40478,
         "description": "The maximum sequence length that this model might ever be used with. Typically set this to something large just in case (e.g., 512 or 1024 or 2048).",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_kwargs": {
         "default": null,
         "description": "Additional kwargs to pass to the pretrained model.",
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "openai-gpt",
         "description": "Name or path of the pretrained model.",
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "type": "string"
        },
        "resid_pdrop": {
         "default": 0.1,
         "description": "The dropout probability for all fully connected layers in the embeddings, encoder, and pooler.",
         "type": "number"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "description": "Are the pretrained encoder weights saved in this model's checkpoint? Automatically set toTrue for trained models to prevent loading pretrained encoder weights from model hub.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "description": "Whether to finetune the model on your dataset.",
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "description": "Whether to use the pretrained weights for the model. If false, the model will train from scratch which is very computationally expensive.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary for the encoder",
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": 30522,
         "description": "Vocabulary size of the GPT model. Defines the number of different tokens that can be represented by the inputs_ids passed when calling OpenAIGPTModel or TFOpenAIGPTModel.",
         "minimum": 1,
         "type": "integer"
        }
       },
       "gpt2": {
        "activation_function": {
         "default": "gelu_new",
         "description": "Activation function, to be selected in the list ['relu', 'silu', 'gelu', 'tanh', 'gelu_new'].",
         "enum": [
          "relu",
          "silu",
          "gelu",
          "tanh",
          "gelu_new"
         ],
         "type": "string"
        },
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "attn_pdrop": {
         "default": 0.1,
         "description": "The dropout ratio for the attention.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embd_pdrop": {
         "default": 0.1,
         "description": "The dropout ratio for the embeddings.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "initializer_range": {
         "default": 0.02,
         "description": "The standard deviation of the truncated_normal_initializer for initializing all weight matrices.",
         "minimum": 0.0,
         "type": "number"
        },
        "layer_norm_epsilon": {
         "default": 1e-05,
         "description": "The epsilon to use in the layer normalization layers.",
         "minimum": 0.0,
         "type": "number"
        },
        "max_sequence_length": {
         "default": null,
         "description": "Maximum length of the input sequence.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "n_ctx": {
         "default": 1024,
         "description": "Dimensionality of the causal mask (usually same as n_positions)",
         "minimum": 1,
         "type": "integer"
        },
        "n_embd": {
         "default": 768,
         "description": "Dimensionality of the embeddings and hidden states.",
         "minimum": 1,
         "type": "integer"
        },
        "n_head": {
         "default": 12,
         "description": "Number of attention heads for each attention layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "n_inner": {
         "default": null,
         "description": "Dimensionality of the inner feed-forward layers. None will set it to 4 times n_embd",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "n_layer": {
         "default": 12,
         "description": "Number of hidden layers in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "n_positions": {
         "default": 1024,
         "description": "The maximum sequence length that this model might ever be used with. Typically set this to something large just in case (e.g., 512 or 1024 or 2048).",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_kwargs": {
         "default": null,
         "description": "Additional kwargs to pass to the pretrained model.",
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "gpt2",
         "description": "Name or path of the pretrained model.",
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "type": "string"
        },
        "resid_pdrop": {
         "default": 0.1,
         "description": "The dropout probability for all fully connected layers in the embeddings, encoder, and pooler.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "scale_attn_weights": {
         "default": true,
         "description": "Scale attention weights by dividing by sqrt(hidden_size).",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "description": "Whether to finetune the model on your dataset.",
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "description": "Whether to use the pretrained weights for the model. If false, the model will train from scratch which is very computationally expensive.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary for the encoder",
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": 50257,
         "description": "Vocabulary size of the GPT-2 model. Defines the number of different tokens that can be represented by the inputs_ids passed when calling GPT2Model or TFGPT2Model.",
         "minimum": 1,
         "type": "integer"
        }
       },
       "jamba": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "attention_every_k": {
         "default": 4,
         "description": "Every `attention_every_k`-th block is attention, the remainder are SSM. Default 4 gives a 1:3 attention:SSM ratio matching the Jamba paper.",
         "minimum": 1,
         "type": null
        },
        "d_conv": {
         "default": 4,
         "description": "Width of the depthwise 1D convolution inside each SSM block.",
         "minimum": 1,
         "type": null
        },
        "d_model": {
         "default": 256,
         "description": "Hidden width of every block \u2014 SSM and attention share the same d_model.",
         "minimum": 1,
         "type": null
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": null
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": null
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": null
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": null
        },
        "expand_factor": {
         "default": 2,
         "description": "Inner expansion factor inside each SSM block.",
         "minimum": 1,
         "type": null
        },
        "ffn_size": {
         "default": 1024,
         "description": "Feed-forward width inside each attention block.",
         "minimum": 1,
         "type": null
        },
        "n_layers": {
         "default": 8,
         "description": "Total number of stacked blocks (SSM + attention combined).",
         "minimum": 1,
         "type": null
        },
        "num_heads": {
         "default": 8,
         "description": "Number of attention heads (and SSD heads, shared).",
         "minimum": 1,
         "type": null
        },
        "output_size": {
         "default": 256,
         "description": "Output feature width emitted by the encoder.",
         "minimum": 1,
         "type": null
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "type": null
        },
        "reduce_output": {
         "default": "mean",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "type": null
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": null
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings.",
         "type": null
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "llm": {
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "base_model": {
         "default": null,
         "description": "Base pretrained model to use. This can be one of the presets defined by Ludwig, a fully qualified name of a pretrained model from the HuggingFace Hub, or a path to a directory containing a pretrained model.",
         "type": null
        },
        "max_sequence_length": {
         "default": null,
         "nullable": true,
         "type": "integer"
        },
        "model_parameters": {
         "default": null,
         "description": "Configurable model parameters for LLMs.",
         "type": null
        },
        "quantization": {
         "default": null,
         "description": "Set quantization options.",
         "type": null
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "longformer": {
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "attention_window": {
         "default": 512,
         "description": "Size of an attention window around each token. If an int, use the same size for all layers. To specify a different window size for each layer, use a List[int] where len(attention_window) == num_hidden_layers.",
         "type": null
        },
        "max_position_embeddings": {
         "default": 4098,
         "description": "The maximum sequence length that this model might ever be used with. Typically set this to something large just in case (e.g., 512 or 1024 or 2048).",
         "minimum": 1,
         "type": "integer"
        },
        "max_sequence_length": {
         "default": null,
         "description": "Maximum length of the input sequence.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_kwargs": {
         "default": null,
         "description": "Additional kwargs to pass to the pretrained model.",
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "allenai/longformer-base-4096",
         "description": "Name or path of the pretrained model.",
         "type": "string"
        },
        "reduce_output": {
         "default": "cls_pooled",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "description": "Are the pretrained encoder weights saved in this model's checkpoint? Automatically set toTrue for trained models to prevent loading pretrained encoder weights from model hub.",
         "type": "boolean"
        },
        "sep_token_id": {
         "default": 2,
         "description": "ID of the separator token, which is used when building a sequence from multiple sequences",
         "type": "integer"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "description": "Whether to finetune the model on your dataset.",
         "type": "boolean"
        },
        "type_vocab_size": {
         "default": 1,
         "description": "The vocabulary size of the token_type_ids passed when calling LongformerEncoder",
         "minimum": 1,
         "type": "integer"
        },
        "use_pretrained": {
         "default": true,
         "description": "Whether to use the pretrained weights for the model. If false, the model will train from scratch which is very computationally expensive.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary for the encoder",
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": 50265,
         "description": "Vocabulary size of the Longformer model.",
         "minimum": 1,
         "type": "integer"
        }
       },
       "mamba": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "d_conv": {
         "default": 4,
         "description": "Width of the 1D convolution in each SSM layer.",
         "minimum": 1,
         "type": "integer"
        },
        "d_model": {
         "default": 256,
         "description": "Dimensionality of the model (hidden size for SSM layers).",
         "minimum": 1,
         "type": "integer"
        },
        "d_state": {
         "default": 16,
         "description": "Dimensionality of the state space in each SSM layer.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "expand_factor": {
         "default": 2,
         "description": "Expansion factor for the inner dimension of SSM layers.",
         "minimum": 1,
         "type": "integer"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "n_layers": {
         "default": 4,
         "description": "Number of stacked SSM layers.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each FC layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "mean",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use bias in linear projections.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "mamba2": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "d_conv": {
         "default": 4,
         "description": "Width of the depthwise 1D convolution inside each block.",
         "minimum": 1,
         "type": null
        },
        "d_model": {
         "default": 256,
         "description": "Hidden width of each Mamba-2 block.",
         "minimum": 1,
         "type": null
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": null
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": null
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": null
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": null
        },
        "expand_factor": {
         "default": 2,
         "description": "Inner expansion factor for each block.",
         "minimum": 1,
         "type": null
        },
        "n_layers": {
         "default": 4,
         "description": "Number of stacked Mamba-2 blocks.",
         "minimum": 1,
         "type": null
        },
        "num_heads": {
         "default": 8,
         "description": "Number of SSD heads. `d_model * expand_factor` must be divisible by `num_heads`.",
         "minimum": 1,
         "type": null
        },
        "output_size": {
         "default": 256,
         "description": "Output feature width emitted by the encoder.",
         "minimum": 1,
         "type": null
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "type": null
        },
        "reduce_output": {
         "default": "mean",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "type": null
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": null
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings.",
         "type": null
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "modernbert": {
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "hidden_act": {
         "default": "gelu",
         "description": "The non-linear activation function (function or string) in the encoder and pooler.",
         "enum": [
          "gelu",
          "relu",
          "silu",
          "gelu_new"
         ],
         "type": null
        },
        "hidden_dropout_prob": {
         "default": 0.0,
         "description": "The dropout probability for all fully connected layers in the embeddings, encoder, and pooler.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "hidden_size": {
         "default": 768,
         "description": "Dimensionality of the encoder layers and the pooler layer.",
         "minimum": 1,
         "type": "integer"
        },
        "initializer_range": {
         "default": 0.02,
         "description": "The standard deviation of the truncated_normal_initializer for initializing all weight matrices.",
         "minimum": 0.0,
         "type": "number"
        },
        "intermediate_size": {
         "default": 1152,
         "description": "Dimensionality of the 'intermediate' (often named feed-forward) layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "layer_norm_eps": {
         "default": 1e-05,
         "description": "The epsilon used by the layer normalization layers.",
         "minimum": 0.0,
         "type": "number"
        },
        "max_position_embeddings": {
         "default": 8192,
         "description": "The maximum sequence length that this model might ever be used with.",
         "minimum": 1,
         "type": "integer"
        },
        "max_sequence_length": {
         "default": null,
         "description": "Maximum length of the input sequence.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_attention_heads": {
         "default": 12,
         "description": "Number of attention heads for each attention layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "num_hidden_layers": {
         "default": 22,
         "description": "Number of hidden layers in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "pad_token_id": {
         "default": 50283,
         "description": "The ID of the token to use as padding.",
         "type": "integer"
        },
        "pretrained_kwargs": {
         "default": null,
         "description": "Additional kwargs to pass to the pretrained model.",
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "answerdotai/ModernBERT-base",
         "description": "Name or path of the pretrained model.",
         "type": "string"
        },
        "reduce_output": {
         "default": "cls_pooled",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "description": "Are the pretrained encoder weights saved in this model's checkpoint? Automatically set toTrue for trained models to prevent loading pretrained encoder weights from model hub.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "description": "Whether to finetune the model on your dataset.",
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "description": "Whether to use the pretrained weights for the model. If false, the model will train from scratch which is very computationally expensive.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary for the encoder",
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": 50368,
         "description": "Vocabulary size of the ModernBERT model.",
         "minimum": 1,
         "type": "integer"
        }
       },
       "parallel_cnn": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "conv_layers": {
         "default": null,
         "description": "A list of dictionaries containing the parameters of all the convolutional layers. The length of the list determines the number of stacked convolutional layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `num_filters`, `filter_size`, `strides`, `padding`, `dilation_rate`, `use_bias`, `pool_function`, `pool_padding`, `pool_size`, `pool_strides`, `bias_initializer`, `weights_initializer`. If any of those values is missing from the dictionary, the default one specified as a parameter of the encoder will be used instead. If both `conv_layers` and `num_conv_layers` are `null`, a default list will be assigned to `conv_layers` with the value `[{filter_size: 7, pool_size: 3}, {filter_size: 7, pool_size: 3}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: 3}]`.",
         "type": null
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate applied to the embedding. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "filter_size": {
         "default": 3,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_conv_layers": {
         "default": null,
         "description": "The number of stacked convolutional layers when `conv_layers` is `null`.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": null,
         "description": "Number of parallel fully connected layers to use.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "Whether to embed the input sequence.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "rnn": {
        "activation": {
         "default": "tanh",
         "description": "The default activation function.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "bidirectional": {
         "default": false,
         "description": "If true, two recurrent networks will perform encoding in the forward and backward direction and their outputs will be concatenated.",
         "type": "boolean"
        },
        "cell_type": {
         "default": "rnn",
         "description": "The type of recurrent cell to use. Available values are: `rnn`, `lstm`, `gru`. For reference about the differences between the cells please refer to [torch.nn Recurrent Layers](https://pytorch.org/docs/stable/nn.html#recurrent-layers).",
         "enum": [
          "rnn",
          "lstm",
          "gru"
         ],
         "type": "string"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of parallel fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "num_layers": {
         "default": 1,
         "description": "The number of stacked recurrent layers.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "recurrent_activation": {
         "default": "sigmoid",
         "description": "The activation function to use in the recurrent step",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "recurrent_dropout": {
         "default": 0.0,
         "description": "The dropout rate for the recurrent state",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "recurrent_initializer": {
         "default": "orthogonal",
         "description": "The initializer for recurrent matrix weights",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "reduce_output": {
         "default": "last",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "state_size": {
         "default": 256,
         "description": "The size of the state of the rnn.",
         "minimum": 1,
         "type": "integer"
        },
        "unit_forget_bias": {
         "default": true,
         "description": "If true, add 1 to the bias of the forget gate at initialization",
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "roberta": {
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "bos_token_id": {
         "default": 0,
         "description": "The beginning of sequence token ID.",
         "type": "integer"
        },
        "eos_token_id": {
         "default": 2,
         "description": "The end of sequence token ID.",
         "type": "integer"
        },
        "max_sequence_length": {
         "default": null,
         "description": "Maximum length of the input sequence.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pad_token_id": {
         "default": 1,
         "description": "The ID of the token to use as padding.",
         "type": "integer"
        },
        "pretrained_kwargs": {
         "default": null,
         "description": "Additional kwargs to pass to the pretrained model.",
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "roberta-base",
         "description": "Name or path of the pretrained model.",
         "type": "string"
        },
        "reduce_output": {
         "default": "cls_pooled",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "description": "Are the pretrained encoder weights saved in this model's checkpoint? Automatically set toTrue for trained models to prevent loading pretrained encoder weights from model hub.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "description": "Whether to finetune the model on your dataset.",
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "description": "Whether to use the pretrained weights for the model. If false, the model will train from scratch which is very computationally expensive.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary for the encoder",
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": null,
         "description": "Vocabulary size of the RoBERTa model.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        }
       },
       "stacked_cnn": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "conv_layers": {
         "default": null,
         "description": "A list of dictionaries containing the parameters of all the convolutional layers. The length of the list determines the number of stacked convolutional layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `num_filters`, `filter_size`, `strides`, `padding`, `dilation_rate`, `use_bias`, `pool_function`, `pool_padding`, `pool_size`, `pool_strides`, `bias_initializer`, `weights_initializer`. If any of those values is missing from the dictionary, the default one specified as a parameter of the encoder will be used instead. If both `conv_layers` and `num_conv_layers` are `null`, a default list will be assigned to `conv_layers` with the value `[{filter_size: 7, pool_size: 3}, {filter_size: 7, pool_size: 3}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: 3}]`.",
         "type": null
        },
        "dilation_rate": {
         "default": 1,
         "description": "Dilation rate to use for dilated convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate applied to the embedding. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "filter_size": {
         "default": 3,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_conv_layers": {
         "default": null,
         "description": "The number of stacked convolutional layers when `conv_layers` is `null`.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": null,
         "description": "Number of parallel fully connected layers to use.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pool_strides": {
         "default": null,
         "description": "Factor to scale down.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "Whether to embed the input sequence.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "strides": {
         "default": 1,
         "description": "Stride length of the convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "stacked_parallel_cnn": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate applied to the embedding. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "filter_size": {
         "default": 3,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": null,
         "description": "Number of parallel fully connected layers to use.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "num_stacked_layers": {
         "default": null,
         "description": "If stacked_layers is null, this is the number of elements in the stack of parallel convolutional layers.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "stacked_layers": {
         "default": null,
         "description": "a nested list of lists of dictionaries containing the parameters of the stack of parallel convolutional layers. The length of the list determines the number of stacked parallel convolutional layers, length of the sub-lists determines the number of parallel conv layers and the content of each dictionary determines the parameters for a specific layer.",
         "type": null
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "t5": {
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "d_ff": {
         "default": 2048,
         "description": "Size of the intermediate feed forward layer in each T5Block.",
         "minimum": 1,
         "type": "integer"
        },
        "d_kv": {
         "default": 64,
         "description": "Size of the key, query, value projections per attention head. d_kv has to be equal to d_model // num_heads.",
         "minimum": 1,
         "type": "integer"
        },
        "d_model": {
         "default": 512,
         "description": "Size of the encoder layers and the pooler layer.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout_rate": {
         "default": 0.1,
         "description": "The ratio for all dropout layers.",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "feed_forward_proj": {
         "default": "relu",
         "description": "Type of feed forward layer to be used. Should be one of 'relu' or 'gated-gelu'. T5v1.1 uses the 'gated-gelu' feed forward projection. Original T5 uses 'relu'.",
         "enum": [
          "relu",
          "gated-gelu"
         ],
         "type": "string"
        },
        "initializer_factor": {
         "default": 1,
         "description": "A factor for initializing all weight matrices (should be kept to 1, used internally for initialization testing).",
         "minimum": 0.0,
         "type": "number"
        },
        "layer_norm_eps": {
         "default": 1e-06,
         "description": "The epsilon used by the layer normalization layers.",
         "minimum": 0.0,
         "type": "number"
        },
        "max_sequence_length": {
         "default": null,
         "description": "Maximum length of the input sequence.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_decoder_layers": {
         "default": 6,
         "description": "Number of hidden layers in the Transformer decoder. Will use the same value as num_layers if not set.",
         "minimum": 1,
         "type": "integer"
        },
        "num_heads": {
         "default": 8,
         "description": "Number of attention heads for each attention layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "num_layers": {
         "default": 6,
         "description": "Number of hidden layers in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_kwargs": {
         "default": null,
         "description": "Additional kwargs to pass to the pretrained model.",
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "t5-small",
         "description": "Name or path of the pretrained model.",
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "type": "string"
        },
        "relative_attention_num_buckets": {
         "default": 32,
         "description": "The number of buckets to use for each attention layer.",
         "minimum": 1,
         "type": "integer"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "description": "Are the pretrained encoder weights saved in this model's checkpoint? Automatically set toTrue for trained models to prevent loading pretrained encoder weights from model hub.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "description": "Whether to finetune the model on your dataset.",
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "description": "Whether to use the pretrained weights for the model. If false, the model will train from scratch which is very computationally expensive.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary for the encoder",
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": 32128,
         "description": "Vocabulary size of the T5 model. Defines the number of different tokens that can be represented by the inputs_ids passed when calling T5Model or TFT5Model.",
         "minimum": 1,
         "type": "integer"
        }
       },
       "tf_idf": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "max_df": {
         "default": 1.0,
         "description": "Maximum document frequency threshold for pruning. Terms that appear in more than this fraction of documents are ignored. Set to a value < 1.0 to remove corpus-specific stop words. Applied during preprocessing.",
         "maximum": 1.0,
         "minimum": 0.0,
         "type": "number"
        },
        "max_sequence_length": {
         "default": null,
         "nullable": true,
         "type": "integer"
        },
        "min_df": {
         "default": 1,
         "description": "Minimum document frequency threshold for pruning. Terms that appear in fewer than this many documents are ignored. Useful for removing very rare terms. Applied during preprocessing.",
         "type": "integer"
        },
        "ngram_range": {
         "description": "The range of n-gram sizes to use for tokenization, as a (min_n, max_n) tuple. For example, (1, 1) means only unigrams, (1, 2) means unigrams and bigrams, (1, 3) means unigrams, bigrams, and trigrams. Applied during preprocessing.",
         "type": "array"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "str2idf": {
         "default": null,
         "type": null
        },
        "vocab": {
         "default": null,
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": null,
         "nullable": true,
         "type": "integer"
        }
       },
       "transformer": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "dropout": {
         "default": 0.1,
         "description": "The dropout rate for the transformer block. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "hidden_size": {
         "default": 256,
         "description": "The size of the hidden representation within the transformer block. It is usually the same as the embedding_size, but if the two values are different, a projection layer will be added before the first transformer block.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of parallel fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "num_heads": {
         "default": 8,
         "description": "Number of attention heads in each transformer block.",
         "minimum": 1,
         "type": "integer"
        },
        "num_layers": {
         "default": 1,
         "description": "The number of transformer layers.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "last",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "transformer_output_size": {
         "default": 256,
         "description": "Size of the fully connected layer after self attention in the transformer block. This is usually the same as hidden_size and embedding_size.",
         "minimum": 1,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "use_rope": {
         "default": false,
         "description": "If True, use Rotary Position Embeddings (RoPE) instead of absolute positional embeddings. RoPE encodes position by rotating query and key vectors, providing better length generalization and relative position awareness. Used by modern LLMs like LLaMA and Mistral.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "xlmroberta": {
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "add_pooling_layer": {
         "default": true,
         "description": "Whether to add a pooling layer to the encoder.",
         "type": "boolean"
        },
        "bos_token_id": {
         "default": 0,
         "description": "The beginning of sequence token ID.",
         "type": "integer"
        },
        "eos_token_id": {
         "default": 2,
         "description": "The end of sequence token ID.",
         "type": "integer"
        },
        "max_position_embeddings": {
         "default": 514,
         "description": "The maximum sequence length that this model might ever be used with. Typically set this to something large just in case (e.g., 512 or 1024 or 2048).",
         "minimum": 1,
         "type": "integer"
        },
        "max_sequence_length": {
         "default": null,
         "description": "Maximum length of the input sequence.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pad_token_id": {
         "default": 1,
         "description": "The ID of the token to use as padding.",
         "type": "integer"
        },
        "pretrained_kwargs": {
         "default": null,
         "description": "Additional kwargs to pass to the pretrained model.",
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "xlm-roberta-base",
         "description": "Name or path of the pretrained model.",
         "type": "string"
        },
        "reduce_output": {
         "default": "cls_pooled",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "type": "string"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "description": "Are the pretrained encoder weights saved in this model's checkpoint? Automatically set toTrue for trained models to prevent loading pretrained encoder weights from model hub.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "description": "Whether to finetune the model on your dataset.",
         "type": "boolean"
        },
        "type_vocab_size": {
         "default": 1,
         "description": "The vocabulary size of the token_type_ids passed in.",
         "minimum": 1,
         "type": "integer"
        },
        "use_pretrained": {
         "default": true,
         "description": "Whether to use the pretrained weights for the model. If false, the model will train from scratch which is very computationally expensive.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary for the encoder",
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": null,
         "description": "Vocabulary size of the XLMRoBERTa model.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        }
       },
       "xlnet": {
        "adapter": {
         "default": null,
         "description": "Whether to use parameter-efficient fine-tuning",
         "type": null
        },
        "attn_type": {
         "default": "bi",
         "description": "The attention type used by the model. Currently only 'bi' is supported.",
         "enum": [
          "bi"
         ],
         "type": "string"
        },
        "bi_data": {
         "default": false,
         "description": "Whether or not to use bidirectional input pipeline. Usually set to True during pretraining and False during finetuning.",
         "type": "boolean"
        },
        "bos_token_id": {
         "default": 1,
         "description": "The beginning of sequence token ID.",
         "type": "integer"
        },
        "clamp_len": {
         "default": -1,
         "description": "Clamp all relative distances larger than clamp_len. Setting this attribute to -1 means no clamping.",
         "type": "integer"
        },
        "d_inner": {
         "default": 3072,
         "description": "Dimensionality of the \u201cintermediate\u201d (often named feed-forward) layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "d_model": {
         "default": 768,
         "description": "Dimensionality of the encoder layers and the pooler layer.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.1,
         "description": "The dropout probability for all fully connected layers in the embeddings, encoder, and pooler.",
         "type": "number"
        },
        "end_n_top": {
         "default": 5,
         "description": "Used in the SQuAD evaluation script.",
         "minimum": 1,
         "type": "integer"
        },
        "eos_token_id": {
         "default": 2,
         "description": "The end of sequence token ID.",
         "type": "integer"
        },
        "ff_activation": {
         "default": "gelu",
         "description": "The non-linear activation function (function or string) in the encoder and pooler. If string, 'gelu', 'relu', 'silu' and 'gelu_new' are supported.",
         "enum": [
          "gelu",
          "relu",
          "silu",
          "gelu_new"
         ],
         "type": "string"
        },
        "initializer_range": {
         "default": 0.02,
         "description": "The standard deviation of the truncated_normal_initializer for initializing all weight matrices.",
         "minimum": 0.0,
         "type": "number"
        },
        "layer_norm_eps": {
         "default": 1e-12,
         "description": "The epsilon used by the layer normalization layers.",
         "minimum": 0.0,
         "type": "number"
        },
        "max_sequence_length": {
         "default": null,
         "description": "Maximum length of the input sequence.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "mem_len": {
         "default": null,
         "description": "The number of tokens to cache. The key/value pairs that have already been pre-computed in a previous forward pass won\u2019t be re-computed.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "n_head": {
         "default": 12,
         "description": "Number of attention heads for each attention layer in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "n_layer": {
         "default": 12,
         "description": "Number of hidden layers in the Transformer encoder.",
         "minimum": 1,
         "type": "integer"
        },
        "pad_token_id": {
         "default": 5,
         "description": "The ID of the token to use as padding.",
         "type": "integer"
        },
        "pretrained_kwargs": {
         "default": null,
         "description": "Additional kwargs to pass to the pretrained model.",
         "nullable": true,
         "type": "object"
        },
        "pretrained_model_name_or_path": {
         "default": "xlnet-base-cased",
         "description": "Name or path of the pretrained model.",
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "The method used to reduce a sequence of tensors down to a single tensor.",
         "type": "string"
        },
        "reuse_len": {
         "default": null,
         "description": "The number of tokens in the current batch to be cached and reused in the future.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "same_length": {
         "default": false,
         "description": "Whether or not to use the same attention length for each token.",
         "type": "boolean"
        },
        "saved_weights_in_checkpoint": {
         "default": false,
         "description": "Are the pretrained encoder weights saved in this model's checkpoint? Automatically set toTrue for trained models to prevent loading pretrained encoder weights from model hub.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "start_n_top": {
         "default": 5,
         "description": "Used in the SQuAD evaluation script.",
         "minimum": 1,
         "type": "integer"
        },
        "summary_activation": {
         "default": "tanh",
         "description": "Argument used when doing sequence summary. Used in the sequence classification and multiple choice models.",
         "type": "string"
        },
        "summary_last_dropout": {
         "default": 0.1,
         "description": "Used in the sequence classification and multiple choice models.",
         "type": "number"
        },
        "summary_type": {
         "default": "last",
         "description": "Argument used when doing sequence summary. Used in the sequence classification and multiple choice models.",
         "enum": [
          "last",
          "first",
          "mean",
          "cls_index",
          "attn"
         ],
         "type": "string"
        },
        "summary_use_proj": {
         "default": true,
         "type": "boolean"
        },
        "trainable": {
         "default": false,
         "description": "Whether to finetune the model on your dataset.",
         "type": "boolean"
        },
        "untie_r": {
         "default": true,
         "description": "Whether or not to untie relative position biases",
         "type": "boolean"
        },
        "use_mems_eval": {
         "default": true,
         "description": "Whether or not the model should make use of the recurrent memory mechanism in evaluation mode.",
         "type": "boolean"
        },
        "use_mems_train": {
         "default": false,
         "description": "Whether or not the model should make use of the recurrent memory mechanism in train mode.",
         "type": "boolean"
        },
        "use_pretrained": {
         "default": true,
         "description": "Whether to use the pretrained weights for the model. If false, the model will train from scratch which is very computationally expensive.",
         "type": "boolean"
        },
        "vocab": {
         "default": null,
         "description": "Vocabulary for the encoder",
         "nullable": true,
         "type": "array"
        },
        "vocab_size": {
         "default": 32000,
         "description": "Vocabulary size of the XLNet model. Defines the number of different tokens that can be represented by the inputs_ids passed when calling XLNetModel or TFXLNetModel.",
         "minimum": 1,
         "type": "integer"
        }
       }
      },
      "types": [
       "embed",
       "parallel_cnn",
       "stacked_cnn",
       "stacked_parallel_cnn",
       "rnn",
       "cnnrnn",
       "mamba",
       "transformer",
       "mamba2",
       "jamba",
       "albert",
       "xlmroberta",
       "bert",
       "modernbert",
       "deberta",
       "gpt",
       "gpt2",
       "roberta",
       "xlnet",
       "distilbert",
       "camembert",
       "t5",
       "electra",
       "longformer",
       "auto_transformer",
       "tf_idf",
       "llm"
      ]
     },
     "preprocessing": {
      "fields": {
       "cache_encoder_embeddings": {
        "default": false,
        "type": "boolean"
       },
       "compute_idf": {
        "default": false,
        "type": "boolean"
       },
       "computed_fill_value": {
        "default": "<UNK>",
        "type": "string"
       },
       "fill_value": {
        "default": "<UNK>",
        "type": "string"
       },
       "lowercase": {
        "default": false,
        "type": "boolean"
       },
       "max_sequence_length": {
        "default": 256,
        "nullable": true,
        "type": "integer"
       },
       "missing_value_strategy": {
        "default": "fill_with_const",
        "type": "string"
       },
       "most_common": {
        "default": 20000,
        "type": "integer"
       },
       "ngram_size": {
        "default": 2,
        "type": "integer"
       },
       "padding": {
        "default": "right",
        "type": "string"
       },
       "padding_symbol": {
        "default": "<PAD>",
        "type": "string"
       },
       "pretrained_model_name_or_path": {
        "default": null,
        "nullable": true,
        "type": "string"
       },
       "prompt": {
        "type": "object"
       },
       "sequence_length": {
        "default": null,
        "nullable": true,
        "type": "integer"
       },
       "tokenizer": {
        "default": "space_punct",
        "type": "string"
       },
       "unknown_symbol": {
        "default": "<UNK>",
        "type": "string"
       },
       "vocab_file": {
        "default": null,
        "nullable": true,
        "type": "string"
       }
      }
     }
    },
    "timeseries": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "name": {
       "description": "Name of the input feature.",
       "type": "string"
      },
      "tied": {
       "default": null,
       "nullable": true,
       "type": "string"
      }
     },
     "encoder": {
      "options": {
       "cnnrnn": {
        "activation": {
         "default": "tanh",
         "description": "The default activation function to use.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "bidirectional": {
         "default": false,
         "description": "If true, two recurrent networks will perform encoding in the forward and backward direction and their outputs will be concatenated.",
         "type": "boolean"
        },
        "cell_type": {
         "default": "rnn",
         "description": "The type of recurrent cell to use. Available values are: `rnn`, `lstm`, `gru`. For reference about the differences between the cells please refer to [torch.nn Recurrent Layers](https://pytorch.org/docs/stable/nn.html#recurrent-layers).",
         "enum": [
          "rnn",
          "lstm",
          "gru"
         ],
         "type": "string"
        },
        "conv_activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each convolutional layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "conv_dropout": {
         "default": 0.0,
         "description": "The dropout rate for the convolutional layers",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "conv_layers": {
         "default": null,
         "description": "A list of dictionaries containing the parameters of all the convolutional layers. The length of the list determines the number of stacked convolutional layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `num_filters`, `filter_size`, `strides`, `padding`, `dilation_rate`, `use_bias`, `pool_function`, `pool_padding`, `pool_size`, `pool_strides`, `bias_initializer`, `weights_initializer`. If any of those values is missing from the dictionary, the default one specified as a parameter of the encoder will be used instead. If both `conv_layers` and `num_conv_layers` are `null`, a default list will be assigned to `conv_layers` with the value `[{filter_size: 7, pool_size: 3}, {filter_size: 7, pool_size: 3}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: 3}]`.",
         "type": null
        },
        "dilation_rate": {
         "default": 1,
         "description": "Dilation rate to use for dilated convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "filter_size": {
         "default": 5,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_conv_layers": {
         "default": null,
         "description": "The number of stacked convolutional layers when `conv_layers` is `null`.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of parallel fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "num_rec_layers": {
         "default": 1,
         "description": "The number of stacked recurrent layers.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pool_strides": {
         "default": null,
         "description": "Factor to scale down.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "recurrent_activation": {
         "default": "sigmoid",
         "description": "The activation function to use in the recurrent step",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "recurrent_dropout": {
         "default": 0.0,
         "description": "The dropout rate for the recurrent state",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "recurrent_initializer": {
         "default": "orthogonal",
         "description": "The initializer for recurrent matrix weights",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "reduce_output": {
         "default": "last",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "state_size": {
         "default": 256,
         "description": "The size of the state of the rnn.",
         "minimum": 1,
         "type": "integer"
        },
        "strides": {
         "default": 1,
         "description": "Stride length of the convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "unit_forget_bias": {
         "default": true,
         "description": "If true, add 1 to the bias of the forget gate at initialization",
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "dense": {
        "activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the dense encoder.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "Default normalization applied at the beginnging of fully connected layers.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_layers": {
         "default": 1,
         "description": "Number of stacked fully connected layers to apply. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "Size of the output of the feature.",
         "minimum": 1,
         "type": "integer"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "jamba": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "attention_every_k": {
         "default": 4,
         "description": "Every `attention_every_k`-th block is attention, the remainder are SSM. Default 4 gives a 1:3 attention:SSM ratio matching the Jamba paper.",
         "minimum": 1,
         "type": null
        },
        "d_conv": {
         "default": 4,
         "description": "Width of the depthwise 1D convolution inside each SSM block.",
         "minimum": 1,
         "type": null
        },
        "d_model": {
         "default": 256,
         "description": "Hidden width of every block \u2014 SSM and attention share the same d_model.",
         "minimum": 1,
         "type": null
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": null
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": null
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": null
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": null
        },
        "expand_factor": {
         "default": 2,
         "description": "Inner expansion factor inside each SSM block.",
         "minimum": 1,
         "type": null
        },
        "ffn_size": {
         "default": 1024,
         "description": "Feed-forward width inside each attention block.",
         "minimum": 1,
         "type": null
        },
        "n_layers": {
         "default": 8,
         "description": "Total number of stacked blocks (SSM + attention combined).",
         "minimum": 1,
         "type": null
        },
        "num_heads": {
         "default": 8,
         "description": "Number of attention heads (and SSD heads, shared).",
         "minimum": 1,
         "type": null
        },
        "output_size": {
         "default": 256,
         "description": "Output feature width emitted by the encoder.",
         "minimum": 1,
         "type": null
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "type": null
        },
        "reduce_output": {
         "default": "mean",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "type": null
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": null
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings.",
         "type": null
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "mamba": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "d_conv": {
         "default": 4,
         "description": "Width of the 1D convolution in each SSM layer.",
         "minimum": 1,
         "type": "integer"
        },
        "d_model": {
         "default": 256,
         "description": "Dimensionality of the model (hidden size for SSM layers).",
         "minimum": 1,
         "type": "integer"
        },
        "d_state": {
         "default": 16,
         "description": "Dimensionality of the state space in each SSM layer.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "expand_factor": {
         "default": 2,
         "description": "Expansion factor for the inner dimension of SSM layers.",
         "minimum": 1,
         "type": "integer"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "n_layers": {
         "default": 4,
         "description": "Number of stacked SSM layers.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each FC layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "mean",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use bias in linear projections.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "mamba2": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "d_conv": {
         "default": 4,
         "description": "Width of the depthwise 1D convolution inside each block.",
         "minimum": 1,
         "type": null
        },
        "d_model": {
         "default": 256,
         "description": "Hidden width of each Mamba-2 block.",
         "minimum": 1,
         "type": null
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": null
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": null
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": null
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": null
        },
        "expand_factor": {
         "default": 2,
         "description": "Inner expansion factor for each block.",
         "minimum": 1,
         "type": null
        },
        "n_layers": {
         "default": 4,
         "description": "Number of stacked Mamba-2 blocks.",
         "minimum": 1,
         "type": null
        },
        "num_heads": {
         "default": 8,
         "description": "Number of SSD heads. `d_model * expand_factor` must be divisible by `num_heads`.",
         "minimum": 1,
         "type": null
        },
        "output_size": {
         "default": 256,
         "description": "Output feature width emitted by the encoder.",
         "minimum": 1,
         "type": null
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "type": null
        },
        "reduce_output": {
         "default": "mean",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "type": null
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": null
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings.",
         "type": null
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "nbeats": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate inside blocks.",
         "maximum": 1.0,
         "minimum": 0.0,
         "type": "number"
        },
        "layer_size": {
         "default": 256,
         "description": "Width of fully-connected layers in each block.",
         "minimum": 1,
         "type": "integer"
        },
        "num_blocks": {
         "default": 3,
         "description": "Number of blocks per stack.",
         "minimum": 1,
         "type": "integer"
        },
        "num_layers": {
         "default": 4,
         "description": "Number of fully-connected layers per block.",
         "minimum": 1,
         "type": "integer"
        },
        "num_stacks": {
         "default": 2,
         "description": "Number of N-BEATS stacks.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "Size of the final output representation.",
         "minimum": 1,
         "type": "integer"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "parallel_cnn": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "conv_layers": {
         "default": null,
         "description": "A list of dictionaries containing the parameters of all the convolutional layers. The length of the list determines the number of stacked convolutional layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `num_filters`, `filter_size`, `strides`, `padding`, `dilation_rate`, `use_bias`, `pool_function`, `pool_padding`, `pool_size`, `pool_strides`, `bias_initializer`, `weights_initializer`. If any of those values is missing from the dictionary, the default one specified as a parameter of the encoder will be used instead. If both `conv_layers` and `num_conv_layers` are `null`, a default list will be assigned to `conv_layers` with the value `[{filter_size: 7, pool_size: 3}, {filter_size: 7, pool_size: 3}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: 3}]`.",
         "type": null
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate applied to the embedding. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "filter_size": {
         "default": 3,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_conv_layers": {
         "default": null,
         "description": "The number of stacked convolutional layers when `conv_layers` is `null`.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": null,
         "description": "Number of parallel fully connected layers to use.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "Whether to embed the input sequence.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "passthrough": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "encoding_size": {
         "default": null,
         "description": "The size of the encoding vector, or None if sequence elements are scalars.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "reduce_output": {
         "default": null,
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "patchtst": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "d_model": {
         "default": 128,
         "description": "Transformer hidden dimension.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout rate.",
         "maximum": 1.0,
         "minimum": 0.0,
         "type": "number"
        },
        "ffn_dim": {
         "default": 256,
         "description": "Feed-forward network hidden dimension inside Transformer blocks.",
         "minimum": 1,
         "type": "integer"
        },
        "num_heads": {
         "default": 8,
         "description": "Number of attention heads.",
         "minimum": 1,
         "type": "integer"
        },
        "num_layers": {
         "default": 3,
         "description": "Number of Transformer encoder layers.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "Output embedding size after pooling patches.",
         "minimum": 1,
         "type": "integer"
        },
        "patch_size": {
         "default": 16,
         "description": "Length of each patch (number of time steps per patch).",
         "minimum": 1,
         "type": "integer"
        },
        "patch_stride": {
         "default": 8,
         "description": "Stride between consecutive patches. Use patch_stride < patch_size for overlapping patches.",
         "minimum": 1,
         "type": "integer"
        },
        "reduce_output": {
         "default": "mean",
         "description": "How to aggregate patch representations: 'mean' pools all patches, 'last' uses the last, 'first' uses the first.",
         "enum": [
          "mean",
          "last",
          "first"
         ],
         "type": "string"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       },
       "rnn": {
        "activation": {
         "default": "tanh",
         "description": "The default activation function.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "bidirectional": {
         "default": false,
         "description": "If true, two recurrent networks will perform encoding in the forward and backward direction and their outputs will be concatenated.",
         "type": "boolean"
        },
        "cell_type": {
         "default": "rnn",
         "description": "The type of recurrent cell to use. Available values are: `rnn`, `lstm`, `gru`. For reference about the differences between the cells please refer to [torch.nn Recurrent Layers](https://pytorch.org/docs/stable/nn.html#recurrent-layers).",
         "enum": [
          "rnn",
          "lstm",
          "gru"
         ],
         "type": "string"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of parallel fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "num_layers": {
         "default": 1,
         "description": "The number of stacked recurrent layers.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "recurrent_activation": {
         "default": "sigmoid",
         "description": "The activation function to use in the recurrent step",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "recurrent_dropout": {
         "default": 0.0,
         "description": "The dropout rate for the recurrent state",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "recurrent_initializer": {
         "default": "orthogonal",
         "description": "The initializer for recurrent matrix weights",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "reduce_output": {
         "default": "last",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "state_size": {
         "default": 256,
         "description": "The size of the state of the rnn.",
         "minimum": 1,
         "type": "integer"
        },
        "unit_forget_bias": {
         "default": true,
         "description": "If true, add 1 to the bias of the forget gate at initialization",
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "stacked_cnn": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "conv_layers": {
         "default": null,
         "description": "A list of dictionaries containing the parameters of all the convolutional layers. The length of the list determines the number of stacked convolutional layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `num_filters`, `filter_size`, `strides`, `padding`, `dilation_rate`, `use_bias`, `pool_function`, `pool_padding`, `pool_size`, `pool_strides`, `bias_initializer`, `weights_initializer`. If any of those values is missing from the dictionary, the default one specified as a parameter of the encoder will be used instead. If both `conv_layers` and `num_conv_layers` are `null`, a default list will be assigned to `conv_layers` with the value `[{filter_size: 7, pool_size: 3}, {filter_size: 7, pool_size: 3}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: null}, {filter_size: 3, pool_size: 3}]`.",
         "type": null
        },
        "dilation_rate": {
         "default": 1,
         "description": "Dilation rate to use for dilated convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate applied to the embedding. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "filter_size": {
         "default": 3,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_conv_layers": {
         "default": null,
         "description": "The number of stacked convolutional layers when `conv_layers` is `null`.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": null,
         "description": "Number of parallel fully connected layers to use.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_padding": {
         "default": "same",
         "description": "Padding to use.",
         "enum": [
          "valid",
          "same"
         ],
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pool_strides": {
         "default": null,
         "description": "Factor to scale down.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "Whether to embed the input sequence.",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "strides": {
         "default": 1,
         "description": "Stride length of the convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "stacked_parallel_cnn": {
        "activation": {
         "default": "relu",
         "description": "The default activation function that will be used for each layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "dropout": {
         "default": 0.0,
         "description": "Dropout rate applied to the embedding. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters for each fully connected layer.",
         "type": null
        },
        "filter_size": {
         "default": 3,
         "description": "Size of the 1d convolutional filter. It indicates how wide the 1d convolutional filter is.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Parameters used if norm is either `batch` or `layer`.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": null,
         "description": "Number of parallel fully connected layers to use.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_filters": {
         "default": 256,
         "description": "Number of filters, and by consequence number of output channels of the 1d convolution.",
         "minimum": 1,
         "type": "integer"
        },
        "num_stacked_layers": {
         "default": null,
         "description": "If stacked_layers is null, this is the number of elements in the stack of parallel convolutional layers.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pool_function": {
         "default": "max",
         "description": "Pooling function to use. `max` will select the maximum value. Any of `average`, `avg`, or `mean` will compute the mean value",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "pool_size": {
         "default": null,
         "description": "The default pool_size that will be used for each layer. If a pool_size is not already specified in conv_layers this is the default pool_size that will be used for each layer. It indicates the size of the max pooling that will be performed along the `s` sequence dimension after the convolution operation.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "sum",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "stacked_layers": {
         "default": null,
         "description": "a nested list of lists of dictionaries containing the parameters of the stack of parallel convolutional layers. The length of the list determines the number of stacked parallel convolutional layers, length of the sub-lists determines the number of parallel conv layers and the content of each dictionary determines the parameters for a specific layer.",
         "type": null
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "transformer": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "dropout": {
         "default": 0.1,
         "description": "The dropout rate for the transformer block. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "embedding_size": {
         "default": 256,
         "description": "The maximum embedding size. The actual size will be `min(vocabulary_size, embedding_size)` for `dense` representations and exactly `vocabulary_size` for the `sparse` encoding, where `vocabulary_size` is the number of unique strings appearing in the training set input column plus the number of special tokens (`<UNK>`, `<PAD>`, `<SOS>`, `<EOS>`).",
         "minimum": 1,
         "type": "integer"
        },
        "embeddings_on_cpu": {
         "default": false,
         "description": "Whether to force the placement of the embedding matrix in regular memory and have the CPU resolve them. By default embedding matrices are stored on GPU memory if a GPU is used, as it allows for faster access, but in some cases the embedding matrix may be too large. This parameter forces the placement of the embedding matrix in regular memory and the CPU is used for embedding lookup, slightly slowing down the process as a result of data transfer between CPU and GPU memory.",
         "type": "boolean"
        },
        "embeddings_trainable": {
         "default": true,
         "description": "If `true` embeddings are trained during the training process, if `false` embeddings are fixed. It may be useful when loading pretrained embeddings for avoiding finetuning them. This parameter has effect only when `representation` is `dense`; `sparse` one-hot encodings are not trainable.",
         "type": "boolean"
        },
        "fc_activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "hidden_size": {
         "default": 256,
         "description": "The size of the hidden representation within the transformer block. It is usually the same as the embedding_size, but if the two values are different, a projection layer will be added before the first transformer block.",
         "minimum": 1,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "The default norm that will be used for each layer.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_fc_layers": {
         "default": 0,
         "description": "Number of parallel fully connected layers to use. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 0,
         "type": "integer"
        },
        "num_heads": {
         "default": 8,
         "description": "Number of attention heads in each transformer block.",
         "minimum": 1,
         "type": "integer"
        },
        "num_layers": {
         "default": 1,
         "description": "The number of transformer layers.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "The default output_size that will be used for each layer.",
         "minimum": 1,
         "type": "integer"
        },
        "pretrained_embeddings": {
         "default": null,
         "description": "Path to a file containing pretrained embeddings. By default `dense` embeddings are initialized randomly, but this parameter allows to specify a path to a file containing embeddings in the [GloVe format](https://nlp.stanford.edu/projects/glove/). When the file containing the embeddings is loaded, only the embeddings with labels present in the vocabulary are kept, the others are discarded. If the vocabulary contains strings that have no match in the embeddings file, their embeddings are initialized with the average of all other embedding plus some random noise to make them different from each other. This parameter has effect only if `representation` is `dense`.",
         "nullable": true,
         "type": "string"
        },
        "reduce_output": {
         "default": "last",
         "description": "How to reduce the output tensor along the `s` sequence length dimension if the rank of the tensor is greater than 2.",
         "enum": [
          "last",
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "attention",
          "attention_pooling",
          "none",
          "None",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "representation": {
         "default": "dense",
         "description": "Representation of the embedding. `dense` means the embeddings are initialized randomly, `sparse` means they are initialized to be one-hot encodings.",
         "enum": [
          "dense",
          "sparse"
         ],
         "type": "string"
        },
        "should_embed": {
         "default": true,
         "description": "If True the input sequence is expected to be made of integers and will be mapped into embeddings",
         "type": "boolean"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "transformer_output_size": {
         "default": 256,
         "description": "Size of the fully connected layer after self attention in the transformer block. This is usually the same as hidden_size and embedding_size.",
         "minimum": 1,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether to use a bias vector.",
         "type": "boolean"
        },
        "use_rope": {
         "default": false,
         "description": "If True, use Rotary Position Embeddings (RoPE) instead of absolute positional embeddings. RoPE encodes position by rotating query and key vectors, providing better length generalization and relative position awareness. Used by modern LLMs like LLaMA and Mistral.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       }
      },
      "types": [
       "dense",
       "passthrough",
       "parallel_cnn",
       "stacked_cnn",
       "stacked_parallel_cnn",
       "rnn",
       "cnnrnn",
       "mamba",
       "transformer",
       "mamba2",
       "jamba",
       "patchtst",
       "nbeats"
      ]
     },
     "preprocessing": {
      "fields": {
       "computed_fill_value": {
        "default": "",
        "type": "string"
       },
       "fill_value": {
        "default": "",
        "type": "string"
       },
       "missing_value_strategy": {
        "default": "fill_with_const",
        "description": "What strategy to follow when a row of data is missing.",
        "enum": [
         "fill_with_const",
         "fill_with_mode",
         "bfill",
         "ffill",
         "drop_row"
        ],
        "type": "string"
       },
       "padding": {
        "default": "right",
        "type": "string"
       },
       "padding_value": {
        "default": 0.0,
        "type": "number"
       },
       "timeseries_length_limit": {
        "default": 256,
        "type": "integer"
       },
       "tokenizer": {
        "default": "space",
        "type": "string"
       },
       "window_size": {
        "default": 0,
        "description": "Optional lookback window size used to convert a column-major dataset (one observation per row) into a row-major dataset (each row has a timeseries window of observations). Starting from a given observation, a sliding window is taken going `window_size - 1` rows back to form the timeseries input feature. If this value is left as 0, then it is assumed that the dataset has been provided in row-major format (i.e., it has already been preprocessed such that each row is a timeseries window).",
        "minimum": 0,
        "type": "integer"
       }
      }
     }
    },
    "vector": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "name": {
       "description": "Name of the input feature.",
       "type": "string"
      },
      "tied": {
       "default": null,
       "nullable": true,
       "type": "string"
      }
     },
     "encoder": {
      "options": {
       "dense": {
        "activation": {
         "default": "relu",
         "description": "Default activation function applied to the output of the fully connected layers.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        },
        "dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "description": "List of dictionaries containing the parameters of all the fully connected layers. The length of the list determines the number of stacked fully connected layers and the content of each dictionary determines the parameters for a specific layer. The available parameters for each layer are: `activation`, `dropout`, `norm`, `norm_params`, `output_size`, `use_bias`, `bias_initializer` and `weights_initializer`. If any of those values is missing from the dictionary, the default one provided as a standalone parameter will be used instead.",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the dense encoder.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "norm": {
         "default": null,
         "description": "Default normalization applied at the beginnging of fully connected layers.",
         "enum": [
          "batch",
          "layer",
          "ghost",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "norm_params": {
         "default": null,
         "description": "Default parameters passed to the `norm` module.",
         "nullable": true,
         "type": "object"
        },
        "num_layers": {
         "default": 1,
         "description": "Number of stacked fully connected layers to apply. Increasing layers adds capacity to the model, enabling it to learn more complex feature interactions.",
         "minimum": 1,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "Size of the output of the feature.",
         "minimum": 1,
         "type": "integer"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix. Options: `uniform`, `normal`, `constant`, `ones`, `zeros`, `eye`, `dirac`, `xavier_uniform`, `xavier_normal`, `kaiming_uniform`, `kaiming_normal`, `orthogonal`, `sparse`, `identity`. Alternatively it is possible to specify a dictionary with a key `type` that identifies the type of initializer and other keys for its parameters, e.g. `{type: normal, mean: 0, stddev: 0}`. For a description of the parameters of each initializer, see [torch.nn.init](https://pytorch.org/docs/stable/nn.init.html).",
         "type": null
        }
       },
       "passthrough": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       }
      },
      "types": [
       "passthrough",
       "dense"
      ]
     },
     "preprocessing": {
      "fields": {
       "computed_fill_value": {
        "default": "",
        "description": "The internally computed fill value to replace missing values with in case the missing_value_strategy is fill_with_mode or fill_with_mean",
        "type": "string"
       },
       "fill_value": {
        "default": "",
        "description": "The value to replace missing values with in case the missing_value_strategy is fill_with_const",
        "type": "string"
       },
       "missing_value_strategy": {
        "default": "fill_with_const",
        "description": "What strategy to follow when there's a missing value in a vector column",
        "enum": [
         "fill_with_const",
         "fill_with_mode",
         "bfill",
         "ffill",
         "drop_row"
        ],
        "type": "string"
       },
       "vector_size": {
        "default": null,
        "description": "The size of the vector. If None, the vector size will be inferred from the data.",
        "minimum": 1,
        "nullable": true,
        "type": "integer"
       }
      }
     }
    }
   },
   "output_features": {
    "anomaly": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "default_validation_metric": {
       "default": "anomaly_auroc",
       "type": "string"
      },
      "dependencies": {
       "description": "List of input features that this feature depends on.",
       "type": "array"
      },
      "input_size": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "name": {
       "description": "Name of the output feature.",
       "type": "string"
      },
      "num_classes": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "reduce_dependencies": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      },
      "reduce_input": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      },
      "threshold": {
       "default": "auto",
       "type": null
      },
      "threshold_percentile": {
       "default": 95.0,
       "type": "number"
      }
     },
     "decoder": {
      "options": {
       "anomaly": {
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the encoder output. Set automatically from the FC stack output shape.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        }
       }
      },
      "types": [
       "anomaly"
      ]
     },
     "loss": {
      "options": {
       "deep_sad": {
        "eta": {
         "default": 1.0,
         "description": "Weight for the labeled anomaly repulsion term.",
         "minimum": 0.0,
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "deep_svdd": {
        "nu": {
         "default": 0.1,
         "description": "Fraction of training examples allowed outside the hypersphere (soft-boundary mode). Set nu=0 for hard-boundary SVDD where all points are pulled toward c.",
         "maximum": 1.0,
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "drocc": {
        "num_perturbation_steps": {
         "default": 5,
         "description": "Gradient ascent steps for adversarial perturbation generation.",
         "minimum": 1,
         "type": "integer"
        },
        "perturbation_strength": {
         "default": 0.1,
         "description": "Magnitude of adversarial perturbations. Typical range: 0.01\u20130.5.",
         "minimum": 0.0,
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       }
      },
      "types": [
       "deep_svdd",
       "deep_sad",
       "drocc"
      ]
     }
    },
    "binary": {
     "common": {
      "calibration": {
       "default": false,
       "type": "boolean"
      },
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "default_validation_metric": {
       "default": "roc_auc",
       "type": "string"
      },
      "dependencies": {
       "description": "List of input features that this feature depends on.",
       "type": "array"
      },
      "input_size": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "name": {
       "description": "Name of the output feature.",
       "type": "string"
      },
      "num_classes": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "preprocessing": {
       "type": "object"
      },
      "reduce_dependencies": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      },
      "reduce_input": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      },
      "threshold": {
       "default": 0.5,
       "type": "number"
      }
     },
     "decoder": {
      "options": {
       "mlp_classifier": {
        "activation": {
         "default": "relu",
         "description": "Activation function applied after each hidden fully-connected layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vectors.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "calibration": {
         "default": null,
         "description": "Post-training calibration method to apply to the decoder logits. 'temperature_scaling' learns a single scalar T that divides logits (calibrated_logits = logits / T) using NLL minimisation on the validation set. It never changes argmax predictions but improves probability reliability. See: Guo et al., 'On Calibration of Modern Neural Networks', ICML 2017. Set to null (default) to disable calibration.",
         "enum": [
          "temperature_scaling",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the decoder. Set automatically from the combiner output size.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "mc_dropout_samples": {
         "default": 0,
         "description": "Number of Monte Carlo forward passes to run at inference time with dropout enabled. When > 0, the decoder is run mc_dropout_samples times and the mean of the resulting probability distributions is used as the prediction; the variance across runs is reported as an 'uncertainty' tensor alongside 'predictions' and 'probabilities'. Setting this to 0 (default) disables MC Dropout. See: Gal & Ghahramani, 'Dropout as a Bayesian Approximation', ICML 2016.",
         "minimum": 0,
         "type": "integer"
        },
        "num_classes": {
         "default": null,
         "description": "Number of classes to predict. Set automatically from the feature vocabulary size.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 1,
         "description": "Number of fully-connected hidden layers to stack before the classification head. When set to 1 (default) a single hidden layer of size output_size is applied. Set to 0 to make this decoder equivalent to the standard single-layer Classifier.",
         "minimum": 0,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "Size of each hidden fully-connected layer. Only used when num_fc_layers > 0.",
         "minimum": 1,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether each fully-connected layer (and the final projection) uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrices.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       },
       "regressor": {
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the decoder.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       }
      },
      "types": [
       "regressor",
       "mlp_classifier"
      ]
     },
     "loss": {
      "options": {
       "binary_weighted_cross_entropy": {
        "confidence_penalty": {
         "default": 0,
         "description": "Penalizes overconfident predictions (low entropy) by adding an additional term that penalizes too confident predictions by adding a `a * (max_entropy - entropy) / max_entropy` term to the loss, where a is the value of this parameter. Useful in case of noisy labels.",
         "minimum": 0.0,
         "type": "number"
        },
        "positive_class_weight": {
         "default": null,
         "description": "Weight of the positive class.",
         "minimum": 0.0,
         "nullable": true,
         "type": "number"
        },
        "robust_lambda": {
         "default": 0,
         "description": "Replaces the loss with `(1 - robust_lambda) * loss + robust_lambda / c` where `c` is the number of classes. Useful in case of noisy labels.",
         "minimum": 0,
         "type": "integer"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "entropic_open_set": {
        "background_class": {
         "default": null,
         "description": "Class index that represents 'unknown' or background samples. Samples with this label receive the entropic penalty instead of cross-entropy. Set to None to disable open-set behaviour and fall back to standard cross-entropy.",
         "nullable": true,
         "type": "integer"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "focal": {
        "alpha": {
         "default": 0.25,
         "description": "Weighting factor for the positive class in binary classification. Balances the importance of positive/negative examples.",
         "maximum": 1.0,
         "minimum": 0.0,
         "type": "number"
        },
        "gamma": {
         "default": 2.0,
         "description": "Focusing parameter that reduces the loss contribution from easy examples and extends the range in which an example receives low loss. gamma=0 reduces focal loss to standard cross-entropy.",
         "minimum": 0.0,
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "objectosphere": {
        "background_class": {
         "default": null,
         "description": "Class index that represents 'unknown' or background samples. Samples with this label receive the entropic + magnitude-suppression penalty. Set to None to fall back to standard cross-entropy.",
         "nullable": true,
         "type": "integer"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        },
        "xi": {
         "default": 10.0,
         "description": "Minimum desired logit L2 norm for known-class samples. A hinge term max(0, xi - ||z||)\u00b2 is added to push representations of known inputs above this threshold. Typical values: 1\u201350.",
         "minimum": 0.0,
         "type": "number"
        },
        "zeta": {
         "default": 0.1,
         "description": "Weight applied to the magnitude-suppression term for unknown samples. Higher values more aggressively shrink logit norms for background inputs.",
         "minimum": 0.0,
         "type": "number"
        }
       }
      },
      "types": [
       "binary_weighted_cross_entropy",
       "entropic_open_set",
       "objectosphere",
       "focal"
      ]
     }
    },
    "category": {
     "common": {
      "calibration": {
       "default": false,
       "type": "boolean"
      },
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "default_validation_metric": {
       "default": "accuracy",
       "type": "string"
      },
      "dependencies": {
       "description": "List of input features that this feature depends on.",
       "type": "array"
      },
      "input_size": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "name": {
       "description": "Name of the output feature.",
       "type": "string"
      },
      "num_classes": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "preprocessing": {
       "type": "object"
      },
      "reduce_dependencies": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      },
      "reduce_input": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      },
      "top_k": {
       "default": 3,
       "type": "integer"
      }
     },
     "decoder": {
      "options": {
       "classifier": {
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "calibration": {
         "default": null,
         "description": "Post-training calibration method to apply to the decoder logits. 'temperature_scaling' learns a single scalar T that divides logits (calibrated_logits = logits / T) using NLL minimisation on the validation set. It never changes argmax predictions but improves probability reliability. See: Guo et al., 'On Calibration of Modern Neural Networks', ICML 2017. Set to null (default) to disable calibration.",
         "enum": [
          "temperature_scaling",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the decoder.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "mc_dropout_samples": {
         "default": 0,
         "description": "Number of Monte Carlo forward passes to run at inference time with dropout enabled. When > 0, the decoder is run mc_dropout_samples times and the mean of the resulting probability distributions is used as the prediction; the variance across runs is reported as an 'uncertainty' tensor alongside 'predictions' and 'probabilities'. Setting this to 0 (default) disables MC Dropout. See: Gal & Ghahramani, 'Dropout as a Bayesian Approximation', ICML 2016.",
         "minimum": 0,
         "type": "integer"
        },
        "num_classes": {
         "default": null,
         "description": "Number of classes to predict.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       },
       "mlp_classifier": {
        "activation": {
         "default": "relu",
         "description": "Activation function applied after each hidden fully-connected layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vectors.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "calibration": {
         "default": null,
         "description": "Post-training calibration method to apply to the decoder logits. 'temperature_scaling' learns a single scalar T that divides logits (calibrated_logits = logits / T) using NLL minimisation on the validation set. It never changes argmax predictions but improves probability reliability. See: Guo et al., 'On Calibration of Modern Neural Networks', ICML 2017. Set to null (default) to disable calibration.",
         "enum": [
          "temperature_scaling",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the decoder. Set automatically from the combiner output size.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "mc_dropout_samples": {
         "default": 0,
         "description": "Number of Monte Carlo forward passes to run at inference time with dropout enabled. When > 0, the decoder is run mc_dropout_samples times and the mean of the resulting probability distributions is used as the prediction; the variance across runs is reported as an 'uncertainty' tensor alongside 'predictions' and 'probabilities'. Setting this to 0 (default) disables MC Dropout. See: Gal & Ghahramani, 'Dropout as a Bayesian Approximation', ICML 2016.",
         "minimum": 0,
         "type": "integer"
        },
        "num_classes": {
         "default": null,
         "description": "Number of classes to predict. Set automatically from the feature vocabulary size.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 1,
         "description": "Number of fully-connected hidden layers to stack before the classification head. When set to 1 (default) a single hidden layer of size output_size is applied. Set to 0 to make this decoder equivalent to the standard single-layer Classifier.",
         "minimum": 0,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "Size of each hidden fully-connected layer. Only used when num_fc_layers > 0.",
         "minimum": 1,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether each fully-connected layer (and the final projection) uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrices.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       }
      },
      "types": [
       "classifier",
       "mlp_classifier"
      ]
     },
     "loss": {
      "options": {
       "corn": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "entmax15": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "entropic_open_set": {
        "background_class": {
         "default": null,
         "description": "Class index that represents 'unknown' or background samples. Samples with this label receive the entropic penalty instead of cross-entropy. Set to None to disable open-set behaviour and fall back to standard cross-entropy.",
         "nullable": true,
         "type": "integer"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "focal": {
        "alpha": {
         "default": 0.25,
         "description": "Weighting factor for the positive class in binary classification. Balances the importance of positive/negative examples.",
         "maximum": 1.0,
         "minimum": 0.0,
         "type": "number"
        },
        "gamma": {
         "default": 2.0,
         "description": "Focusing parameter that reduces the loss contribution from easy examples and extends the range in which an example receives low loss. gamma=0 reduces focal loss to standard cross-entropy.",
         "minimum": 0.0,
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "objectosphere": {
        "background_class": {
         "default": null,
         "description": "Class index that represents 'unknown' or background samples. Samples with this label receive the entropic + magnitude-suppression penalty. Set to None to fall back to standard cross-entropy.",
         "nullable": true,
         "type": "integer"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        },
        "xi": {
         "default": 10.0,
         "description": "Minimum desired logit L2 norm for known-class samples. A hinge term max(0, xi - ||z||)\u00b2 is added to push representations of known inputs above this threshold. Typical values: 1\u201350.",
         "minimum": 0.0,
         "type": "number"
        },
        "zeta": {
         "default": 0.1,
         "description": "Weight applied to the magnitude-suppression term for unknown samples. Higher values more aggressively shrink logit norms for background inputs.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "poly": {
        "epsilon": {
         "default": 1.0,
         "description": "Coefficient for the polynomial correction term. epsilon=0 reduces PolyLoss to standard cross-entropy.",
         "minimum": 0.0,
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "softmax_cross_entropy": {
        "class_similarities": {
         "default": null,
         "description": "If not `null` it is a `c x c` matrix in the form of a list of lists that contains the mutual similarity of classes. It is used if `class_similarities_temperature` is greater than 0. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too).",
         "nullable": true,
         "type": "array"
        },
        "class_similarities_temperature": {
         "default": 0,
         "description": "The temperature parameter of the softmax that is performed on each row of `class_similarities`. The output of that softmax is used to determine the supervision vector to provide instead of the one hot vector that would be provided otherwise for each datapoint. The intuition behind it is that errors between similar classes are more tolerable than errors between really different classes.",
         "minimum": 0,
         "type": "integer"
        },
        "class_weights": {
         "default": null,
         "description": "Weights to apply to each class in the loss. If not specified, all classes are weighted equally. The value can be a vector of weights, one for each class, that is multiplied to the loss of the datapoints that have that class as ground truth. It is an alternative to oversampling in case of unbalanced class distribution. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too). Alternatively, the value can be a dictionary with class strings as keys and weights as values, like `{class_a: 0.5, class_b: 0.7, ...}`.",
         "type": null
        },
        "confidence_penalty": {
         "default": 0,
         "description": "Penalizes overconfident predictions (low entropy) by adding an additional term that penalizes too confident predictions by adding a `a * (max_entropy - entropy) / max_entropy` term to the loss, where a is the value of this parameter. Useful in case of noisy labels.",
         "minimum": 0.0,
         "type": "number"
        },
        "robust_lambda": {
         "default": 0,
         "description": "Replaces the loss with `(1 - robust_lambda) * loss + robust_lambda / c` where `c` is the number of classes. Useful in case of noisy labels.",
         "minimum": 0,
         "type": "integer"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "sparsemax": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       }
      },
      "types": [
       "softmax_cross_entropy",
       "corn",
       "entropic_open_set",
       "objectosphere",
       "focal",
       "poly",
       "sparsemax",
       "entmax15"
      ]
     }
    },
    "category_distribution": {
     "common": {
      "calibration": {
       "default": false,
       "type": "boolean"
      },
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "default_validation_metric": {
       "default": "accuracy",
       "type": "string"
      },
      "dependencies": {
       "description": "List of input features that this feature depends on.",
       "type": "array"
      },
      "input_size": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "name": {
       "description": "Name of the output feature.",
       "type": "string"
      },
      "num_classes": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "preprocessing": {
       "type": "object"
      },
      "reduce_dependencies": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      },
      "reduce_input": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      },
      "top_k": {
       "default": 3,
       "type": "integer"
      }
     },
     "decoder": {
      "options": {
       "classifier": {
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "calibration": {
         "default": null,
         "description": "Post-training calibration method to apply to the decoder logits. 'temperature_scaling' learns a single scalar T that divides logits (calibrated_logits = logits / T) using NLL minimisation on the validation set. It never changes argmax predictions but improves probability reliability. See: Guo et al., 'On Calibration of Modern Neural Networks', ICML 2017. Set to null (default) to disable calibration.",
         "enum": [
          "temperature_scaling",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the decoder.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "mc_dropout_samples": {
         "default": 0,
         "description": "Number of Monte Carlo forward passes to run at inference time with dropout enabled. When > 0, the decoder is run mc_dropout_samples times and the mean of the resulting probability distributions is used as the prediction; the variance across runs is reported as an 'uncertainty' tensor alongside 'predictions' and 'probabilities'. Setting this to 0 (default) disables MC Dropout. See: Gal & Ghahramani, 'Dropout as a Bayesian Approximation', ICML 2016.",
         "minimum": 0,
         "type": "integer"
        },
        "num_classes": {
         "default": null,
         "description": "Number of classes to predict.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       },
       "mlp_classifier": {
        "activation": {
         "default": "relu",
         "description": "Activation function applied after each hidden fully-connected layer.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vectors.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "calibration": {
         "default": null,
         "description": "Post-training calibration method to apply to the decoder logits. 'temperature_scaling' learns a single scalar T that divides logits (calibrated_logits = logits / T) using NLL minimisation on the validation set. It never changes argmax predictions but improves probability reliability. See: Guo et al., 'On Calibration of Modern Neural Networks', ICML 2017. Set to null (default) to disable calibration.",
         "enum": [
          "temperature_scaling",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "dropout": {
         "default": 0.0,
         "description": "Default dropout rate applied to fully connected layers. Increasing dropout is a common form of regularization to combat overfitting. The dropout is expressed as the probability of an element to be zeroed out (0.0 means no dropout).",
         "maximum": 1,
         "minimum": 0,
         "type": "number"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the decoder. Set automatically from the combiner output size.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "mc_dropout_samples": {
         "default": 0,
         "description": "Number of Monte Carlo forward passes to run at inference time with dropout enabled. When > 0, the decoder is run mc_dropout_samples times and the mean of the resulting probability distributions is used as the prediction; the variance across runs is reported as an 'uncertainty' tensor alongside 'predictions' and 'probabilities'. Setting this to 0 (default) disables MC Dropout. See: Gal & Ghahramani, 'Dropout as a Bayesian Approximation', ICML 2016.",
         "minimum": 0,
         "type": "integer"
        },
        "num_classes": {
         "default": null,
         "description": "Number of classes to predict. Set automatically from the feature vocabulary size.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 1,
         "description": "Number of fully-connected hidden layers to stack before the classification head. When set to 1 (default) a single hidden layer of size output_size is applied. Set to 0 to make this decoder equivalent to the standard single-layer Classifier.",
         "minimum": 0,
         "type": "integer"
        },
        "output_size": {
         "default": 256,
         "description": "Size of each hidden fully-connected layer. Only used when num_fc_layers > 0.",
         "minimum": 1,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether each fully-connected layer (and the final projection) uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrices.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       }
      },
      "types": [
       "classifier",
       "mlp_classifier"
      ]
     },
     "loss": {
      "options": {
       "corn": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "entmax15": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "entropic_open_set": {
        "background_class": {
         "default": null,
         "description": "Class index that represents 'unknown' or background samples. Samples with this label receive the entropic penalty instead of cross-entropy. Set to None to disable open-set behaviour and fall back to standard cross-entropy.",
         "nullable": true,
         "type": "integer"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "focal": {
        "alpha": {
         "default": 0.25,
         "description": "Weighting factor for the positive class in binary classification. Balances the importance of positive/negative examples.",
         "maximum": 1.0,
         "minimum": 0.0,
         "type": "number"
        },
        "gamma": {
         "default": 2.0,
         "description": "Focusing parameter that reduces the loss contribution from easy examples and extends the range in which an example receives low loss. gamma=0 reduces focal loss to standard cross-entropy.",
         "minimum": 0.0,
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "objectosphere": {
        "background_class": {
         "default": null,
         "description": "Class index that represents 'unknown' or background samples. Samples with this label receive the entropic + magnitude-suppression penalty. Set to None to fall back to standard cross-entropy.",
         "nullable": true,
         "type": "integer"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        },
        "xi": {
         "default": 10.0,
         "description": "Minimum desired logit L2 norm for known-class samples. A hinge term max(0, xi - ||z||)\u00b2 is added to push representations of known inputs above this threshold. Typical values: 1\u201350.",
         "minimum": 0.0,
         "type": "number"
        },
        "zeta": {
         "default": 0.1,
         "description": "Weight applied to the magnitude-suppression term for unknown samples. Higher values more aggressively shrink logit norms for background inputs.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "poly": {
        "epsilon": {
         "default": 1.0,
         "description": "Coefficient for the polynomial correction term. epsilon=0 reduces PolyLoss to standard cross-entropy.",
         "minimum": 0.0,
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "softmax_cross_entropy": {
        "class_similarities": {
         "default": null,
         "description": "If not `null` it is a `c x c` matrix in the form of a list of lists that contains the mutual similarity of classes. It is used if `class_similarities_temperature` is greater than 0. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too).",
         "nullable": true,
         "type": "array"
        },
        "class_similarities_temperature": {
         "default": 0,
         "description": "The temperature parameter of the softmax that is performed on each row of `class_similarities`. The output of that softmax is used to determine the supervision vector to provide instead of the one hot vector that would be provided otherwise for each datapoint. The intuition behind it is that errors between similar classes are more tolerable than errors between really different classes.",
         "minimum": 0,
         "type": "integer"
        },
        "class_weights": {
         "default": null,
         "description": "Weights to apply to each class in the loss. If not specified, all classes are weighted equally. The value can be a vector of weights, one for each class, that is multiplied to the loss of the datapoints that have that class as ground truth. It is an alternative to oversampling in case of unbalanced class distribution. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too). Alternatively, the value can be a dictionary with class strings as keys and weights as values, like `{class_a: 0.5, class_b: 0.7, ...}`.",
         "type": null
        },
        "confidence_penalty": {
         "default": 0,
         "description": "Penalizes overconfident predictions (low entropy) by adding an additional term that penalizes too confident predictions by adding a `a * (max_entropy - entropy) / max_entropy` term to the loss, where a is the value of this parameter. Useful in case of noisy labels.",
         "minimum": 0.0,
         "type": "number"
        },
        "robust_lambda": {
         "default": 0,
         "description": "Replaces the loss with `(1 - robust_lambda) * loss + robust_lambda / c` where `c` is the number of classes. Useful in case of noisy labels.",
         "minimum": 0,
         "type": "integer"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "sparsemax": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       }
      },
      "types": [
       "softmax_cross_entropy",
       "corn",
       "entropic_open_set",
       "objectosphere",
       "focal",
       "poly",
       "sparsemax",
       "entmax15"
      ]
     }
    },
    "image": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "default_validation_metric": {
       "default": "loss",
       "description": "Internal only use parameter: default validation metric for image output feature.",
       "enum": [
        "loss"
       ],
       "type": "string"
      },
      "dependencies": {
       "description": "List of input features that this feature depends on.",
       "type": "array"
      },
      "input_size": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "name": {
       "description": "Name of the output feature.",
       "type": "string"
      },
      "num_classes": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "preprocessing": {
       "type": "object"
      },
      "reduce_dependencies": {
       "default": null,
       "description": "How to reduce the dependencies of the output feature.",
       "enum": [
        "last",
        "sum",
        "mean",
        "avg",
        "max",
        "concat",
        "attention",
        "attention_pooling",
        "none",
        "None",
        null
       ],
       "nullable": true,
       "type": "string"
      },
      "reduce_input": {
       "default": null,
       "description": "How to reduce an input that is not a vector, but a matrix or a higher order tensor, on the first dimension (second if you count the batch dimension)",
       "enum": [
        "last",
        "sum",
        "mean",
        "avg",
        "max",
        "concat",
        "attention",
        "attention_pooling",
        "none",
        "None",
        null
       ],
       "nullable": true,
       "type": "string"
      }
     },
     "decoder": {
      "options": {
       "fpn": {
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "height": {
         "default": null,
         "description": "Height of the output segmentation map.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "input_size": {
         "default": null,
         "description": "Size of the input feature vector from the combiner.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_channels": {
         "default": 256,
         "description": "Number of channels in each FPN level after the lateral 1x1 projection. All pyramid levels are projected to this width before the top-down merge.",
         "minimum": 1,
         "type": "integer"
        },
        "num_classes": {
         "default": null,
         "description": "Number of segmentation classes to predict.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "num_levels": {
         "default": 4,
         "description": "Number of pyramid levels to build in the top-down pathway. More levels capture coarser context; typical range is 2-5.",
         "minimum": 1,
         "type": "integer"
        },
        "width": {
         "default": null,
         "description": "Width of the output segmentation map.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        }
       },
       "segformer": {
        "dropout": {
         "default": 0.1,
         "description": "Dropout probability applied after the hidden MLP projection.",
         "maximum": 1.0,
         "minimum": 0.0,
         "type": "number"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "height": {
         "default": null,
         "description": "Height of the output segmentation map.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "hidden_size": {
         "default": 256,
         "description": "Width of the hidden MLP projection applied to the feature map before upsampling. Larger values increase capacity but also compute cost.",
         "minimum": 1,
         "type": "integer"
        },
        "input_size": {
         "default": null,
         "description": "Size of the input feature vector from the combiner.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_channels": {
         "default": null,
         "description": "Number of channels in the input image (informational; set from preprocessing).",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "num_classes": {
         "default": null,
         "description": "Number of segmentation classes to predict.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "width": {
         "default": null,
         "description": "Width of the output segmentation map.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        }
       },
       "unet": {
        "conv_norm": {
         "default": "batch",
         "description": "This is the default norm that will be used for each double conv layer.It can be null or batch.",
         "enum": [
          "batch",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "height": {
         "default": null,
         "description": "Height of the output image.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "input_size": {
         "default": 1024,
         "description": "Size of the input to the decoder.",
         "minimum": 1,
         "type": "integer"
        },
        "num_channels": {
         "default": null,
         "description": "Number of channels in the output image.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "num_classes": {
         "default": null,
         "description": "Number of classes to predict in the output.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "num_stages": {
         "default": 4,
         "description": "Number of encoder/decoder stage pairs in the UNet. The input image dimensions must be divisible by 2^num_stages. Increasing this value lets the model capture features at more spatial scales.",
         "minimum": 1,
         "type": "integer"
        },
        "width": {
         "default": null,
         "description": "Width of the output image.",
         "minimum": 0,
         "nullable": true,
         "type": "integer"
        }
       }
      },
      "types": [
       "unet",
       "segformer",
       "fpn"
      ]
     },
     "loss": {
      "options": {
       "dice": {
        "smooth": {
         "default": 1.0,
         "description": "Laplace smoothing term added to numerator and denominator to prevent division by zero when both prediction and target are empty.",
         "minimum": 0.0,
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "focal": {
        "alpha": {
         "default": 0.25,
         "description": "Weighting factor for the positive class in binary classification. Balances the importance of positive/negative examples.",
         "maximum": 1.0,
         "minimum": 0.0,
         "type": "number"
        },
        "gamma": {
         "default": 2.0,
         "description": "Focusing parameter that reduces the loss contribution from easy examples and extends the range in which an example receives low loss. gamma=0 reduces focal loss to standard cross-entropy.",
         "minimum": 0.0,
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "lovasz_softmax": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "softmax_cross_entropy": {
        "class_similarities": {
         "default": null,
         "description": "If not `null` it is a `c x c` matrix in the form of a list of lists that contains the mutual similarity of classes. It is used if `class_similarities_temperature` is greater than 0. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too).",
         "nullable": true,
         "type": "array"
        },
        "class_similarities_temperature": {
         "default": 0,
         "description": "The temperature parameter of the softmax that is performed on each row of `class_similarities`. The output of that softmax is used to determine the supervision vector to provide instead of the one hot vector that would be provided otherwise for each datapoint. The intuition behind it is that errors between similar classes are more tolerable than errors between really different classes.",
         "minimum": 0,
         "type": "integer"
        },
        "class_weights": {
         "default": null,
         "description": "Weights to apply to each class in the loss. If not specified, all classes are weighted equally. The value can be a vector of weights, one for each class, that is multiplied to the loss of the datapoints that have that class as ground truth. It is an alternative to oversampling in case of unbalanced class distribution. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too). Alternatively, the value can be a dictionary with class strings as keys and weights as values, like `{class_a: 0.5, class_b: 0.7, ...}`.",
         "type": null
        },
        "confidence_penalty": {
         "default": 0,
         "description": "Penalizes overconfident predictions (low entropy) by adding an additional term that penalizes too confident predictions by adding a `a * (max_entropy - entropy) / max_entropy` term to the loss, where a is the value of this parameter. Useful in case of noisy labels.",
         "minimum": 0.0,
         "type": "number"
        },
        "robust_lambda": {
         "default": 0,
         "description": "Replaces the loss with `(1 - robust_lambda) * loss + robust_lambda / c` where `c` is the number of classes. Useful in case of noisy labels.",
         "minimum": 0,
         "type": "integer"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       }
      },
      "types": [
       "softmax_cross_entropy",
       "focal",
       "dice",
       "lovasz_softmax"
      ]
     }
    },
    "number": {
     "common": {
      "clip": {
       "default": null,
       "type": null
      },
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "default_validation_metric": {
       "default": "mean_squared_error",
       "type": "string"
      },
      "dependencies": {
       "description": "List of input features that this feature depends on.",
       "type": "array"
      },
      "input_size": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "name": {
       "description": "Name of the output feature.",
       "type": "string"
      },
      "num_classes": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "preprocessing": {
       "type": "object"
      },
      "reduce_dependencies": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      },
      "reduce_input": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      }
     },
     "decoder": {
      "options": {
       "regressor": {
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the decoder.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       }
      },
      "types": [
       "regressor"
      ]
     },
     "loss": {
      "options": {
       "huber": {
        "delta": {
         "default": 1.0,
         "description": "Threshold at which to change between delta-scaled L1 and L2 loss.",
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "mean_absolute_error": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "mean_absolute_percentage_error": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "mean_squared_error": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "root_mean_squared_error": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "root_mean_squared_percentage_error": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       }
      },
      "types": [
       "mean_squared_error",
       "mean_absolute_error",
       "mean_absolute_percentage_error",
       "root_mean_squared_error",
       "root_mean_squared_percentage_error",
       "huber"
      ]
     }
    },
    "sequence": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "default_validation_metric": {
       "default": "loss",
       "description": "Internal only use parameter: default validation metric for sequence output feature.",
       "enum": [
        "loss"
       ],
       "type": "string"
      },
      "dependencies": {
       "description": "List of input features that this feature depends on.",
       "type": "array"
      },
      "input_size": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "name": {
       "description": "Name of the output feature.",
       "type": "string"
      },
      "num_classes": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "preprocessing": {
       "type": "object"
      },
      "reduce_dependencies": {
       "default": "sum",
       "description": "How to reduce the dependencies of the output feature.",
       "enum": [
        "last",
        "sum",
        "mean",
        "avg",
        "max",
        "concat",
        "attention",
        "attention_pooling",
        "none",
        "None",
        null
       ],
       "nullable": true,
       "type": "string"
      },
      "reduce_input": {
       "default": "sum",
       "description": "How to reduce an input that is not a vector, but a matrix or a higher order tensor, on the first dimension (second if you count the batch dimension)",
       "enum": [
        "last",
        "sum",
        "mean",
        "avg",
        "max",
        "concat",
        "attention",
        "attention_pooling",
        "none",
        "None",
        null
       ],
       "nullable": true,
       "type": "string"
      }
     },
     "decoder": {
      "options": {
       "generator": {
        "beam_length_penalty": {
         "default": 1.0,
         "description": "Length penalty exponent applied to beam search scores. Score = log_prob / (length ^ beam_length_penalty). Values > 1 penalise longer sequences; values < 1 favour them. Only used when beam_width > 1.",
         "minimum": 0.0,
         "type": "number"
        },
        "beam_width": {
         "default": 1,
         "description": "Width of the beam for beam search decoding. 1 = greedy decoding (default). Values > 1 enable beam search at inference time, keeping the top beam_width candidate sequences at each step.",
         "minimum": 1,
         "type": "integer"
        },
        "cell_type": {
         "default": "gru",
         "description": "Type of recurrent cell to use.",
         "enum": [
          "rnn",
          "lstm",
          "gru"
         ],
         "type": "string"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": 256,
         "description": "Size of the input to the decoder.",
         "minimum": 1,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "num_layers": {
         "default": 1,
         "description": "The number of stacked recurrent layers.",
         "minimum": 1,
         "type": "integer"
        },
        "reduce_input": {
         "default": "sum",
         "description": "How to reduce an input that is not a vector, but a matrix or a higher order tensor, on the first dimension (second if you count the batch dimension)",
         "enum": [
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "last"
         ],
         "type": "string"
        },
        "teacher_forcing_decay": {
         "default": "none",
         "description": "Decay schedule for teacher forcing probability during training. none always uses full teacher forcing; linear linearly decays the probability to zero; exponential applies exponential decay. Implements scheduled sampling (Bengio et al., NeurIPS 2015).",
         "enum": [
          "none",
          "linear",
          "exponential"
         ],
         "type": "string"
        },
        "teacher_forcing_decay_rate": {
         "default": 0.01,
         "description": "Rate of decay for the teacher forcing probability per decoding step when teacher_forcing_decay is linear or exponential.",
         "maximum": 1.0,
         "minimum": 0.0,
         "type": "number"
        }
       },
       "tagger": {
        "attention_embedding_size": {
         "default": 256,
         "description": "The embedding size of the multi-head self attention layer.",
         "minimum": 1,
         "type": "integer"
        },
        "attention_num_heads": {
         "default": 8,
         "description": "The number of attention heads in the multi-head self attention layer.",
         "minimum": 1,
         "type": "integer"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": 256,
         "description": "Size of the input to the decoder.",
         "minimum": 1,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "use_attention": {
         "default": false,
         "description": "Whether to apply a multi-head self attention layer before prediction.",
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        }
       },
       "transformer_generator": {
        "beam_length_penalty": {
         "default": 1.0,
         "description": "Length penalty exponent for beam search. Score = log_prob / (length ^ beam_length_penalty). Only active when beam_width > 1.",
         "minimum": 0.0,
         "type": "number"
        },
        "beam_width": {
         "default": 1,
         "description": "Width of the beam for beam search decoding at inference time. 1 = greedy decoding (default). Values > 1 keep the top beam_width candidates at each step.",
         "minimum": 1,
         "type": "integer"
        },
        "d_model": {
         "default": 256,
         "description": "Dimensionality of the transformer decoder layers (embedding size and hidden size). Must match input_size or a projection will be applied.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout probability applied within transformer decoder layers.",
         "maximum": 1.0,
         "minimum": 0.0,
         "type": "number"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "ffn_size": {
         "default": 1024,
         "description": "Size of the feed-forward network (dim_feedforward) inside each transformer decoder layer.",
         "minimum": 1,
         "type": "integer"
        },
        "input_size": {
         "default": 256,
         "description": "Size of the encoder output (d_model). The encoder output is projected to this size before being used as cross-attention memory in the transformer decoder.",
         "minimum": 1,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "num_heads": {
         "default": 8,
         "description": "Number of attention heads in each multi-head attention sub-layer. d_model must be divisible by num_heads.",
         "minimum": 1,
         "type": "integer"
        },
        "num_layers": {
         "default": 2,
         "description": "Number of transformer decoder layers.",
         "minimum": 1,
         "type": "integer"
        },
        "reduce_input": {
         "default": "sum",
         "description": "How to reduce a 3-D encoder output (batch x seq x hidden) to a 2-D context vector (batch x hidden). Ignored when the encoder output is already 2-D.",
         "enum": [
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "last"
         ],
         "type": "string"
        }
       }
      },
      "types": [
       "generator",
       "transformer_generator",
       "tagger"
      ]
     },
     "loss": {
      "options": {
       "entmax15": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "next_token_softmax_cross_entropy": {
        "class_similarities": {
         "default": null,
         "nullable": true,
         "type": "array"
        },
        "class_similarities_temperature": {
         "default": 0,
         "type": "integer"
        },
        "class_weights": {
         "default": null,
         "type": null
        },
        "confidence_penalty": {
         "default": 0,
         "type": "number"
        },
        "robust_lambda": {
         "default": 0,
         "type": "integer"
        },
        "unique": {
         "default": false,
         "type": "boolean"
        },
        "weight": {
         "default": 1.0,
         "type": "number"
        }
       },
       "sequence_softmax_cross_entropy": {
        "class_similarities": {
         "default": null,
         "description": "If not `null` it is a `c x c` matrix in the form of a list of lists that contains the mutual similarity of classes. It is used if `class_similarities_temperature` is greater than 0. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too).",
         "nullable": true,
         "type": "array"
        },
        "class_similarities_temperature": {
         "default": 0,
         "description": "The temperature parameter of the softmax that is performed on each row of `class_similarities`. The output of that softmax is used to determine the supervision vector to provide instead of the one hot vector that would be provided otherwise for each datapoint. The intuition behind it is that errors between similar classes are more tolerable than errors between really different classes.",
         "minimum": 0,
         "type": "integer"
        },
        "class_weights": {
         "default": null,
         "description": "Weights to apply to each class in the loss. If not specified, all classes are weighted equally. The value can be a vector of weights, one for each class, that is multiplied to the loss of the datapoints that have that class as ground truth. It is an alternative to oversampling in case of unbalanced class distribution. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too). Alternatively, the value can be a dictionary with class strings as keys and weights as values, like `{class_a: 0.5, class_b: 0.7, ...}`.",
         "type": null
        },
        "confidence_penalty": {
         "default": 0,
         "description": "Penalizes overconfident predictions (low entropy) by adding an additional term that penalizes too confident predictions by adding a `a * (max_entropy - entropy) / max_entropy` term to the loss, where a is the value of this parameter. Useful in case of noisy labels.",
         "minimum": 0.0,
         "type": "number"
        },
        "robust_lambda": {
         "default": 0,
         "description": "Replaces the loss with `(1 - robust_lambda) * loss + robust_lambda / c` where `c` is the number of classes. Useful in case of noisy labels.",
         "minimum": 0,
         "type": "integer"
        },
        "unique": {
         "default": false,
         "description": "If true, the loss is only computed for unique elements in the sequence.",
         "type": "boolean"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "sparsemax": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       }
      },
      "types": [
       "sequence_softmax_cross_entropy",
       "next_token_softmax_cross_entropy",
       "sparsemax",
       "entmax15"
      ]
     }
    },
    "set": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "default_validation_metric": {
       "default": "jaccard",
       "description": "Internal only use parameter: default validation metric for set output feature.",
       "enum": [
        "jaccard"
       ],
       "type": "string"
      },
      "dependencies": {
       "description": "List of input features that this feature depends on.",
       "type": "array"
      },
      "input_size": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "name": {
       "description": "Name of the output feature.",
       "type": "string"
      },
      "num_classes": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "preprocessing": {
       "type": "object"
      },
      "reduce_dependencies": {
       "default": "sum",
       "description": "How to reduce the dependencies of the output feature.",
       "enum": [
        "last",
        "sum",
        "mean",
        "avg",
        "max",
        "concat",
        "attention",
        "attention_pooling",
        "none",
        "None",
        null
       ],
       "nullable": true,
       "type": "string"
      },
      "reduce_input": {
       "default": "sum",
       "description": "How to reduce an input that is not a vector, but a matrix or a higher order tensor, on the first dimension (second if you count the batch dimension)",
       "enum": [
        "last",
        "sum",
        "mean",
        "avg",
        "max",
        "concat",
        "attention",
        "attention_pooling",
        "none",
        "None",
        null
       ],
       "nullable": true,
       "type": "string"
      },
      "threshold": {
       "default": 0.5,
       "description": "The threshold used to convert output probabilities to predictions. Tokens with predictedprobabilities greater than or equal to threshold are predicted to be in the output set (True).",
       "maximum": 1,
       "minimum": 0,
       "type": "number"
      }
     },
     "decoder": {
      "options": {
       "classifier": {
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "calibration": {
         "default": null,
         "description": "Post-training calibration method to apply to the decoder logits. 'temperature_scaling' learns a single scalar T that divides logits (calibrated_logits = logits / T) using NLL minimisation on the validation set. It never changes argmax predictions but improves probability reliability. See: Guo et al., 'On Calibration of Modern Neural Networks', ICML 2017. Set to null (default) to disable calibration.",
         "enum": [
          "temperature_scaling",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the decoder.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "mc_dropout_samples": {
         "default": 0,
         "description": "Number of Monte Carlo forward passes to run at inference time with dropout enabled. When > 0, the decoder is run mc_dropout_samples times and the mean of the resulting probability distributions is used as the prediction; the variance across runs is reported as an 'uncertainty' tensor alongside 'predictions' and 'probabilities'. Setting this to 0 (default) disables MC Dropout. See: Gal & Ghahramani, 'Dropout as a Bayesian Approximation', ICML 2016.",
         "minimum": 0,
         "type": "integer"
        },
        "num_classes": {
         "default": null,
         "description": "Number of classes to predict.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       }
      },
      "types": [
       "classifier"
      ]
     },
     "loss": {
      "options": {
       "sigmoid_cross_entropy": {
        "class_weights": {
         "default": null,
         "description": "Weights to apply to each class in the loss. If not specified, all classes are weighted equally. The value can be a vector of weights, one for each class, that is multiplied to the loss of the datapoints that have that class as ground truth. It is an alternative to oversampling in case of unbalanced class distribution. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too). Alternatively, the value can be a dictionary with class strings as keys and weights as values, like `{class_a: 0.5, class_b: 0.7, ...}`.",
         "type": null
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       }
      },
      "types": [
       "sigmoid_cross_entropy"
      ]
     }
    },
    "text": {
     "common": {
      "class_similarities": {
       "default": null,
       "nullable": true,
       "type": "array"
      },
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "default_validation_metric": {
       "default": "loss",
       "type": "string"
      },
      "dependencies": {
       "description": "List of input features that this feature depends on.",
       "type": "array"
      },
      "input_size": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "name": {
       "description": "Name of the output feature.",
       "type": "string"
      },
      "num_classes": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "preprocessing": {
       "type": "object"
      },
      "reduce_dependencies": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      },
      "reduce_input": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      }
     },
     "decoder": {
      "options": {
       "generator": {
        "beam_length_penalty": {
         "default": 1.0,
         "description": "Length penalty exponent applied to beam search scores. Score = log_prob / (length ^ beam_length_penalty). Values > 1 penalise longer sequences; values < 1 favour them. Only used when beam_width > 1.",
         "minimum": 0.0,
         "type": "number"
        },
        "beam_width": {
         "default": 1,
         "description": "Width of the beam for beam search decoding. 1 = greedy decoding (default). Values > 1 enable beam search at inference time, keeping the top beam_width candidate sequences at each step.",
         "minimum": 1,
         "type": "integer"
        },
        "cell_type": {
         "default": "gru",
         "description": "Type of recurrent cell to use.",
         "enum": [
          "rnn",
          "lstm",
          "gru"
         ],
         "type": "string"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": 256,
         "description": "Size of the input to the decoder.",
         "minimum": 1,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "num_layers": {
         "default": 1,
         "description": "The number of stacked recurrent layers.",
         "minimum": 1,
         "type": "integer"
        },
        "reduce_input": {
         "default": "sum",
         "description": "How to reduce an input that is not a vector, but a matrix or a higher order tensor, on the first dimension (second if you count the batch dimension)",
         "enum": [
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "last"
         ],
         "type": "string"
        },
        "teacher_forcing_decay": {
         "default": "none",
         "description": "Decay schedule for teacher forcing probability during training. none always uses full teacher forcing; linear linearly decays the probability to zero; exponential applies exponential decay. Implements scheduled sampling (Bengio et al., NeurIPS 2015).",
         "enum": [
          "none",
          "linear",
          "exponential"
         ],
         "type": "string"
        },
        "teacher_forcing_decay_rate": {
         "default": 0.01,
         "description": "Rate of decay for the teacher forcing probability per decoding step when teacher_forcing_decay is linear or exponential.",
         "maximum": 1.0,
         "minimum": 0.0,
         "type": "number"
        }
       },
       "tagger": {
        "attention_embedding_size": {
         "default": 256,
         "description": "The embedding size of the multi-head self attention layer.",
         "minimum": 1,
         "type": "integer"
        },
        "attention_num_heads": {
         "default": 8,
         "description": "The number of attention heads in the multi-head self attention layer.",
         "minimum": 1,
         "type": "integer"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": 256,
         "description": "Size of the input to the decoder.",
         "minimum": 1,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "use_attention": {
         "default": false,
         "description": "Whether to apply a multi-head self attention layer before prediction.",
         "type": "boolean"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        }
       },
       "transformer_generator": {
        "beam_length_penalty": {
         "default": 1.0,
         "description": "Length penalty exponent for beam search. Score = log_prob / (length ^ beam_length_penalty). Only active when beam_width > 1.",
         "minimum": 0.0,
         "type": "number"
        },
        "beam_width": {
         "default": 1,
         "description": "Width of the beam for beam search decoding at inference time. 1 = greedy decoding (default). Values > 1 keep the top beam_width candidates at each step.",
         "minimum": 1,
         "type": "integer"
        },
        "d_model": {
         "default": 256,
         "description": "Dimensionality of the transformer decoder layers (embedding size and hidden size). Must match input_size or a projection will be applied.",
         "minimum": 1,
         "type": "integer"
        },
        "dropout": {
         "default": 0.1,
         "description": "Dropout probability applied within transformer decoder layers.",
         "maximum": 1.0,
         "minimum": 0.0,
         "type": "number"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "ffn_size": {
         "default": 1024,
         "description": "Size of the feed-forward network (dim_feedforward) inside each transformer decoder layer.",
         "minimum": 1,
         "type": "integer"
        },
        "input_size": {
         "default": 256,
         "description": "Size of the encoder output (d_model). The encoder output is projected to this size before being used as cross-attention memory in the transformer decoder.",
         "minimum": 1,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "num_heads": {
         "default": 8,
         "description": "Number of attention heads in each multi-head attention sub-layer. d_model must be divisible by num_heads.",
         "minimum": 1,
         "type": "integer"
        },
        "num_layers": {
         "default": 2,
         "description": "Number of transformer decoder layers.",
         "minimum": 1,
         "type": "integer"
        },
        "reduce_input": {
         "default": "sum",
         "description": "How to reduce a 3-D encoder output (batch x seq x hidden) to a 2-D context vector (batch x hidden). Ignored when the encoder output is already 2-D.",
         "enum": [
          "sum",
          "mean",
          "avg",
          "max",
          "concat",
          "last"
         ],
         "type": "string"
        }
       }
      },
      "types": [
       "generator",
       "transformer_generator",
       "tagger"
      ]
     },
     "loss": {
      "options": {
       "entmax15": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "next_token_softmax_cross_entropy": {
        "class_similarities": {
         "default": null,
         "nullable": true,
         "type": "array"
        },
        "class_similarities_temperature": {
         "default": 0,
         "type": "integer"
        },
        "class_weights": {
         "default": null,
         "type": null
        },
        "confidence_penalty": {
         "default": 0,
         "type": "number"
        },
        "robust_lambda": {
         "default": 0,
         "type": "integer"
        },
        "unique": {
         "default": false,
         "type": "boolean"
        },
        "weight": {
         "default": 1.0,
         "type": "number"
        }
       },
       "sequence_softmax_cross_entropy": {
        "class_similarities": {
         "default": null,
         "description": "If not `null` it is a `c x c` matrix in the form of a list of lists that contains the mutual similarity of classes. It is used if `class_similarities_temperature` is greater than 0. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too).",
         "nullable": true,
         "type": "array"
        },
        "class_similarities_temperature": {
         "default": 0,
         "description": "The temperature parameter of the softmax that is performed on each row of `class_similarities`. The output of that softmax is used to determine the supervision vector to provide instead of the one hot vector that would be provided otherwise for each datapoint. The intuition behind it is that errors between similar classes are more tolerable than errors between really different classes.",
         "minimum": 0,
         "type": "integer"
        },
        "class_weights": {
         "default": null,
         "description": "Weights to apply to each class in the loss. If not specified, all classes are weighted equally. The value can be a vector of weights, one for each class, that is multiplied to the loss of the datapoints that have that class as ground truth. It is an alternative to oversampling in case of unbalanced class distribution. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too). Alternatively, the value can be a dictionary with class strings as keys and weights as values, like `{class_a: 0.5, class_b: 0.7, ...}`.",
         "type": null
        },
        "confidence_penalty": {
         "default": 0,
         "description": "Penalizes overconfident predictions (low entropy) by adding an additional term that penalizes too confident predictions by adding a `a * (max_entropy - entropy) / max_entropy` term to the loss, where a is the value of this parameter. Useful in case of noisy labels.",
         "minimum": 0.0,
         "type": "number"
        },
        "robust_lambda": {
         "default": 0,
         "description": "Replaces the loss with `(1 - robust_lambda) * loss + robust_lambda / c` where `c` is the number of classes. Useful in case of noisy labels.",
         "minimum": 0,
         "type": "integer"
        },
        "unique": {
         "default": false,
         "description": "If true, the loss is only computed for unique elements in the sequence.",
         "type": "boolean"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "sparsemax": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       }
      },
      "types": [
       "sequence_softmax_cross_entropy",
       "next_token_softmax_cross_entropy",
       "sparsemax",
       "entmax15"
      ]
     }
    },
    "timeseries": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "default_validation_metric": {
       "default": "mean_squared_error",
       "description": "Internal parameter.",
       "enum": [
        "mean_squared_error"
       ],
       "type": "string"
      },
      "dependencies": {
       "description": "List of input features that this feature depends on.",
       "type": "array"
      },
      "horizon": {
       "default": null,
       "description": "Internal parameter. Obtained from preprocessing",
       "minimum": 1,
       "nullable": true,
       "type": "integer"
      },
      "input_size": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "name": {
       "description": "Name of the output feature.",
       "type": "string"
      },
      "num_classes": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "preprocessing": {
       "type": "object"
      },
      "reduce_dependencies": {
       "default": null,
       "description": "How to reduce the dependencies of the output feature.",
       "enum": [
        "last",
        "sum",
        "mean",
        "avg",
        "max",
        "concat",
        "attention",
        "attention_pooling",
        "none",
        "None",
        null
       ],
       "nullable": true,
       "type": "string"
      },
      "reduce_input": {
       "default": null,
       "description": "How to reduce an input that is not a vector, but a matrix or a higher order tensor, on the first dimension (second if you count the batch dimension)",
       "enum": [
        "last",
        "sum",
        "mean",
        "avg",
        "max",
        "concat",
        "attention",
        "attention_pooling",
        "none",
        "None",
        null
       ],
       "nullable": true,
       "type": "string"
      }
     },
     "decoder": {
      "options": {
       "projector": {
        "activation": {
         "default": null,
         "description": "Indicates the activation function applied to the output.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "clip": {
         "default": null,
         "description": "Clip the output of the decoder to be within the given range.",
         "type": null
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the decoder.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "multiplier": {
         "default": 1.0,
         "description": "Multiplier to scale the activated outputs by. Useful when setting `activation` to something that outputs a value between [-1, 1] like tanh to re-scale values back to order of magnitude of the data you're trying to predict. A good rule of thumb in such cases is to pick a value like `x * (max - min)` where x is a scalar in the range [1, 2]. For example, if you're trying to predict something like temperature, it might make sense to pick a multiplier on the order of `100`.",
         "type": "number"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "output_size": {
         "default": null,
         "description": "Size of the output of the decoder.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       }
      },
      "types": [
       "projector"
      ]
     },
     "loss": {
      "options": {
       "huber": {
        "delta": {
         "default": 1.0,
         "description": "Threshold at which to change between delta-scaled L1 and L2 loss.",
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "mean_absolute_error": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "mean_absolute_percentage_error": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "mean_squared_error": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       }
      },
      "types": [
       "mean_squared_error",
       "mean_absolute_error",
       "mean_absolute_percentage_error",
       "huber"
      ]
     }
    },
    "vector": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "default_validation_metric": {
       "default": "mean_squared_error",
       "description": "Internal only use parameter: default validation metric for binary output feature.",
       "enum": [
        "mean_squared_error"
       ],
       "type": "string"
      },
      "dependencies": {
       "description": "List of input features that this feature depends on.",
       "type": "array"
      },
      "input_size": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "name": {
       "description": "Name of the output feature.",
       "type": "string"
      },
      "num_classes": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "preprocessing": {
       "type": "object"
      },
      "reduce_dependencies": {
       "default": null,
       "description": "How to reduce the dependencies of the output feature.",
       "enum": [
        "last",
        "sum",
        "mean",
        "avg",
        "max",
        "concat",
        "attention",
        "attention_pooling",
        "none",
        "None",
        null
       ],
       "nullable": true,
       "type": "string"
      },
      "reduce_input": {
       "default": null,
       "description": "How to reduce an input that is not a vector, but a matrix or a higher order tensor, on the first dimension (second if you count the batch dimension)",
       "enum": [
        "last",
        "sum",
        "mean",
        "avg",
        "max",
        "concat",
        "attention",
        "attention_pooling",
        "none",
        "None",
        null
       ],
       "nullable": true,
       "type": "string"
      },
      "softmax": {
       "default": false,
       "description": "Determines whether to apply a softmax at the end of the decoder. This is useful for predicting a vector of values that sum up to 1 and can be interpreted as probabilities.",
       "type": "boolean"
      },
      "vector_size": {
       "default": null,
       "description": "The size of the vector. If None, the vector size will be inferred from the data.",
       "minimum": 1,
       "nullable": true,
       "type": "integer"
      }
     },
     "decoder": {
      "options": {
       "projector": {
        "activation": {
         "default": null,
         "description": "Indicates the activation function applied to the output.",
         "enum": [
          "elu",
          "leakyRelu",
          "logSigmoid",
          "relu",
          "sigmoid",
          "tanh",
          "softmax",
          "gelu",
          "silu",
          "swish",
          "mish",
          "selu",
          "prelu",
          "relu6",
          "hardswish",
          "hardsigmoid",
          "softplus",
          "celu",
          "swiglu",
          "geglu",
          "reglu",
          "sparsemax",
          "entmax15",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "clip": {
         "default": null,
         "description": "Clip the output of the decoder to be within the given range.",
         "type": null
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the decoder.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "multiplier": {
         "default": 1.0,
         "description": "Multiplier to scale the activated outputs by. Useful when setting `activation` to something that outputs a value between [-1, 1] like tanh to re-scale values back to order of magnitude of the data you're trying to predict. A good rule of thumb in such cases is to pick a value like `x * (max - min)` where x is a scalar in the range [1, 2]. For example, if you're trying to predict something like temperature, it might make sense to pick a multiplier on the order of `100`.",
         "type": "number"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "output_size": {
         "default": null,
         "description": "Size of the output of the decoder.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       }
      },
      "types": [
       "projector"
      ]
     },
     "loss": {
      "options": {
       "huber": {
        "delta": {
         "default": 1.0,
         "description": "Threshold at which to change between delta-scaled L1 and L2 loss.",
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "mean_absolute_error": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "mean_absolute_percentage_error": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "mean_squared_error": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "nt_xent": {
        "temperature": {
         "default": 0.07,
         "description": "Temperature parameter for scaling the cosine similarity scores. Lower values make the distribution sharper, higher values softer.",
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "softmax_cross_entropy": {
        "class_similarities": {
         "default": null,
         "description": "If not `null` it is a `c x c` matrix in the form of a list of lists that contains the mutual similarity of classes. It is used if `class_similarities_temperature` is greater than 0. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too).",
         "nullable": true,
         "type": "array"
        },
        "class_similarities_temperature": {
         "default": 0,
         "description": "The temperature parameter of the softmax that is performed on each row of `class_similarities`. The output of that softmax is used to determine the supervision vector to provide instead of the one hot vector that would be provided otherwise for each datapoint. The intuition behind it is that errors between similar classes are more tolerable than errors between really different classes.",
         "minimum": 0,
         "type": "integer"
        },
        "class_weights": {
         "default": null,
         "description": "Weights to apply to each class in the loss. If not specified, all classes are weighted equally. The value can be a vector of weights, one for each class, that is multiplied to the loss of the datapoints that have that class as ground truth. It is an alternative to oversampling in case of unbalanced class distribution. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too). Alternatively, the value can be a dictionary with class strings as keys and weights as values, like `{class_a: 0.5, class_b: 0.7, ...}`.",
         "type": null
        },
        "confidence_penalty": {
         "default": 0,
         "description": "Penalizes overconfident predictions (low entropy) by adding an additional term that penalizes too confident predictions by adding a `a * (max_entropy - entropy) / max_entropy` term to the loss, where a is the value of this parameter. Useful in case of noisy labels.",
         "minimum": 0.0,
         "type": "number"
        },
        "robust_lambda": {
         "default": 0,
         "description": "Replaces the loss with `(1 - robust_lambda) * loss + robust_lambda / c` where `c` is the number of classes. Useful in case of noisy labels.",
         "minimum": 0,
         "type": "integer"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       }
      },
      "types": [
       "mean_squared_error",
       "mean_absolute_error",
       "mean_absolute_percentage_error",
       "softmax_cross_entropy",
       "huber",
       "nt_xent"
      ]
     }
    }
   },
   "top_level": {
    "model_type": {
     "default": "ecd",
     "enum": [
      "ecd"
     ],
     "type": "string"
    },
    "preset": {
     "default": null,
     "description": "Quality preset that sets sensible defaults for combiner, trainer, and other settings. User-specified values always take precedence. 'medium_quality': fast training with concat combiner. 'high_quality': transformer combiner with uncertainty loss balancing. 'best_quality': FT-Transformer, uncertainty loss balancing, model soup. 'tabular_realmlp': RealMLP defaults (Holzm\u00fcller et al., 2024) \u2014 robust scaling on number features, deep concat FC stack, AdamW + cosine LR, long training horizon.",
     "enum": [
      "medium_quality",
      "high_quality",
      "best_quality",
      "tabular_realmlp",
      null
     ],
     "nullable": true,
     "type": "string"
    }
   }
  },
  "llm": {
   "input_features": {
    "text": {
     "common": {
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "name": {
       "description": "Name of the input feature.",
       "type": "string"
      },
      "tied": {
       "default": null,
       "nullable": true,
       "type": "string"
      }
     },
     "encoder": {
      "options": {
       "passthrough": {
        "adapter": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "skip": {
         "default": false,
         "type": "boolean"
        }
       }
      },
      "types": [
       "passthrough"
      ]
     },
     "preprocessing": {
      "fields": {
       "cache_encoder_embeddings": {
        "default": false,
        "type": "boolean"
       },
       "compute_idf": {
        "default": false,
        "type": "boolean"
       },
       "computed_fill_value": {
        "default": "<UNK>",
        "type": "string"
       },
       "fill_value": {
        "default": "<UNK>",
        "type": "string"
       },
       "lowercase": {
        "default": false,
        "type": "boolean"
       },
       "max_sequence_length": {
        "default": null,
        "description": "The maximum length (number of tokens) of the sequence. Sequences that are longer than this value will be truncated. Useful as a stopgap measure if `sequence_length` is set to `None`. If `None`, max sequence length will be inferred from the training dataset.",
        "minimum": 1,
        "nullable": true,
        "type": "integer"
       },
       "missing_value_strategy": {
        "default": "fill_with_const",
        "type": "string"
       },
       "most_common": {
        "default": 20000,
        "type": "integer"
       },
       "ngram_size": {
        "default": 2,
        "type": "integer"
       },
       "padding": {
        "default": "right",
        "type": "string"
       },
       "padding_symbol": {
        "default": "<PAD>",
        "type": "string"
       },
       "pretrained_model_name_or_path": {
        "default": null,
        "nullable": true,
        "type": "string"
       },
       "sequence_length": {
        "default": null,
        "nullable": true,
        "type": "integer"
       },
       "tokenizer": {
        "default": "space_punct",
        "type": "string"
       },
       "unknown_symbol": {
        "default": "<UNK>",
        "type": "string"
       },
       "vocab_file": {
        "default": null,
        "nullable": true,
        "type": "string"
       }
      }
     }
    }
   },
   "output_features": {
    "category": {
     "common": {
      "calibration": {
       "default": false,
       "type": "boolean"
      },
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "default_validation_metric": {
       "default": "accuracy",
       "type": "string"
      },
      "dependencies": {
       "description": "List of input features that this feature depends on.",
       "type": "array"
      },
      "input_size": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "name": {
       "description": "Name of the output feature.",
       "type": "string"
      },
      "num_classes": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "preprocessing": {
       "type": "object"
      },
      "reduce_dependencies": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      },
      "reduce_input": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      },
      "top_k": {
       "default": 3,
       "type": "integer"
      }
     },
     "decoder": {
      "options": {
       "category_extractor": {
        "constrain_to_vocabulary": {
         "default": false,
         "description": "When True, restrict generation to tokens that are valid prefixes of the category labels. Implemented via a HuggingFace LogitsProcessor. Requires tokenizer='hf_tokenizer'.",
         "type": "boolean"
        },
        "fallback_label": {
         "default": "",
         "description": "The label to use if the parser fails to parse the input.",
         "nullable": true,
         "type": "string"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": null,
         "nullable": true,
         "type": "integer"
        },
        "match": {
         "default": null,
         "description": "A dictionary of label classes and their corresponding match patterns definitions that will be used to parse the output of the LLM.",
         "type": null
        },
        "match_strategy": {
         "default": "contains",
         "type": "string"
        },
        "max_new_tokens": {
         "default": null,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "pretrained_model_name_or_path": {
         "default": "",
         "nullable": true,
         "type": "string"
        },
        "str2idx": {
         "default": null,
         "description": "A dictionary of label classes and their corresponding indices that will be used to parse the output of the LLM.",
         "type": null
        },
        "tokenizer": {
         "default": "hf_tokenizer",
         "type": "string"
        },
        "vocab_file": {
         "default": "",
         "nullable": true,
         "type": "string"
        }
       },
       "classifier": {
        "bias_initializer": {
         "default": "zeros",
         "description": "Initializer for the bias vector.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        },
        "calibration": {
         "default": null,
         "description": "Post-training calibration method to apply to the decoder logits. 'temperature_scaling' learns a single scalar T that divides logits (calibrated_logits = logits / T) using NLL minimisation on the validation set. It never changes argmax predictions but improves probability reliability. See: Guo et al., 'On Calibration of Modern Neural Networks', ICML 2017. Set to null (default) to disable calibration.",
         "enum": [
          "temperature_scaling",
          null
         ],
         "nullable": true,
         "type": "string"
        },
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": null,
         "description": "Size of the input to the decoder.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "mc_dropout_samples": {
         "default": 0,
         "description": "Number of Monte Carlo forward passes to run at inference time with dropout enabled. When > 0, the decoder is run mc_dropout_samples times and the mean of the resulting probability distributions is used as the prediction; the variance across runs is reported as an 'uncertainty' tensor alongside 'predictions' and 'probabilities'. Setting this to 0 (default) disables MC Dropout. See: Gal & Ghahramani, 'Dropout as a Bayesian Approximation', ICML 2016.",
         "minimum": 0,
         "type": "integer"
        },
        "num_classes": {
         "default": null,
         "description": "Number of classes to predict.",
         "minimum": 1,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "use_bias": {
         "default": true,
         "description": "Whether the layer uses a bias vector.",
         "type": "boolean"
        },
        "weights_initializer": {
         "default": "xavier_uniform",
         "description": "Initializer for the weight matrix.",
         "enum": [
          "uniform",
          "normal",
          "constant",
          "ones",
          "zeros",
          "eye",
          "dirac",
          "xavier_uniform",
          "xavier_normal",
          "kaiming_uniform",
          "kaiming_normal",
          "orthogonal",
          "sparse",
          "identity"
         ],
         "type": "string"
        }
       }
      },
      "types": [
       "classifier",
       "category_extractor"
      ]
     },
     "loss": {
      "options": {
       "corn": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "entmax15": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "entropic_open_set": {
        "background_class": {
         "default": null,
         "description": "Class index that represents 'unknown' or background samples. Samples with this label receive the entropic penalty instead of cross-entropy. Set to None to disable open-set behaviour and fall back to standard cross-entropy.",
         "nullable": true,
         "type": "integer"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "focal": {
        "alpha": {
         "default": 0.25,
         "description": "Weighting factor for the positive class in binary classification. Balances the importance of positive/negative examples.",
         "maximum": 1.0,
         "minimum": 0.0,
         "type": "number"
        },
        "gamma": {
         "default": 2.0,
         "description": "Focusing parameter that reduces the loss contribution from easy examples and extends the range in which an example receives low loss. gamma=0 reduces focal loss to standard cross-entropy.",
         "minimum": 0.0,
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "objectosphere": {
        "background_class": {
         "default": null,
         "description": "Class index that represents 'unknown' or background samples. Samples with this label receive the entropic + magnitude-suppression penalty. Set to None to fall back to standard cross-entropy.",
         "nullable": true,
         "type": "integer"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        },
        "xi": {
         "default": 10.0,
         "description": "Minimum desired logit L2 norm for known-class samples. A hinge term max(0, xi - ||z||)\u00b2 is added to push representations of known inputs above this threshold. Typical values: 1\u201350.",
         "minimum": 0.0,
         "type": "number"
        },
        "zeta": {
         "default": 0.1,
         "description": "Weight applied to the magnitude-suppression term for unknown samples. Higher values more aggressively shrink logit norms for background inputs.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "poly": {
        "epsilon": {
         "default": 1.0,
         "description": "Coefficient for the polynomial correction term. epsilon=0 reduces PolyLoss to standard cross-entropy.",
         "minimum": 0.0,
         "type": "number"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "softmax_cross_entropy": {
        "class_similarities": {
         "default": null,
         "description": "If not `null` it is a `c x c` matrix in the form of a list of lists that contains the mutual similarity of classes. It is used if `class_similarities_temperature` is greater than 0. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too).",
         "nullable": true,
         "type": "array"
        },
        "class_similarities_temperature": {
         "default": 0,
         "description": "The temperature parameter of the softmax that is performed on each row of `class_similarities`. The output of that softmax is used to determine the supervision vector to provide instead of the one hot vector that would be provided otherwise for each datapoint. The intuition behind it is that errors between similar classes are more tolerable than errors between really different classes.",
         "minimum": 0,
         "type": "integer"
        },
        "class_weights": {
         "default": null,
         "description": "Weights to apply to each class in the loss. If not specified, all classes are weighted equally. The value can be a vector of weights, one for each class, that is multiplied to the loss of the datapoints that have that class as ground truth. It is an alternative to oversampling in case of unbalanced class distribution. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too). Alternatively, the value can be a dictionary with class strings as keys and weights as values, like `{class_a: 0.5, class_b: 0.7, ...}`.",
         "type": null
        },
        "confidence_penalty": {
         "default": 0,
         "description": "Penalizes overconfident predictions (low entropy) by adding an additional term that penalizes too confident predictions by adding a `a * (max_entropy - entropy) / max_entropy` term to the loss, where a is the value of this parameter. Useful in case of noisy labels.",
         "minimum": 0.0,
         "type": "number"
        },
        "robust_lambda": {
         "default": 0,
         "description": "Replaces the loss with `(1 - robust_lambda) * loss + robust_lambda / c` where `c` is the number of classes. Useful in case of noisy labels.",
         "minimum": 0,
         "type": "integer"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "sparsemax": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       }
      },
      "types": [
       "softmax_cross_entropy",
       "corn",
       "entropic_open_set",
       "objectosphere",
       "focal",
       "poly",
       "sparsemax",
       "entmax15"
      ]
     }
    },
    "text": {
     "common": {
      "class_similarities": {
       "default": null,
       "nullable": true,
       "type": "array"
      },
      "column": {
       "description": "Name of the column.",
       "type": "string"
      },
      "default_validation_metric": {
       "default": "loss",
       "description": "Internal only use parameter: default validation metric for text output feature for LLMs.",
       "enum": [
        "loss"
       ],
       "type": "string"
      },
      "dependencies": {
       "description": "List of input features that this feature depends on.",
       "type": "array"
      },
      "input_size": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "name": {
       "description": "Name of the output feature.",
       "type": "string"
      },
      "num_classes": {
       "default": null,
       "nullable": true,
       "type": "integer"
      },
      "preprocessing": {
       "type": "object"
      },
      "reduce_dependencies": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      },
      "reduce_input": {
       "default": "sum",
       "nullable": true,
       "type": "string"
      }
     },
     "decoder": {
      "options": {
       "text_extractor": {
        "fc_activation": {
         "default": "relu",
         "nullable": true,
         "type": "string"
        },
        "fc_bias_initializer": {
         "default": "zeros",
         "type": null
        },
        "fc_dropout": {
         "default": 0.0,
         "type": "number"
        },
        "fc_layers": {
         "default": null,
         "type": null
        },
        "fc_norm": {
         "default": null,
         "nullable": true,
         "type": "string"
        },
        "fc_norm_params": {
         "default": null,
         "nullable": true,
         "type": "object"
        },
        "fc_output_size": {
         "default": 256,
         "type": "integer"
        },
        "fc_use_bias": {
         "default": true,
         "type": "boolean"
        },
        "fc_weights_initializer": {
         "default": "xavier_uniform",
         "type": null
        },
        "input_size": {
         "default": null,
         "nullable": true,
         "type": "integer"
        },
        "match_strategy": {
         "default": "contains",
         "type": "string"
        },
        "max_new_tokens": {
         "default": null,
         "nullable": true,
         "type": "integer"
        },
        "num_fc_layers": {
         "default": 0,
         "type": "integer"
        },
        "pretrained_model_name_or_path": {
         "default": "",
         "nullable": true,
         "type": "string"
        },
        "tokenizer": {
         "default": "hf_tokenizer",
         "type": "string"
        },
        "vocab_file": {
         "default": "",
         "nullable": true,
         "type": "string"
        }
       }
      },
      "types": [
       "text_extractor"
      ]
     },
     "loss": {
      "options": {
       "entmax15": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "next_token_softmax_cross_entropy": {
        "class_similarities": {
         "default": null,
         "nullable": true,
         "type": "array"
        },
        "class_similarities_temperature": {
         "default": 0,
         "type": "integer"
        },
        "class_weights": {
         "default": null,
         "type": null
        },
        "confidence_penalty": {
         "default": 0,
         "type": "number"
        },
        "robust_lambda": {
         "default": 0,
         "type": "integer"
        },
        "unique": {
         "default": false,
         "type": "boolean"
        },
        "weight": {
         "default": 1.0,
         "type": "number"
        }
       },
       "sequence_softmax_cross_entropy": {
        "class_similarities": {
         "default": null,
         "description": "If not `null` it is a `c x c` matrix in the form of a list of lists that contains the mutual similarity of classes. It is used if `class_similarities_temperature` is greater than 0. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too).",
         "nullable": true,
         "type": "array"
        },
        "class_similarities_temperature": {
         "default": 0,
         "description": "The temperature parameter of the softmax that is performed on each row of `class_similarities`. The output of that softmax is used to determine the supervision vector to provide instead of the one hot vector that would be provided otherwise for each datapoint. The intuition behind it is that errors between similar classes are more tolerable than errors between really different classes.",
         "minimum": 0,
         "type": "integer"
        },
        "class_weights": {
         "default": null,
         "description": "Weights to apply to each class in the loss. If not specified, all classes are weighted equally. The value can be a vector of weights, one for each class, that is multiplied to the loss of the datapoints that have that class as ground truth. It is an alternative to oversampling in case of unbalanced class distribution. The ordering of the vector follows the category to integer ID mapping in the JSON metadata file (the `<UNK>` class needs to be included too). Alternatively, the value can be a dictionary with class strings as keys and weights as values, like `{class_a: 0.5, class_b: 0.7, ...}`.",
         "type": null
        },
        "confidence_penalty": {
         "default": 0,
         "description": "Penalizes overconfident predictions (low entropy) by adding an additional term that penalizes too confident predictions by adding a `a * (max_entropy - entropy) / max_entropy` term to the loss, where a is the value of this parameter. Useful in case of noisy labels.",
         "minimum": 0.0,
         "type": "number"
        },
        "robust_lambda": {
         "default": 0,
         "description": "Replaces the loss with `(1 - robust_lambda) * loss + robust_lambda / c` where `c` is the number of classes. Useful in case of noisy labels.",
         "minimum": 0,
         "type": "integer"
        },
        "unique": {
         "default": false,
         "description": "If true, the loss is only computed for unique elements in the sequence.",
         "type": "boolean"
        },
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       },
       "sparsemax": {
        "weight": {
         "default": 1.0,
         "description": "Weight of the loss.",
         "minimum": 0.0,
         "type": "number"
        }
       }
      },
      "types": [
       "sequence_softmax_cross_entropy",
       "next_token_softmax_cross_entropy",
       "sparsemax",
       "entmax15"
      ]
     }
    }
   },
   "top_level": {
    "adapter": {
     "default": null,
     "description": "Whether to use parameter-efficient fine-tuning",
     "type": null
    },
    "adapters": {
     "type": "object"
    },
    "base_model": {
     "description": "Base pretrained model to use. This can be one of the presets defined by Ludwig, a fully qualified name of a pretrained model from the HuggingFace Hub, or a path to a directory containing a pretrained model.",
     "type": null
    },
    "generation": {
     "type": "object"
    },
    "is_multimodal": {
     "default": false,
     "description": "When true, treat ``base_model`` as a vision-language model (VLM) \u2014 Qwen2-VL, LLaVA, InternVL, and similar. Ludwig then loads the base via ``AutoModelForVision2Seq`` instead of ``AutoModelForCausalLM`` and uses the multimodal ``AutoProcessor`` for tokenisation / image preprocessing. Fine-tuning input features can then include images alongside text.",
     "type": "boolean"
    },
    "model_parameters": {
     "description": "Configurable model parameters for LLMs.",
     "type": null
    },
    "model_type": {
     "default": "llm",
     "enum": [
      "llm"
     ],
     "type": "string"
    },
    "prompt": {
     "type": "object"
    },
    "quantization": {
     "description": "Set quantization options.",
     "type": null
    },
    "trust_remote_code": {
     "default": false,
     "description": "Whether to trust and execute remote code from the HuggingFace model repository. Required for some models (e.g. Phi-2, Qwen) that use custom architectures. Only enable this for models you trust.",
     "type": "boolean"
    }
   }
  }
 },
 "splits": {
  "options": {
   "datetime": {
    "column": {
     "default": null,
     "description": "The column name to perform datetime splitting on.",
     "nullable": true,
     "type": "string"
    },
    "probabilities": {
     "description": "Proportion of data to split into train, validation, and test sets.",
     "type": "array"
    }
   },
   "fixed": {
    "column": {
     "default": "split",
     "description": "The column name to use for fixed splitting.",
     "type": "string"
    }
   },
   "hash": {
    "column": {
     "default": null,
     "description": "The column name to perform hash splitting on.",
     "nullable": true,
     "type": "string"
    },
    "probabilities": {
     "description": "Proportion of data to split into train, validation, and test sets.",
     "type": "array"
    }
   },
   "random": {
    "probabilities": {
     "description": "Probabilities for splitting data into train, validation, and test sets.",
     "type": "array"
    }
   },
   "stratify": {
    "column": {
     "default": null,
     "description": "The column name to base the stratified splitting on.",
     "nullable": true,
     "type": "string"
    },
    "probabilities": {
     "description": "Probabilities for splitting data into train, validation, and test sets.",
     "type": "array"
    }
   }
  },
  "types": [
   "random",
   "fixed",
   "stratify",
   "datetime",
   "hash"
  ]
 },
 "trainer": {
  "ecd": {
   "fields": {
    "batch_size": {
     "default": "auto",
     "description": "The number of training examples utilized in one training step of the model. If \u2019auto\u2019, the batch size that maximized training throughput (samples / sec) will be used. For CPU training, the tuned batch size is capped at 128 as throughput benefits of large batch sizes are less noticeable without a GPU.",
     "type": null
    },
    "bucketing_field": {
     "default": null,
     "description": "Feature to use for bucketing datapoints",
     "nullable": true,
     "type": "string"
    },
    "checkpoints_per_epoch": {
     "default": 0,
     "description": "Number of checkpoints per epoch. For example, 2 -> checkpoints are written every half of an epoch. Note that it is invalid to specify both non-zero `steps_per_checkpoint` and non-zero `checkpoints_per_epoch`.",
     "minimum": 0,
     "type": "integer"
    },
    "compile": {
     "default": false,
     "description": "Whether to compile the model before training.",
     "type": "boolean"
    },
    "contrastive_pretrain_epochs": {
     "default": 0,
     "description": "Number of epochs of contrastive pre-alignment between per-feature encoders to run before the main training loop. 0 disables pre-alignment (default). A brief warmup (1-3 epochs) is usually enough to pull encoder output spaces into alignment so the downstream combiner sees already-comparable representations. Inspired by CLIP-style alignment (Radford et al., ICML 2021) adapted to Ludwig's multi-encoder ECD architecture.",
     "minimum": 0,
     "type": "integer"
    },
    "contrastive_pretrain_learnable_temperature": {
     "default": true,
     "description": "When True (default), the InfoNCE log-temperature is a trainable parameter following the CLIP convention. Set to False to fix the temperature at contrastive_pretrain_temperature throughout pre-alignment.",
     "type": "boolean"
    },
    "contrastive_pretrain_projection_dim": {
     "default": 128,
     "description": "Width of the shared projection space used during contrastive pre-alignment. The per-feature projection heads are discarded after pre-alignment \u2014 only the updated encoder weights carry forward into the main training loop.",
     "minimum": 1,
     "type": "integer"
    },
    "contrastive_pretrain_temperature": {
     "default": 0.07,
     "description": "Initial InfoNCE temperature for contrastive pre-alignment. Lower values sharpen the softmax. 0.07 matches CLIP's initial value.",
     "minimum": 0.0,
     "type": "number"
    },
    "early_stop": {
     "default": 5,
     "description": "Number of consecutive rounds of evaluation without any improvement on the `validation_metric` that triggers training to stop. Can be set to -1, which disables early stopping entirely.",
     "minimum": -1,
     "type": "integer"
    },
    "effective_batch_size": {
     "default": "auto",
     "description": "The effective batch size is the total number of samples used to compute a single gradient update to the model weights. This differs from `batch_size` by taking `gradient_accumulation_steps` and number of training worker processes into account. In practice, `effective_batch_size = batch_size * gradient_accumulation_steps * num_workers`. If 'auto', the effective batch size is derivied implicitly from `batch_size`, but if set explicitly, then one of `batch_size` or `gradient_accumulation_steps` must be set to something other than 'auto', and consequently will be set following the formula given above.",
     "type": null
    },
    "enable_gradient_checkpointing": {
     "default": false,
     "description": "Whether to enable gradient checkpointing, which trades compute for memory.This is useful for training very deep models with limited memory.",
     "type": "boolean"
    },
    "enable_profiling": {
     "default": false,
     "type": "boolean"
    },
    "epochs": {
     "default": 100,
     "description": "Number of epochs the algorithm is intended to be run over. Overridden if `train_steps` is set",
     "minimum": 1,
     "type": "integer"
    },
    "eval_batch_size": {
     "default": null,
     "description": "Size of batch to pass to the model for evaluation. If it is `0` or `None`, the same value of `batch_size` is used. This is useful to speedup evaluation with a much bigger batch size than training, if enough memory is available. If \u2019auto\u2019, the biggest batch size (power of 2) that can fit in memory will be used.",
     "type": null
    },
    "eval_steps": {
     "default": null,
     "description": "The number of steps to use for evaluation. If None, the entire evaluation set will be used.",
     "minimum": 0,
     "nullable": true,
     "type": "number"
    },
    "evaluate_training_set": {
     "default": false,
     "description": "Whether to evaluate on the entire training set during evaluation. By default, training metrics will be computed at the end of each training step, and accumulated up to the evaluation phase. In practice, computing training set metrics during training is up to 30% faster than running a separate evaluation pass over the training set, but results in more noisy training metrics, particularly during the earlier epochs. It's recommended to only set this to True if you need very exact training set metrics, and are willing to pay a significant performance penalty for them.",
     "type": "boolean"
    },
    "gradient_accumulation_steps": {
     "default": "auto",
     "description": "Number of steps to accumulate gradients over before performing a weight update.",
     "type": null
    },
    "gradient_clipping": {
     "description": "Parameter values for gradient clipping.",
     "type": null
    },
    "increase_batch_size_eval_metric": {
     "default": "loss",
     "description": "Which metric to listen on for increasing the batch size.",
     "type": "string"
    },
    "increase_batch_size_eval_split": {
     "default": "training",
     "description": "Which dataset split to listen on for increasing the batch size.",
     "type": "string"
    },
    "increase_batch_size_on_plateau": {
     "default": 0,
     "description": "The number of times to increase the batch size on a plateau.",
     "minimum": 0,
     "type": "integer"
    },
    "increase_batch_size_on_plateau_patience": {
     "default": 5,
     "description": "How many epochs to wait for before increasing the batch size.",
     "minimum": 0,
     "type": "integer"
    },
    "increase_batch_size_on_plateau_rate": {
     "default": 2.0,
     "description": "Rate at which the batch size increases.",
     "minimum": 0.0,
     "type": "number"
    },
    "layers_to_freeze_regex": {
     "default": null,
     "description": "Freeze specific layers based on provided regex. Freezing specific layers can improve a pretrained model's performance in a number of ways. At a basic level, freezing early layers can prevent overfitting by retaining more general features (beneficial for small datasets). Also can reduce computational resource use and lower overall training time due to less gradient calculations.",
     "nullable": true,
     "type": "string"
    },
    "learning_rate": {
     "default": 0.001,
     "description": "Controls how much to change the model in response to the estimated error each time the model weights are updated. If 'auto', the optimal learning rate is estimated by choosing the learning rate that produces the smallest non-diverging gradient update.",
     "type": null
    },
    "learning_rate_scaling": {
     "default": "linear",
     "description": "Scale by which to increase the learning rate as the number of distributed workers increases. Traditionally the learning rate is scaled linearly with the number of workers to reflect the proportion by which the effective batch size is increased. For very large batch sizes, a softer square-root scale can sometimes lead to better model performance. If the learning rate is hand-tuned for a given number of workers, setting this value to constant can be used to disable scale-up.",
     "enum": [
      "constant",
      "sqrt",
      "linear"
     ],
     "type": "string"
    },
    "learning_rate_scheduler": {
     "description": "Parameter values for learning rate scheduler.",
     "type": "object"
    },
    "loss_balancing": {
     "default": "none",
     "description": "Multi-task loss balancing strategy for models with multiple output features. 'none': static weighted sum (default). 'log_transform': log(1+loss) compression (DB-MTL). 'uncertainty': learnable homoscedastic uncertainty weighting (Kendall et al., CVPR 2018). 'famo': fast adaptive multitask optimization (Liu et al., NeurIPS 2023). 'gradnorm': gradient normalization (Chen et al., ICML 2018). 'nash_mtl': Nash bargaining solution for multi-task weighting (Navon et al., ICML 2022). 'pareto_mtl': Pareto-optimal multi-task learning with preference vectors (Lin et al., NeurIPS 2019).",
     "enum": [
      "none",
      "log_transform",
      "uncertainty",
      "famo",
      "gradnorm",
      "nash_mtl",
      "pareto_mtl"
     ],
     "type": "string"
    },
    "loss_balancing_alpha": {
     "default": 1.5,
     "description": "Asymmetry parameter for gradnorm and smoothing for famo loss balancing.",
     "type": "number"
    },
    "loss_balancing_lr": {
     "default": 0.01,
     "description": "Learning rate for famo loss balancing weight updates.",
     "type": "number"
    },
    "loss_balancing_preference_vector": {
     "default": null,
     "description": "Preference vector used by `loss_balancing: pareto_mtl`. One entry per output feature (in the order they appear in `output_features`), non-negative, normalised internally to sum to 1. Training is steered toward the Pareto-optimal point where the task losses are inversely proportional to this vector. When null, a uniform preference is used.",
     "nullable": true,
     "type": "array"
    },
    "loss_balancing_tchebycheff_weight": {
     "default": 0.5,
     "description": "Mixing weight for `pareto_mtl` between the linear-scalarised term (weight = 1 - tchebycheff_weight) and the Tchebycheff max term (weight = tchebycheff_weight). Pure Tchebycheff (1.0) enforces exact preference adherence but is rough to train; pure linear (0.0) trains smoothly but diverges from the exact preference. Default 0.5 matches Mahapatra & Rajan's 'mixed-exact' scalarisation (ICML 2020).",
     "maximum": 1.0,
     "minimum": 0.0,
     "type": "number"
    },
    "max_batch_size": {
     "default": 1099511627776,
     "description": "Auto batch size tuning and increasing batch size on plateau will be capped at this value. The default value is 2^40.",
     "minimum": 1,
     "nullable": true,
     "type": "integer"
    },
    "modality_dropout": {
     "default": 0.0,
     "description": "Probability of dropping each input feature's encoder output during training. Dropped features are replaced with learnable missing-modality embeddings. Set to 0.0 to disable (default). Improves robustness to missing inputs at inference.",
     "maximum": 1.0,
     "minimum": 0.0,
     "type": "number"
    },
    "model_soup": {
     "default": null,
     "description": "Model soup strategy for averaging top-K checkpoint weights after training. 'uniform': average all top-K checkpoints. 'greedy': greedily add checkpoints that improve validation metric. None to disable (default).",
     "enum": [
      "uniform",
      "greedy",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "model_soup_top_k": {
     "default": 5,
     "description": "Number of top checkpoints to keep for model soup.",
     "minimum": 1,
     "type": "integer"
    },
    "optimizer": {
     "description": "Optimizer type and its parameters. The optimizer is responsble for applying the gradients computed from the loss during backpropagation as updates to the model weights.",
     "type": "object"
    },
    "profiler": {
     "description": "Parameter values for profiling config.",
     "type": "object"
    },
    "regularization_lambda": {
     "default": 0.0,
     "description": "Strength of the regularization.",
     "maximum": 1,
     "minimum": 0,
     "type": "number"
    },
    "regularization_type": {
     "default": "l2",
     "description": "Type of regularization.",
     "enum": [
      "l1",
      "l2",
      "l1_l2",
      null
     ],
     "nullable": true,
     "type": "string"
    },
    "should_shuffle": {
     "default": true,
     "description": "Whether to shuffle batches during training when true.",
     "type": "boolean"
    },
    "skip_all_evaluation": {
     "default": false,
     "type": "boolean"
    },
    "steps_per_checkpoint": {
     "default": 0,
     "description": "How often the model is checkpointed. Also dictates maximum evaluation frequency. If 0 the model is checkpointed after every epoch.",
     "minimum": 0,
     "type": "integer"
    },
    "train_steps": {
     "default": null,
     "description": "Maximum number of training steps the algorithm is intended to be run over. Unset by default. If set, will override `epochs` and if left unset then `epochs` is used to determine training length.",
     "minimum": 1,
     "nullable": true,
     "type": "integer"
    },
    "use_mixed_precision": {
     "default": false,
     "description": "Enable automatic mixed-precision (AMP) during training.",
     "type": "boolean"
    },
    "validation_field": {
     "default": null,
     "description": "The field for which the `validation_metric` is used for validation-related mechanics like early stopping, parameter change plateaus, as well as what hyperparameter optimization uses to determine the best trial. If unset (default), the first output feature is used. If explicitly specified, neither `validation_field` nor `validation_metric` are overwritten.",
     "nullable": true,
     "type": "string"
    },
    "validation_metric": {
     "default": null,
     "description": "Metric from `validation_field` that is used. If validation_field is not explicitly specified, this is overwritten to be the first output feature type's `default_validation_metric`, consistent with validation_field. If the validation_metric is specified, then we will use the first output feature that produces this metric as the `validation_field`.",
     "nullable": true,
     "type": "string"
    }
   }
  },
  "llm": {
   "options": {
    "dpo": {
     "base_learning_rate": {
      "default": 0.0,
      "type": "number"
     },
     "batch_size": {
      "default": 1,
      "type": null
     },
     "bucketing_field": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "checkpoints_per_epoch": {
      "default": 0,
      "type": "integer"
     },
     "compile": {
      "default": false,
      "type": "boolean"
     },
     "contrastive_pretrain_epochs": {
      "default": 0,
      "type": "integer"
     },
     "contrastive_pretrain_learnable_temperature": {
      "default": true,
      "type": "boolean"
     },
     "contrastive_pretrain_projection_dim": {
      "default": 128,
      "type": "integer"
     },
     "contrastive_pretrain_temperature": {
      "default": 0.07,
      "type": "number"
     },
     "dpo_beta": {
      "default": 0.1,
      "description": "Temperature parameter for DPO loss. Controls how much the policy can deviate from the reference model. Lower values keep the policy closer to the reference. Typical range: 0.05 to 0.5.",
      "type": "number"
     },
     "dpo_label_smoothing": {
      "default": 0.0,
      "description": "Label smoothing for DPO preference targets. 0 means no smoothing.",
      "maximum": 0.5,
      "minimum": 0.0,
      "type": "number"
     },
     "dpo_loss_type": {
      "default": "sigmoid",
      "description": "DPO loss variant. 'sigmoid' is the standard DPO loss. 'ipo' is Identity Preference Optimization which uses a squared loss.",
      "enum": [
       "sigmoid",
       "ipo"
      ],
      "type": "string"
     },
     "early_stop": {
      "default": 5,
      "type": "integer"
     },
     "effective_batch_size": {
      "default": "auto",
      "type": null
     },
     "enable_gradient_checkpointing": {
      "default": false,
      "type": "boolean"
     },
     "enable_profiling": {
      "default": false,
      "type": "boolean"
     },
     "epochs": {
      "default": 100,
      "type": "integer"
     },
     "eval_batch_size": {
      "default": 2,
      "type": null
     },
     "eval_steps": {
      "default": null,
      "nullable": true,
      "type": "number"
     },
     "evaluate_training_set": {
      "default": false,
      "type": "boolean"
     },
     "gradient_accumulation_steps": {
      "default": "auto",
      "type": null
     },
     "gradient_clipping": {
      "description": "Parameter values for gradient clipping.",
      "type": null
     },
     "increase_batch_size_eval_metric": {
      "default": "loss",
      "type": "string"
     },
     "increase_batch_size_eval_split": {
      "default": "training",
      "type": "string"
     },
     "increase_batch_size_on_plateau": {
      "default": 0,
      "type": "integer"
     },
     "increase_batch_size_on_plateau_patience": {
      "default": 5,
      "type": "integer"
     },
     "increase_batch_size_on_plateau_rate": {
      "default": 2.0,
      "type": "number"
     },
     "layers_to_freeze_regex": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "learning_rate": {
      "default": 0.001,
      "description": "Controls how much to change the model in response to the estimated error each time the model weights are updated. If 'auto', the optimal learning rate is estimated by choosing the learning rate that produces the smallest non-diverging gradient update.",
      "type": null
     },
     "learning_rate_scaling": {
      "default": "linear",
      "type": "string"
     },
     "learning_rate_scheduler": {
      "description": "Parameter values for learning rate scheduler.",
      "type": "object"
     },
     "loss_balancing": {
      "default": "none",
      "type": "string"
     },
     "loss_balancing_alpha": {
      "default": 1.5,
      "type": "number"
     },
     "loss_balancing_lr": {
      "default": 0.01,
      "type": "number"
     },
     "loss_balancing_preference_vector": {
      "default": null,
      "nullable": true,
      "type": "array"
     },
     "loss_balancing_tchebycheff_weight": {
      "default": 0.5,
      "type": "number"
     },
     "max_batch_size": {
      "default": 1099511627776,
      "nullable": true,
      "type": "integer"
     },
     "modality_dropout": {
      "default": 0.0,
      "type": "number"
     },
     "model_soup": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "model_soup_top_k": {
      "default": 5,
      "type": "integer"
     },
     "optimizer": {
      "description": "Optimizer type and its parameters. The optimizer is responsble for applying the gradients computed from the loss during backpropagation as updates to the model weights.",
      "type": "object"
     },
     "profiler": {
      "description": "Parameter values for profiling config.",
      "type": "object"
     },
     "regularization_lambda": {
      "default": 0.0,
      "type": "number"
     },
     "regularization_type": {
      "default": "l2",
      "nullable": true,
      "type": "string"
     },
     "rejected_column": {
      "default": "rejected",
      "description": "Name of the column containing rejected completions for preference training.",
      "type": "string"
     },
     "should_shuffle": {
      "default": true,
      "type": "boolean"
     },
     "skip_all_evaluation": {
      "default": false,
      "type": "boolean"
     },
     "steps_per_checkpoint": {
      "default": 0,
      "type": "integer"
     },
     "train_steps": {
      "default": null,
      "nullable": true,
      "type": "integer"
     },
     "use_mixed_precision": {
      "default": false,
      "type": "boolean"
     },
     "validation_field": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "validation_metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     }
    },
    "finetune": {
     "base_learning_rate": {
      "default": 0.0,
      "description": "Base learning rate used for training in the LLM trainer.",
      "minimum": 0.0,
      "type": "number"
     },
     "batch_size": {
      "default": 1,
      "description": "The number of training examples utilized in one training step of the model. If `auto`, the batch size that maximized training throughput (samples / sec) will be used.",
      "type": null
     },
     "bucketing_field": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "checkpoints_per_epoch": {
      "default": 0,
      "type": "integer"
     },
     "compile": {
      "default": false,
      "type": "boolean"
     },
     "contrastive_pretrain_epochs": {
      "default": 0,
      "type": "integer"
     },
     "contrastive_pretrain_learnable_temperature": {
      "default": true,
      "type": "boolean"
     },
     "contrastive_pretrain_projection_dim": {
      "default": 128,
      "type": "integer"
     },
     "contrastive_pretrain_temperature": {
      "default": 0.07,
      "type": "number"
     },
     "early_stop": {
      "default": 5,
      "type": "integer"
     },
     "effective_batch_size": {
      "default": "auto",
      "type": null
     },
     "enable_gradient_checkpointing": {
      "default": false,
      "type": "boolean"
     },
     "enable_profiling": {
      "default": false,
      "type": "boolean"
     },
     "epochs": {
      "default": 100,
      "type": "integer"
     },
     "eval_batch_size": {
      "default": 2,
      "description": "Size of batch to pass to the model for evaluation. If it is `0` or `None`, the same value of `batch_size` is used. This is useful to speedup evaluation with a much bigger batch size than training, if enough memory is available. If `auto`, the biggest batch size (power of 2) that can fit in memory will be used.",
      "type": null
     },
     "eval_steps": {
      "default": null,
      "nullable": true,
      "type": "number"
     },
     "evaluate_training_set": {
      "default": false,
      "type": "boolean"
     },
     "gradient_accumulation_steps": {
      "default": "auto",
      "type": null
     },
     "gradient_clipping": {
      "description": "Parameter values for gradient clipping.",
      "type": null
     },
     "increase_batch_size_eval_metric": {
      "default": "loss",
      "type": "string"
     },
     "increase_batch_size_eval_split": {
      "default": "training",
      "type": "string"
     },
     "increase_batch_size_on_plateau": {
      "default": 0,
      "type": "integer"
     },
     "increase_batch_size_on_plateau_patience": {
      "default": 5,
      "type": "integer"
     },
     "increase_batch_size_on_plateau_rate": {
      "default": 2.0,
      "type": "number"
     },
     "layers_to_freeze_regex": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "learning_rate": {
      "default": 0.001,
      "description": "Controls how much to change the model in response to the estimated error each time the model weights are updated. If 'auto', the optimal learning rate is estimated by choosing the learning rate that produces the smallest non-diverging gradient update.",
      "type": null
     },
     "learning_rate_scaling": {
      "default": "linear",
      "type": "string"
     },
     "learning_rate_scheduler": {
      "description": "Parameter values for learning rate scheduler.",
      "type": "object"
     },
     "loss_balancing": {
      "default": "none",
      "type": "string"
     },
     "loss_balancing_alpha": {
      "default": 1.5,
      "type": "number"
     },
     "loss_balancing_lr": {
      "default": 0.01,
      "type": "number"
     },
     "loss_balancing_preference_vector": {
      "default": null,
      "nullable": true,
      "type": "array"
     },
     "loss_balancing_tchebycheff_weight": {
      "default": 0.5,
      "type": "number"
     },
     "max_batch_size": {
      "default": 1099511627776,
      "nullable": true,
      "type": "integer"
     },
     "modality_dropout": {
      "default": 0.0,
      "type": "number"
     },
     "model_soup": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "model_soup_top_k": {
      "default": 5,
      "type": "integer"
     },
     "optimizer": {
      "description": "Optimizer type and its parameters. The optimizer is responsble for applying the gradients computed from the loss during backpropagation as updates to the model weights.",
      "type": "object"
     },
     "profiler": {
      "description": "Parameter values for profiling config.",
      "type": "object"
     },
     "regularization_lambda": {
      "default": 0.0,
      "type": "number"
     },
     "regularization_type": {
      "default": "l2",
      "nullable": true,
      "type": "string"
     },
     "should_shuffle": {
      "default": true,
      "type": "boolean"
     },
     "skip_all_evaluation": {
      "default": false,
      "type": "boolean"
     },
     "steps_per_checkpoint": {
      "default": 0,
      "type": "integer"
     },
     "train_steps": {
      "default": null,
      "nullable": true,
      "type": "integer"
     },
     "use_mixed_precision": {
      "default": false,
      "type": "boolean"
     },
     "validation_field": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "validation_metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     }
    },
    "grpo": {
     "base_learning_rate": {
      "default": 0.0,
      "type": "number"
     },
     "batch_size": {
      "default": 1,
      "type": null
     },
     "bucketing_field": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "checkpoints_per_epoch": {
      "default": 0,
      "type": "integer"
     },
     "compile": {
      "default": false,
      "type": "boolean"
     },
     "contrastive_pretrain_epochs": {
      "default": 0,
      "type": "integer"
     },
     "contrastive_pretrain_learnable_temperature": {
      "default": true,
      "type": "boolean"
     },
     "contrastive_pretrain_projection_dim": {
      "default": 128,
      "type": "integer"
     },
     "contrastive_pretrain_temperature": {
      "default": 0.07,
      "type": "number"
     },
     "early_stop": {
      "default": 5,
      "type": "integer"
     },
     "effective_batch_size": {
      "default": "auto",
      "type": null
     },
     "enable_gradient_checkpointing": {
      "default": false,
      "type": "boolean"
     },
     "enable_profiling": {
      "default": false,
      "type": "boolean"
     },
     "epochs": {
      "default": 100,
      "type": "integer"
     },
     "eval_batch_size": {
      "default": 2,
      "type": null
     },
     "eval_steps": {
      "default": null,
      "nullable": true,
      "type": "number"
     },
     "evaluate_training_set": {
      "default": false,
      "type": "boolean"
     },
     "gradient_accumulation_steps": {
      "default": "auto",
      "type": null
     },
     "gradient_clipping": {
      "description": "Parameter values for gradient clipping.",
      "type": null
     },
     "grpo_beta": {
      "default": 0.04,
      "description": "KL penalty coefficient.",
      "type": "number"
     },
     "grpo_epsilon": {
      "default": 0.2,
      "description": "PPO clipping parameter.",
      "maximum": 1.0,
      "minimum": 0.0,
      "type": "number"
     },
     "grpo_num_generations": {
      "default": 4,
      "description": "Completions to generate per prompt.",
      "minimum": 1,
      "type": "integer"
     },
     "increase_batch_size_eval_metric": {
      "default": "loss",
      "type": "string"
     },
     "increase_batch_size_eval_split": {
      "default": "training",
      "type": "string"
     },
     "increase_batch_size_on_plateau": {
      "default": 0,
      "type": "integer"
     },
     "increase_batch_size_on_plateau_patience": {
      "default": 5,
      "type": "integer"
     },
     "increase_batch_size_on_plateau_rate": {
      "default": 2.0,
      "type": "number"
     },
     "layers_to_freeze_regex": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "learning_rate": {
      "default": 0.001,
      "description": "Controls how much to change the model in response to the estimated error each time the model weights are updated. If 'auto', the optimal learning rate is estimated by choosing the learning rate that produces the smallest non-diverging gradient update.",
      "type": null
     },
     "learning_rate_scaling": {
      "default": "linear",
      "type": "string"
     },
     "learning_rate_scheduler": {
      "description": "Parameter values for learning rate scheduler.",
      "type": "object"
     },
     "loss_balancing": {
      "default": "none",
      "type": "string"
     },
     "loss_balancing_alpha": {
      "default": 1.5,
      "type": "number"
     },
     "loss_balancing_lr": {
      "default": 0.01,
      "type": "number"
     },
     "loss_balancing_preference_vector": {
      "default": null,
      "nullable": true,
      "type": "array"
     },
     "loss_balancing_tchebycheff_weight": {
      "default": 0.5,
      "type": "number"
     },
     "max_batch_size": {
      "default": 1099511627776,
      "nullable": true,
      "type": "integer"
     },
     "modality_dropout": {
      "default": 0.0,
      "type": "number"
     },
     "model_soup": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "model_soup_top_k": {
      "default": 5,
      "type": "integer"
     },
     "optimizer": {
      "description": "Optimizer type and its parameters. The optimizer is responsble for applying the gradients computed from the loss during backpropagation as updates to the model weights.",
      "type": "object"
     },
     "profiler": {
      "description": "Parameter values for profiling config.",
      "type": "object"
     },
     "regularization_lambda": {
      "default": 0.0,
      "type": "number"
     },
     "regularization_type": {
      "default": "l2",
      "nullable": true,
      "type": "string"
     },
     "should_shuffle": {
      "default": true,
      "type": "boolean"
     },
     "skip_all_evaluation": {
      "default": false,
      "type": "boolean"
     },
     "steps_per_checkpoint": {
      "default": 0,
      "type": "integer"
     },
     "train_steps": {
      "default": null,
      "nullable": true,
      "type": "integer"
     },
     "use_mixed_precision": {
      "default": false,
      "type": "boolean"
     },
     "validation_field": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "validation_metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     }
    },
    "kto": {
     "base_learning_rate": {
      "default": 0.0,
      "type": "number"
     },
     "batch_size": {
      "default": 1,
      "type": null
     },
     "bucketing_field": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "checkpoints_per_epoch": {
      "default": 0,
      "type": "integer"
     },
     "compile": {
      "default": false,
      "type": "boolean"
     },
     "contrastive_pretrain_epochs": {
      "default": 0,
      "type": "integer"
     },
     "contrastive_pretrain_learnable_temperature": {
      "default": true,
      "type": "boolean"
     },
     "contrastive_pretrain_projection_dim": {
      "default": 128,
      "type": "integer"
     },
     "contrastive_pretrain_temperature": {
      "default": 0.07,
      "type": "number"
     },
     "early_stop": {
      "default": 5,
      "type": "integer"
     },
     "effective_batch_size": {
      "default": "auto",
      "type": null
     },
     "enable_gradient_checkpointing": {
      "default": false,
      "type": "boolean"
     },
     "enable_profiling": {
      "default": false,
      "type": "boolean"
     },
     "epochs": {
      "default": 100,
      "type": "integer"
     },
     "eval_batch_size": {
      "default": 2,
      "type": null
     },
     "eval_steps": {
      "default": null,
      "nullable": true,
      "type": "number"
     },
     "evaluate_training_set": {
      "default": false,
      "type": "boolean"
     },
     "gradient_accumulation_steps": {
      "default": "auto",
      "type": null
     },
     "gradient_clipping": {
      "description": "Parameter values for gradient clipping.",
      "type": null
     },
     "increase_batch_size_eval_metric": {
      "default": "loss",
      "type": "string"
     },
     "increase_batch_size_eval_split": {
      "default": "training",
      "type": "string"
     },
     "increase_batch_size_on_plateau": {
      "default": 0,
      "type": "integer"
     },
     "increase_batch_size_on_plateau_patience": {
      "default": 5,
      "type": "integer"
     },
     "increase_batch_size_on_plateau_rate": {
      "default": 2.0,
      "type": "number"
     },
     "kto_beta": {
      "default": 0.1,
      "description": "KTO temperature parameter.",
      "type": "number"
     },
     "layers_to_freeze_regex": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "learning_rate": {
      "default": 0.001,
      "description": "Controls how much to change the model in response to the estimated error each time the model weights are updated. If 'auto', the optimal learning rate is estimated by choosing the learning rate that produces the smallest non-diverging gradient update.",
      "type": null
     },
     "learning_rate_scaling": {
      "default": "linear",
      "type": "string"
     },
     "learning_rate_scheduler": {
      "description": "Parameter values for learning rate scheduler.",
      "type": "object"
     },
     "loss_balancing": {
      "default": "none",
      "type": "string"
     },
     "loss_balancing_alpha": {
      "default": 1.5,
      "type": "number"
     },
     "loss_balancing_lr": {
      "default": 0.01,
      "type": "number"
     },
     "loss_balancing_preference_vector": {
      "default": null,
      "nullable": true,
      "type": "array"
     },
     "loss_balancing_tchebycheff_weight": {
      "default": 0.5,
      "type": "number"
     },
     "max_batch_size": {
      "default": 1099511627776,
      "nullable": true,
      "type": "integer"
     },
     "modality_dropout": {
      "default": 0.0,
      "type": "number"
     },
     "model_soup": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "model_soup_top_k": {
      "default": 5,
      "type": "integer"
     },
     "optimizer": {
      "description": "Optimizer type and its parameters. The optimizer is responsble for applying the gradients computed from the loss during backpropagation as updates to the model weights.",
      "type": "object"
     },
     "profiler": {
      "description": "Parameter values for profiling config.",
      "type": "object"
     },
     "regularization_lambda": {
      "default": 0.0,
      "type": "number"
     },
     "regularization_type": {
      "default": "l2",
      "nullable": true,
      "type": "string"
     },
     "rejected_column": {
      "default": "rejected",
      "description": "Column with rejected completions.",
      "type": "string"
     },
     "should_shuffle": {
      "default": true,
      "type": "boolean"
     },
     "skip_all_evaluation": {
      "default": false,
      "type": "boolean"
     },
     "steps_per_checkpoint": {
      "default": 0,
      "type": "integer"
     },
     "train_steps": {
      "default": null,
      "nullable": true,
      "type": "integer"
     },
     "use_mixed_precision": {
      "default": false,
      "type": "boolean"
     },
     "validation_field": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "validation_metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     }
    },
    "none": {
     "base_learning_rate": {
      "default": 0.0,
      "type": "number"
     },
     "batch_size": {
      "default": 1,
      "type": "integer"
     },
     "checkpoints_per_epoch": {
      "default": 0,
      "type": "integer"
     },
     "early_stop": {
      "default": -1,
      "type": "integer"
     },
     "enable_profiling": {
      "default": false,
      "type": "boolean"
     },
     "epochs": {
      "default": 3,
      "type": "integer"
     },
     "eval_batch_size": {
      "default": 2,
      "type": "integer"
     },
     "eval_steps": {
      "default": null,
      "nullable": true,
      "type": "number"
     },
     "evaluate_training_set": {
      "default": false,
      "type": "boolean"
     },
     "learning_rate": {
      "default": 0.0002,
      "description": "Controls how much to change the model in response to the estimated error each time the model weights are updated. If 'auto', the optimal learning rate is estimated by choosing the learning rate that produces the smallest non-diverging gradient update.",
      "type": null
     },
     "packing": {
      "default": false,
      "type": "boolean"
     },
     "packing_max_sequences_per_pack": {
      "default": 8,
      "type": "integer"
     },
     "profiler": {
      "description": "Parameter values for profiling config.",
      "type": "object"
     },
     "should_shuffle": {
      "default": true,
      "type": "boolean"
     },
     "skip_all_evaluation": {
      "default": false,
      "type": "boolean"
     },
     "steps_per_checkpoint": {
      "default": 0,
      "type": "integer"
     },
     "train_steps": {
      "default": null,
      "nullable": true,
      "type": "integer"
     },
     "validation_field": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "validation_metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     }
    },
    "orpo": {
     "base_learning_rate": {
      "default": 0.0,
      "type": "number"
     },
     "batch_size": {
      "default": 1,
      "type": null
     },
     "bucketing_field": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "checkpoints_per_epoch": {
      "default": 0,
      "type": "integer"
     },
     "compile": {
      "default": false,
      "type": "boolean"
     },
     "contrastive_pretrain_epochs": {
      "default": 0,
      "type": "integer"
     },
     "contrastive_pretrain_learnable_temperature": {
      "default": true,
      "type": "boolean"
     },
     "contrastive_pretrain_projection_dim": {
      "default": 128,
      "type": "integer"
     },
     "contrastive_pretrain_temperature": {
      "default": 0.07,
      "type": "number"
     },
     "early_stop": {
      "default": 5,
      "type": "integer"
     },
     "effective_batch_size": {
      "default": "auto",
      "type": null
     },
     "enable_gradient_checkpointing": {
      "default": false,
      "type": "boolean"
     },
     "enable_profiling": {
      "default": false,
      "type": "boolean"
     },
     "epochs": {
      "default": 100,
      "type": "integer"
     },
     "eval_batch_size": {
      "default": 2,
      "type": null
     },
     "eval_steps": {
      "default": null,
      "nullable": true,
      "type": "number"
     },
     "evaluate_training_set": {
      "default": false,
      "type": "boolean"
     },
     "gradient_accumulation_steps": {
      "default": "auto",
      "type": null
     },
     "gradient_clipping": {
      "description": "Parameter values for gradient clipping.",
      "type": null
     },
     "increase_batch_size_eval_metric": {
      "default": "loss",
      "type": "string"
     },
     "increase_batch_size_eval_split": {
      "default": "training",
      "type": "string"
     },
     "increase_batch_size_on_plateau": {
      "default": 0,
      "type": "integer"
     },
     "increase_batch_size_on_plateau_patience": {
      "default": 5,
      "type": "integer"
     },
     "increase_batch_size_on_plateau_rate": {
      "default": 2.0,
      "type": "number"
     },
     "layers_to_freeze_regex": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "learning_rate": {
      "default": 0.001,
      "description": "Controls how much to change the model in response to the estimated error each time the model weights are updated. If 'auto', the optimal learning rate is estimated by choosing the learning rate that produces the smallest non-diverging gradient update.",
      "type": null
     },
     "learning_rate_scaling": {
      "default": "linear",
      "type": "string"
     },
     "learning_rate_scheduler": {
      "description": "Parameter values for learning rate scheduler.",
      "type": "object"
     },
     "loss_balancing": {
      "default": "none",
      "type": "string"
     },
     "loss_balancing_alpha": {
      "default": 1.5,
      "type": "number"
     },
     "loss_balancing_lr": {
      "default": 0.01,
      "type": "number"
     },
     "loss_balancing_preference_vector": {
      "default": null,
      "nullable": true,
      "type": "array"
     },
     "loss_balancing_tchebycheff_weight": {
      "default": 0.5,
      "type": "number"
     },
     "max_batch_size": {
      "default": 1099511627776,
      "nullable": true,
      "type": "integer"
     },
     "modality_dropout": {
      "default": 0.0,
      "type": "number"
     },
     "model_soup": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "model_soup_top_k": {
      "default": 5,
      "type": "integer"
     },
     "optimizer": {
      "description": "Optimizer type and its parameters. The optimizer is responsble for applying the gradients computed from the loss during backpropagation as updates to the model weights.",
      "type": "object"
     },
     "orpo_beta": {
      "default": 0.1,
      "description": "ORPO odds ratio weight.",
      "type": "number"
     },
     "profiler": {
      "description": "Parameter values for profiling config.",
      "type": "object"
     },
     "regularization_lambda": {
      "default": 0.0,
      "type": "number"
     },
     "regularization_type": {
      "default": "l2",
      "nullable": true,
      "type": "string"
     },
     "rejected_column": {
      "default": "rejected",
      "description": "Column with rejected completions.",
      "type": "string"
     },
     "should_shuffle": {
      "default": true,
      "type": "boolean"
     },
     "skip_all_evaluation": {
      "default": false,
      "type": "boolean"
     },
     "steps_per_checkpoint": {
      "default": 0,
      "type": "integer"
     },
     "train_steps": {
      "default": null,
      "nullable": true,
      "type": "integer"
     },
     "use_mixed_precision": {
      "default": false,
      "type": "boolean"
     },
     "validation_field": {
      "default": null,
      "nullable": true,
      "type": "string"
     },
     "validation_metric": {
      "default": null,
      "nullable": true,
      "type": "string"
     }
    }
   },
   "types": [
    "none",
    "finetune",
    "dpo",
    "kto",
    "orpo",
    "grpo"
   ]
  }
 },
 "trainer_blocks": {
  "ecd": {
   "gradient_clipping": {
    "fields": {
     "clipglobalnorm": {
      "default": 0.5,
      "description": "Maximum allowed norm of the gradients",
      "nullable": true,
      "type": "number"
     },
     "clipnorm": {
      "default": null,
      "description": "Maximum allowed norm of the gradients",
      "nullable": true,
      "type": "number"
     },
     "clipvalue": {
      "default": null,
      "description": "Maximum allowed value of the gradients",
      "nullable": true,
      "type": "number"
     }
    }
   },
   "learning_rate_scheduler": {
    "fields": {
     "decay": {
      "default": null,
      "description": "Learning rate decay schedule. Options: 'linear', 'exponential', 'cosine', 'one_cycle', 'inverse_sqrt', 'polynomial', 'wsd'.",
      "enum": [
       "linear",
       "exponential",
       "cosine",
       "one_cycle",
       "inverse_sqrt",
       "polynomial",
       "wsd",
       null
      ],
      "nullable": true,
      "type": "string"
     },
     "decay_rate": {
      "default": 0.96,
      "description": "Decay per epoch (%): Factor to decrease the Learning rate.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "decay_steps": {
      "default": 10000,
      "description": "The number of steps to take in the exponential learning rate decay.",
      "minimum": 1,
      "type": "integer"
     },
     "div_factor": {
      "default": 25.0,
      "description": "Determines the initial learning rate (initial_lr = max_lr / div_factor) for OneCycleLR. Used only when decay='one_cycle'.",
      "type": "number"
     },
     "eta_min": {
      "default": 0,
      "description": "Minimum learning rate allowed for cosine annealing decay. Default: 0.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "final_div_factor": {
      "default": 10000.0,
      "description": "Determines the minimum learning rate (min_lr = initial_lr / final_div_factor) for OneCycleLR. Used only when decay='one_cycle'.",
      "type": "number"
     },
     "inverse_sqrt_warmup_steps": {
      "default": 4000,
      "description": "Number of warmup steps for the inverse square root scheduler. After warmup, the LR decays as 1/sqrt(step). This is the classic Transformer schedule from Vaswani et al. (2017). Used only when decay='inverse_sqrt'.",
      "minimum": 1,
      "type": "integer"
     },
     "max_lr": {
      "default": null,
      "description": "Maximum learning rate for the OneCycleLR scheduler. If None, defaults to the optimizer's base learning rate. Used only when decay='one_cycle'.",
      "nullable": true,
      "type": "number"
     },
     "pct_start": {
      "default": 0.3,
      "description": "Fraction of training steps spent increasing the learning rate in the OneCycleLR scheduler. Used only when decay='one_cycle'.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "polynomial_end_lr": {
      "default": 0.0,
      "description": "Final (minimum) learning rate at the end of polynomial decay. Used only when decay='polynomial'.",
      "type": "number"
     },
     "polynomial_power": {
      "default": 1.0,
      "description": "Power of the polynomial decay. power=1.0 gives linear decay; higher values give more concave decay curves. Used only when decay='polynomial'.",
      "type": "number"
     },
     "reduce_eval_metric": {
      "default": "loss",
      "description": "Metric plateau used to trigger when we reduce the learning rate when `reduce_on_plateau > 0`.",
      "type": "string"
     },
     "reduce_eval_split": {
      "default": "training",
      "description": "Which dataset split to listen on for reducing the learning rate when `reduce_on_plateau > 0`.",
      "type": "string"
     },
     "reduce_on_plateau": {
      "default": 0,
      "description": "How many times to reduce the learning rate when the algorithm hits a plateau (i.e. the performance on the training set does not improve)",
      "minimum": 0,
      "type": "integer"
     },
     "reduce_on_plateau_patience": {
      "default": 10,
      "description": "How many evaluation steps have to pass before the learning rate reduces when `reduce_on_plateau > 0`.",
      "minimum": 0,
      "type": "integer"
     },
     "reduce_on_plateau_rate": {
      "default": 0.1,
      "description": "Rate at which we reduce the learning rate when `reduce_on_plateau > 0`.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "staircase": {
      "default": false,
      "description": "Decays the learning rate at discrete intervals.",
      "type": "boolean"
     },
     "t_0": {
      "default": null,
      "description": "Number of steps before the first restart for cosine annealing decay. If not specified, it will be set to `steps_per_checkpoint`.",
      "minimum": 1,
      "nullable": true,
      "type": "integer"
     },
     "t_mult": {
      "default": 1,
      "description": "Period multiplier after each restart for cosine annealing decay. Defaults to 1, i.e., restart every `t_0` steps. If set to a larger value, the period between restarts increases by that multiplier. For e.g., if t_mult is 2, then the periods would be: t_0, 2*t_0, 2^2*t_0, 2^3*t_0, etc.",
      "minimum": 1,
      "type": "integer"
     },
     "warmup_evaluations": {
      "default": 0,
      "description": "Number of evaluation steps to warmup the learning rate for.",
      "minimum": 0.0,
      "type": "integer"
     },
     "warmup_fraction": {
      "default": 0.0,
      "description": "Fraction of total training steps to warmup the learning rate for.",
      "minimum": 0.0,
      "type": "number"
     },
     "wsd_decay_fraction": {
      "default": 0.1,
      "description": "Fraction of total training steps spent in the decay phase of the WSD scheduler. wsd_warmup_fraction + wsd_stable_fraction + wsd_decay_fraction should sum to 1. Used only when decay='wsd'.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "wsd_stable_fraction": {
      "default": 0.8,
      "description": "Fraction of total training steps spent in the constant LR phase of the WSD scheduler. Used only when decay='wsd'.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "wsd_warmup_fraction": {
      "default": 0.1,
      "description": "Fraction of total training steps spent in the linear warmup phase of the WSD scheduler. Used only when decay='wsd'.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     }
    }
   },
   "optimizer": {
    "options": {
     "adadelta": {
      "eps": {
       "default": 1e-06,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "rho": {
       "default": 0.9,
       "description": "Coefficient used for computing a running average of squared gradients.",
       "maximum": 1,
       "minimum": 0,
       "type": "number"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay ($L2$ penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "adafactor": {
      "lr": {
       "default": null,
       "description": "Learning rate. Set to None (default) when `relative_step=True` so that Adafactor manages its own schedule. Must be provided when `relative_step=False`.",
       "minimum": 0.0,
       "nullable": true,
       "type": "number"
      },
      "relative_step": {
       "default": true,
       "description": "If True, a time-dependent learning rate is computed instead of using the external `lr`. Do not combine with an external LR scheduler.",
       "type": "boolean"
      },
      "scale_parameter": {
       "default": true,
       "description": "If True, the learning rate is scaled by the root mean square of the parameters. Should be True when `relative_step=True`.",
       "type": "boolean"
      },
      "warmup_init": {
       "default": false,
       "description": "If True, the time-dependent learning rate is linearly increased at initialization. Only effective when `relative_step=True`.",
       "type": "boolean"
      }
     },
     "adagrad": {
      "eps": {
       "default": 1e-10,
       "description": "Term added to the denominator to improve numerical stability.",
       "type": "number"
      },
      "initial_accumulator_value": {
       "default": 0,
       "minimum": 0.0,
       "type": "number"
      },
      "lr_decay": {
       "default": 0,
       "description": "Learning rate decay.",
       "type": "number"
      },
      "weight_decay": {
       "default": 0,
       "description": "Weight decay ($L2$ penalty).",
       "type": "number"
      }
     },
     "adagrad_8bit": {
      "block_wise": {
       "default": true,
       "description": "Whether to use block wise update.",
       "type": "boolean"
      },
      "eps": {
       "default": 1e-10,
       "type": "number"
      },
      "initial_accumulator_value": {
       "default": 0,
       "type": "number"
      },
      "lr_decay": {
       "default": 0,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0,
       "type": "number"
      }
     },
     "adam": {
      "amsgrad": {
       "default": false,
       "description": "Whether to use the AMSGrad variant of this algorithm from the paper 'On the Convergence of Adam and Beyond'.",
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "eps": {
       "default": 1e-08,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay (L2 penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "adam_8bit": {
      "amsgrad": {
       "default": false,
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "description": "Whether to use block wise update.",
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "adamax": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "eps": {
       "default": 1e-08,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay ($L2$ penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "adamw": {
      "amsgrad": {
       "default": false,
       "description": "Whether to use the AMSGrad variant of this algorithm from the paper 'On the Convergence of Adam and Beyond'.",
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "eps": {
       "default": 1e-08,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay ($L2$ penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "adamw_8bit": {
      "amsgrad": {
       "default": false,
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "description": "Whether to use block wise update.",
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "lamb": {
      "adam_w_mode": {
       "default": true,
       "description": "Whether to use the AdamW mode of this algorithm from the paper 'Decoupled Weight Decay Regularization'.",
       "type": "boolean"
      },
      "amsgrad": {
       "default": false,
       "description": "Whether to use the AMSGrad variant of this algorithm from the paper 'On the Convergence of Adam and Beyond'.",
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "bias_correction": {
       "default": true,
       "type": "boolean"
      },
      "block_wise": {
       "default": false,
       "description": "Whether to use block wise update.",
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "max_unorm": {
       "default": 1.0,
       "maximum": 1.0,
       "minimum": 0.0,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay (L2 penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "lamb_8bit": {
      "adam_w_mode": {
       "default": true,
       "type": "boolean"
      },
      "amsgrad": {
       "default": false,
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "bias_correction": {
       "default": true,
       "type": "boolean"
      },
      "block_wise": {
       "default": false,
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "max_unorm": {
       "default": 1.0,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "lars": {
      "dampening": {
       "default": 0.0,
       "description": "Dampening for momentum.",
       "maximum": 1.0,
       "minimum": 0.0,
       "type": "number"
      },
      "max_unorm": {
       "default": 1.0,
       "maximum": 1.0,
       "minimum": 0.0,
       "type": "number"
      },
      "momentum": {
       "default": 0.9,
       "description": "Momentum factor.",
       "maximum": 1.0,
       "type": "number"
      },
      "nesterov": {
       "default": false,
       "description": "Enables Nesterov momentum.",
       "type": "boolean"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay (L2 penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "lars_8bit": {
      "dampening": {
       "default": 0.0,
       "type": "number"
      },
      "max_unorm": {
       "default": 1.0,
       "type": "number"
      },
      "momentum": {
       "default": 0.9,
       "type": "number"
      },
      "nesterov": {
       "default": false,
       "type": "boolean"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "lbfgs": {
      "history_size": {
       "default": 100,
       "description": "Update history size.",
       "type": "integer"
      },
      "line_search_fn": {
       "default": null,
       "description": "Line search function to use.",
       "enum": [
        "strong_wolfe",
        null
       ],
       "nullable": true,
       "type": "string"
      },
      "max_eval": {
       "default": null,
       "description": "Maximum number of function evaluations per optimization step. Default: `max_iter` * 1.25.",
       "nullable": true,
       "type": "integer"
      },
      "max_iter": {
       "default": 20,
       "description": "Maximum number of iterations per optimization step.",
       "type": "integer"
      },
      "tolerance_change": {
       "default": 1e-09,
       "description": "Termination tolerance on function value/parameter changes.",
       "minimum": 0.0,
       "type": "number"
      },
      "tolerance_grad": {
       "default": 1e-07,
       "description": "Termination tolerance on first order optimality.",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "lion": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "block_wise": {
       "default": true,
       "description": "Whether to use block wise update.",
       "type": "boolean"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay (L2 penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "lion_8bit": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "type": "boolean"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "muon": {
      "momentum": {
       "default": 0.95,
       "description": "Momentum factor for Nesterov SGD applied before orthogonalization.",
       "maximum": 1.0,
       "minimum": 0.0,
       "type": "number"
      },
      "nesterov": {
       "default": true,
       "description": "If True, use Nesterov momentum (look-ahead gradient) before orthogonalization. The original Muon paper uses Nesterov.",
       "type": "boolean"
      }
     },
     "nadam": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "eps": {
       "default": 1e-08,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "momentum_decay": {
       "default": 0.004,
       "description": "Momentum decay.",
       "minimum": 0.0,
       "type": "number"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay ($L2$ penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "paged_adam": {
      "amsgrad": {
       "default": false,
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "paged_adam_8bit": {
      "amsgrad": {
       "default": false,
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "paged_adamw": {
      "amsgrad": {
       "default": false,
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "paged_adamw_8bit": {
      "amsgrad": {
       "default": false,
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "paged_lion": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "type": "boolean"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "paged_lion_8bit": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "type": "boolean"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "radam": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "eps": {
       "default": 1e-08,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay (L2 penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "rmsprop": {
      "alpha": {
       "default": 0.99,
       "description": "Smoothing constant.",
       "minimum": 0.0,
       "type": "number"
      },
      "centered": {
       "default": false,
       "description": "If True, computes the centered RMSProp, and the gradient is normalized by an estimation of its variance.",
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "momentum": {
       "default": 0.0,
       "description": "Momentum factor.",
       "minimum": 0.0,
       "type": "number"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay ($L2$ penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "rmsprop_8bit": {
      "alpha": {
       "default": 0.99,
       "type": "number"
      },
      "block_wise": {
       "default": true,
       "description": "Whether to use block wise update.",
       "type": "boolean"
      },
      "centered": {
       "default": false,
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "momentum": {
       "default": 0.0,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "schedule_free_adamw": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "warmup_steps": {
       "default": 0,
       "description": "Number of linear warmup steps built into the optimizer. Replaces an external warmup scheduler -- do not combine with one.",
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay (decoupled L2 penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "sgd": {
      "dampening": {
       "default": 0.0,
       "description": "Dampening for momentum.",
       "minimum": 0.0,
       "type": "number"
      },
      "momentum": {
       "default": 0.0,
       "description": "Momentum factor.",
       "minimum": 0.0,
       "type": "number"
      },
      "nesterov": {
       "default": false,
       "description": "Enables Nesterov momentum.",
       "type": "boolean"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay ($L2$ penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "sgd_8bit": {
      "block_wise": {
       "default": false,
       "description": "Whether to use block wise update.",
       "type": "boolean"
      },
      "dampening": {
       "default": 0.0,
       "type": "number"
      },
      "momentum": {
       "default": 0.0,
       "type": "number"
      },
      "nesterov": {
       "default": false,
       "type": "boolean"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "soap": {
      "betas": {
       "default": [
        0.95,
        0.95
       ],
       "description": "Coefficients for the first and second Adam moment estimates run in the Shampoo eigenbasis. Note: SOAP typically uses higher beta1 (0.95) than standard Adam.",
       "type": null
      },
      "weight_decay": {
       "default": 0.01,
       "description": "Weight decay (decoupled L2 penalty, as in AdamW).",
       "minimum": 0.0,
       "type": "number"
      }
     }
    },
    "types": [
     "sgd",
     "sgd_8bit",
     "lbfgs",
     "adam",
     "adam_8bit",
     "paged_adam",
     "paged_adam_8bit",
     "adamw",
     "adamw_8bit",
     "paged_adamw",
     "paged_adamw_8bit",
     "adadelta",
     "adagrad",
     "adagrad_8bit",
     "adamax",
     "nadam",
     "rmsprop",
     "rmsprop_8bit",
     "lamb",
     "lamb_8bit",
     "lars",
     "lars_8bit",
     "lion",
     "lion_8bit",
     "paged_lion",
     "paged_lion_8bit",
     "radam",
     "adafactor",
     "schedule_free_adamw",
     "muon",
     "soap"
    ]
   },
   "profiler": {
    "fields": {
     "active": {
      "default": 3,
      "description": "The number of steps that are actively recorded. Values more than 10 wil dramatically slow down tensorboard loading.",
      "minimum": 0,
      "type": "integer"
     },
     "repeat": {
      "default": 5,
      "description": "The optional number of profiling cycles. Use 0 to profile the entire training run.",
      "minimum": 0,
      "type": "integer"
     },
     "skip_first": {
      "default": 0,
      "description": "The number of steps to skip in the beginning of training.",
      "maximum": 100,
      "minimum": 0,
      "type": "integer"
     },
     "wait": {
      "default": 1,
      "description": "The number of steps to wait profiling.",
      "minimum": 0,
      "type": "integer"
     },
     "warmup": {
      "default": 1,
      "description": "The number of steps for profiler warmup after waiting finishes.",
      "minimum": 0,
      "type": "integer"
     }
    }
   }
  },
  "llm": {
   "gradient_clipping": {
    "fields": {
     "clipglobalnorm": {
      "default": 0.5,
      "description": "Maximum allowed norm of the gradients",
      "nullable": true,
      "type": "number"
     },
     "clipnorm": {
      "default": null,
      "description": "Maximum allowed norm of the gradients",
      "nullable": true,
      "type": "number"
     },
     "clipvalue": {
      "default": null,
      "description": "Maximum allowed value of the gradients",
      "nullable": true,
      "type": "number"
     }
    }
   },
   "learning_rate_scheduler": {
    "fields": {
     "decay": {
      "default": null,
      "description": "Learning rate decay schedule. Options: 'linear', 'exponential', 'cosine', 'one_cycle', 'inverse_sqrt', 'polynomial', 'wsd'.",
      "enum": [
       "linear",
       "exponential",
       "cosine",
       "one_cycle",
       "inverse_sqrt",
       "polynomial",
       "wsd",
       null
      ],
      "nullable": true,
      "type": "string"
     },
     "decay_rate": {
      "default": 0.96,
      "description": "Decay per epoch (%): Factor to decrease the Learning rate.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "decay_steps": {
      "default": 10000,
      "description": "The number of steps to take in the exponential learning rate decay.",
      "minimum": 1,
      "type": "integer"
     },
     "div_factor": {
      "default": 25.0,
      "description": "Determines the initial learning rate (initial_lr = max_lr / div_factor) for OneCycleLR. Used only when decay='one_cycle'.",
      "type": "number"
     },
     "eta_min": {
      "default": 0,
      "description": "Minimum learning rate allowed for cosine annealing decay. Default: 0.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "final_div_factor": {
      "default": 10000.0,
      "description": "Determines the minimum learning rate (min_lr = initial_lr / final_div_factor) for OneCycleLR. Used only when decay='one_cycle'.",
      "type": "number"
     },
     "inverse_sqrt_warmup_steps": {
      "default": 4000,
      "description": "Number of warmup steps for the inverse square root scheduler. After warmup, the LR decays as 1/sqrt(step). This is the classic Transformer schedule from Vaswani et al. (2017). Used only when decay='inverse_sqrt'.",
      "minimum": 1,
      "type": "integer"
     },
     "max_lr": {
      "default": null,
      "description": "Maximum learning rate for the OneCycleLR scheduler. If None, defaults to the optimizer's base learning rate. Used only when decay='one_cycle'.",
      "nullable": true,
      "type": "number"
     },
     "pct_start": {
      "default": 0.3,
      "description": "Fraction of training steps spent increasing the learning rate in the OneCycleLR scheduler. Used only when decay='one_cycle'.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "polynomial_end_lr": {
      "default": 0.0,
      "description": "Final (minimum) learning rate at the end of polynomial decay. Used only when decay='polynomial'.",
      "type": "number"
     },
     "polynomial_power": {
      "default": 1.0,
      "description": "Power of the polynomial decay. power=1.0 gives linear decay; higher values give more concave decay curves. Used only when decay='polynomial'.",
      "type": "number"
     },
     "reduce_eval_metric": {
      "default": "loss",
      "description": "Metric plateau used to trigger when we reduce the learning rate when `reduce_on_plateau > 0`.",
      "type": "string"
     },
     "reduce_eval_split": {
      "default": "training",
      "description": "Which dataset split to listen on for reducing the learning rate when `reduce_on_plateau > 0`.",
      "type": "string"
     },
     "reduce_on_plateau": {
      "default": 0,
      "description": "How many times to reduce the learning rate when the algorithm hits a plateau (i.e. the performance on the training set does not improve)",
      "minimum": 0,
      "type": "integer"
     },
     "reduce_on_plateau_patience": {
      "default": 10,
      "description": "How many evaluation steps have to pass before the learning rate reduces when `reduce_on_plateau > 0`.",
      "minimum": 0,
      "type": "integer"
     },
     "reduce_on_plateau_rate": {
      "default": 0.1,
      "description": "Rate at which we reduce the learning rate when `reduce_on_plateau > 0`.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "staircase": {
      "default": false,
      "description": "Decays the learning rate at discrete intervals.",
      "type": "boolean"
     },
     "t_0": {
      "default": null,
      "description": "Number of steps before the first restart for cosine annealing decay. If not specified, it will be set to `steps_per_checkpoint`.",
      "minimum": 1,
      "nullable": true,
      "type": "integer"
     },
     "t_mult": {
      "default": 1,
      "description": "Period multiplier after each restart for cosine annealing decay. Defaults to 1, i.e., restart every `t_0` steps. If set to a larger value, the period between restarts increases by that multiplier. For e.g., if t_mult is 2, then the periods would be: t_0, 2*t_0, 2^2*t_0, 2^3*t_0, etc.",
      "minimum": 1,
      "type": "integer"
     },
     "warmup_evaluations": {
      "default": 0,
      "description": "Number of evaluation steps to warmup the learning rate for.",
      "minimum": 0.0,
      "type": "integer"
     },
     "warmup_fraction": {
      "default": 0.0,
      "description": "Fraction of total training steps to warmup the learning rate for.",
      "minimum": 0.0,
      "type": "number"
     },
     "wsd_decay_fraction": {
      "default": 0.1,
      "description": "Fraction of total training steps spent in the decay phase of the WSD scheduler. wsd_warmup_fraction + wsd_stable_fraction + wsd_decay_fraction should sum to 1. Used only when decay='wsd'.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "wsd_stable_fraction": {
      "default": 0.8,
      "description": "Fraction of total training steps spent in the constant LR phase of the WSD scheduler. Used only when decay='wsd'.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     },
     "wsd_warmup_fraction": {
      "default": 0.1,
      "description": "Fraction of total training steps spent in the linear warmup phase of the WSD scheduler. Used only when decay='wsd'.",
      "maximum": 1,
      "minimum": 0,
      "type": "number"
     }
    }
   },
   "optimizer": {
    "options": {
     "adadelta": {
      "eps": {
       "default": 1e-06,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "rho": {
       "default": 0.9,
       "description": "Coefficient used for computing a running average of squared gradients.",
       "maximum": 1,
       "minimum": 0,
       "type": "number"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay ($L2$ penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "adafactor": {
      "lr": {
       "default": null,
       "description": "Learning rate. Set to None (default) when `relative_step=True` so that Adafactor manages its own schedule. Must be provided when `relative_step=False`.",
       "minimum": 0.0,
       "nullable": true,
       "type": "number"
      },
      "relative_step": {
       "default": true,
       "description": "If True, a time-dependent learning rate is computed instead of using the external `lr`. Do not combine with an external LR scheduler.",
       "type": "boolean"
      },
      "scale_parameter": {
       "default": true,
       "description": "If True, the learning rate is scaled by the root mean square of the parameters. Should be True when `relative_step=True`.",
       "type": "boolean"
      },
      "warmup_init": {
       "default": false,
       "description": "If True, the time-dependent learning rate is linearly increased at initialization. Only effective when `relative_step=True`.",
       "type": "boolean"
      }
     },
     "adagrad": {
      "eps": {
       "default": 1e-10,
       "description": "Term added to the denominator to improve numerical stability.",
       "type": "number"
      },
      "initial_accumulator_value": {
       "default": 0,
       "minimum": 0.0,
       "type": "number"
      },
      "lr_decay": {
       "default": 0,
       "description": "Learning rate decay.",
       "type": "number"
      },
      "weight_decay": {
       "default": 0,
       "description": "Weight decay ($L2$ penalty).",
       "type": "number"
      }
     },
     "adagrad_8bit": {
      "block_wise": {
       "default": true,
       "description": "Whether to use block wise update.",
       "type": "boolean"
      },
      "eps": {
       "default": 1e-10,
       "type": "number"
      },
      "initial_accumulator_value": {
       "default": 0,
       "type": "number"
      },
      "lr_decay": {
       "default": 0,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0,
       "type": "number"
      }
     },
     "adam": {
      "amsgrad": {
       "default": false,
       "description": "Whether to use the AMSGrad variant of this algorithm from the paper 'On the Convergence of Adam and Beyond'.",
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "eps": {
       "default": 1e-08,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay (L2 penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "adam_8bit": {
      "amsgrad": {
       "default": false,
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "description": "Whether to use block wise update.",
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "adamax": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "eps": {
       "default": 1e-08,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay ($L2$ penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "adamw": {
      "amsgrad": {
       "default": false,
       "description": "Whether to use the AMSGrad variant of this algorithm from the paper 'On the Convergence of Adam and Beyond'.",
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "eps": {
       "default": 1e-08,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay ($L2$ penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "adamw_8bit": {
      "amsgrad": {
       "default": false,
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "description": "Whether to use block wise update.",
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "lamb": {
      "adam_w_mode": {
       "default": true,
       "description": "Whether to use the AdamW mode of this algorithm from the paper 'Decoupled Weight Decay Regularization'.",
       "type": "boolean"
      },
      "amsgrad": {
       "default": false,
       "description": "Whether to use the AMSGrad variant of this algorithm from the paper 'On the Convergence of Adam and Beyond'.",
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "bias_correction": {
       "default": true,
       "type": "boolean"
      },
      "block_wise": {
       "default": false,
       "description": "Whether to use block wise update.",
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "max_unorm": {
       "default": 1.0,
       "maximum": 1.0,
       "minimum": 0.0,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay (L2 penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "lamb_8bit": {
      "adam_w_mode": {
       "default": true,
       "type": "boolean"
      },
      "amsgrad": {
       "default": false,
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "bias_correction": {
       "default": true,
       "type": "boolean"
      },
      "block_wise": {
       "default": false,
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "max_unorm": {
       "default": 1.0,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "lars": {
      "dampening": {
       "default": 0.0,
       "description": "Dampening for momentum.",
       "maximum": 1.0,
       "minimum": 0.0,
       "type": "number"
      },
      "max_unorm": {
       "default": 1.0,
       "maximum": 1.0,
       "minimum": 0.0,
       "type": "number"
      },
      "momentum": {
       "default": 0.9,
       "description": "Momentum factor.",
       "maximum": 1.0,
       "type": "number"
      },
      "nesterov": {
       "default": false,
       "description": "Enables Nesterov momentum.",
       "type": "boolean"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay (L2 penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "lars_8bit": {
      "dampening": {
       "default": 0.0,
       "type": "number"
      },
      "max_unorm": {
       "default": 1.0,
       "type": "number"
      },
      "momentum": {
       "default": 0.9,
       "type": "number"
      },
      "nesterov": {
       "default": false,
       "type": "boolean"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "lbfgs": {
      "history_size": {
       "default": 100,
       "description": "Update history size.",
       "type": "integer"
      },
      "line_search_fn": {
       "default": null,
       "description": "Line search function to use.",
       "enum": [
        "strong_wolfe",
        null
       ],
       "nullable": true,
       "type": "string"
      },
      "max_eval": {
       "default": null,
       "description": "Maximum number of function evaluations per optimization step. Default: `max_iter` * 1.25.",
       "nullable": true,
       "type": "integer"
      },
      "max_iter": {
       "default": 20,
       "description": "Maximum number of iterations per optimization step.",
       "type": "integer"
      },
      "tolerance_change": {
       "default": 1e-09,
       "description": "Termination tolerance on function value/parameter changes.",
       "minimum": 0.0,
       "type": "number"
      },
      "tolerance_grad": {
       "default": 1e-07,
       "description": "Termination tolerance on first order optimality.",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "lion": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "block_wise": {
       "default": true,
       "description": "Whether to use block wise update.",
       "type": "boolean"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay (L2 penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "lion_8bit": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "type": "boolean"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "muon": {
      "momentum": {
       "default": 0.95,
       "description": "Momentum factor for Nesterov SGD applied before orthogonalization.",
       "maximum": 1.0,
       "minimum": 0.0,
       "type": "number"
      },
      "nesterov": {
       "default": true,
       "description": "If True, use Nesterov momentum (look-ahead gradient) before orthogonalization. The original Muon paper uses Nesterov.",
       "type": "boolean"
      }
     },
     "nadam": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "eps": {
       "default": 1e-08,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "momentum_decay": {
       "default": 0.004,
       "description": "Momentum decay.",
       "minimum": 0.0,
       "type": "number"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay ($L2$ penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "paged_adam": {
      "amsgrad": {
       "default": false,
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "paged_adam_8bit": {
      "amsgrad": {
       "default": false,
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "paged_adamw": {
      "amsgrad": {
       "default": false,
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "paged_adamw_8bit": {
      "amsgrad": {
       "default": false,
       "type": "boolean"
      },
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "paged_lion": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "type": "boolean"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "paged_lion_8bit": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "type": null
      },
      "block_wise": {
       "default": true,
       "type": "boolean"
      },
      "percentile_clipping": {
       "default": 100,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "radam": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "eps": {
       "default": 1e-08,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay (L2 penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "rmsprop": {
      "alpha": {
       "default": 0.99,
       "description": "Smoothing constant.",
       "minimum": 0.0,
       "type": "number"
      },
      "centered": {
       "default": false,
       "description": "If True, computes the centered RMSProp, and the gradient is normalized by an estimation of its variance.",
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "description": "Term added to the denominator to improve numerical stability.",
       "minimum": 0.0,
       "type": "number"
      },
      "momentum": {
       "default": 0.0,
       "description": "Momentum factor.",
       "minimum": 0.0,
       "type": "number"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay ($L2$ penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "rmsprop_8bit": {
      "alpha": {
       "default": 0.99,
       "type": "number"
      },
      "block_wise": {
       "default": true,
       "description": "Whether to use block wise update.",
       "type": "boolean"
      },
      "centered": {
       "default": false,
       "type": "boolean"
      },
      "eps": {
       "default": 1e-08,
       "type": "number"
      },
      "momentum": {
       "default": 0.0,
       "type": "number"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "schedule_free_adamw": {
      "betas": {
       "default": [
        0.9,
        0.999
       ],
       "description": "Coefficients used for computing running averages of gradient and its square.",
       "type": null
      },
      "warmup_steps": {
       "default": 0,
       "description": "Number of linear warmup steps built into the optimizer. Replaces an external warmup scheduler -- do not combine with one.",
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay (decoupled L2 penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "sgd": {
      "dampening": {
       "default": 0.0,
       "description": "Dampening for momentum.",
       "minimum": 0.0,
       "type": "number"
      },
      "momentum": {
       "default": 0.0,
       "description": "Momentum factor.",
       "minimum": 0.0,
       "type": "number"
      },
      "nesterov": {
       "default": false,
       "description": "Enables Nesterov momentum.",
       "type": "boolean"
      },
      "weight_decay": {
       "default": 0.0,
       "description": "Weight decay ($L2$ penalty).",
       "minimum": 0.0,
       "type": "number"
      }
     },
     "sgd_8bit": {
      "block_wise": {
       "default": false,
       "description": "Whether to use block wise update.",
       "type": "boolean"
      },
      "dampening": {
       "default": 0.0,
       "type": "number"
      },
      "momentum": {
       "default": 0.0,
       "type": "number"
      },
      "nesterov": {
       "default": false,
       "type": "boolean"
      },
      "percentile_clipping": {
       "default": 100,
       "description": "Percentile clipping.",
       "maximum": 100,
       "minimum": 0,
       "type": "integer"
      },
      "weight_decay": {
       "default": 0.0,
       "type": "number"
      }
     },
     "soap": {
      "betas": {
       "default": [
        0.95,
        0.95
       ],
       "description": "Coefficients for the first and second Adam moment estimates run in the Shampoo eigenbasis. Note: SOAP typically uses higher beta1 (0.95) than standard Adam.",
       "type": null
      },
      "weight_decay": {
       "default": 0.01,
       "description": "Weight decay (decoupled L2 penalty, as in AdamW).",
       "minimum": 0.0,
       "type": "number"
      }
     }
    },
    "types": [
     "sgd",
     "sgd_8bit",
     "lbfgs",
     "adam",
     "adam_8bit",
     "paged_adam",
     "paged_adam_8bit",
     "adamw",
     "adamw_8bit",
     "paged_adamw",
     "paged_adamw_8bit",
     "adadelta",
     "adagrad",
     "adagrad_8bit",
     "adamax",
     "nadam",
     "rmsprop",
     "rmsprop_8bit",
     "lamb",
     "lamb_8bit",
     "lars",
     "lars_8bit",
     "lion",
     "lion_8bit",
     "paged_lion",
     "paged_lion_8bit",
     "radam",
     "adafactor",
     "schedule_free_adamw",
     "muon",
     "soap"
    ]
   },
   "profiler": {
    "fields": {
     "active": {
      "default": 3,
      "description": "The number of steps that are actively recorded. Values more than 10 wil dramatically slow down tensorboard loading.",
      "minimum": 0,
      "type": "integer"
     },
     "repeat": {
      "default": 5,
      "description": "The optional number of profiling cycles. Use 0 to profile the entire training run.",
      "minimum": 0,
      "type": "integer"
     },
     "skip_first": {
      "default": 0,
      "description": "The number of steps to skip in the beginning of training.",
      "maximum": 100,
      "minimum": 0,
      "type": "integer"
     },
     "wait": {
      "default": 1,
      "description": "The number of steps to wait profiling.",
      "minimum": 0,
      "type": "integer"
     },
     "warmup": {
      "default": 1,
      "description": "The number of steps for profiler warmup after waiting finishes.",
      "minimum": 0,
      "type": "integer"
     }
    }
   }
  }
 }
}
